---
title: "Introduction to Stochastic Differential Equations for Generative Machine Learning: A Variational Perspective"
title_zh: 面向生成式机器学习的随机微分方程导论：变分视角
authors: "Ole Winther, Paul Jeha, Sander Dieleman, Andriy Mnih, Manfred Opper, Andrea Dittadi"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31576v1"
tags: ["query:gen-models"]
score: 9.0
evidence: 关于扩散模型、分数匹配和流匹配的教程
tldr: 生成式机器学习中，常微分和随机微分方程推动了图像、视频和生物分子生成等领域的显著进展。本文以变分视角，自包含地介绍微分方程、概率框架及Fokker-Planck方程，推导变分下界ELBO，并将扩散模型、分数匹配和流匹配统一为一般变分方法的特定参数化。通过一维密度建模示例比较不同参数化，展示该框架的灵活性和有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31576v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1636, \"height\": 842, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31576v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1687, \"height\": 1566, \"label\": \"Figure\"}]"
motivation: 为生成模型中的微分方程方法提供统一且自包含的变分视角，促进对扩散模型等技术的理解。
method: 从变分下界ELBO出发，将扩散模型、分数匹配和流匹配视为一般变分框架的特定参数化。
result: 通过一维密度建模示例，比较了不同参数化在生成性能上的差异，证实框架的通用性。
conclusion: 本文为理解随机微分方程在生成建模中的应用提供了统一变分基础，并指导实践中的参数选择。
---

## 摘要
常微分方程和随机微分方程的使用推动了生成式机器学习的重大进展，例如在图像、视频和生物分子生成中的应用。本文提供了一个自成体系且非正式的介绍，涵盖微分方程、在生成式建模中使用它们的概率框架，以及控制微分方程随机变量边缘分布时间演化的Fokker-Planck方程。推导了对数似然的变分下界（证据下界，ELBO），并将其作为讨论扩散模型、分数匹配和流匹配的通用起点。所有这些方法都可以看作是最一般变分方法的特定参数化。一个一维密度建模问题被用作简单示例来比较不同的参数化。

## Abstract
The use of ordinary and stochastic differential equations has led to substantial progress in generative machine learning with applications to, for example, image, video and biomolecule generation. This paper provides a self-contained and informal introduction to the differential equations, the probabilistic framework for using them in generative modeling and the Fokker--Planck equation that governs the temporal evolution of the marginal distribution of the stochastic variables of the differential equations. The variational lower bound on the log-likelihood (the evidence lower bound, ELBO) is derived and used as a general starting point for a discussion of diffusion models, score matching, and flow matching. All of these approaches may be viewed as specific parameterizations of the most general variational approach. A one-dimensional density modeling problem is used as a simple example to compare different parameterizations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：扩散模型、分数匹配和流匹配等生成式机器学习方法在图像、视频、分子生成上取得了显著成功。然而，这些方法在文献中常以不同的视角被呈现（如从SDE、分数函数、流场出发），缺乏一个统一的理论框架。初学者难以理解它们之间的内在联系，同时许多教程要么过于侧重数学严谨性（如依赖Itô微积分和Girsanov定理），要么过于直观而缺乏概率基础。
- **整体目标**：提供一个自包含的、非正式的介绍，将ODEs/SDEs、Fokker–Planck方程、变分下界（ELBO）作为统一的起点，说明扩散、分数匹配和流匹配都可以被视为一般变分方法的特定参数化。作者旨在为读者解锁相关文献，而非全面综述。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：
  - 将生成模型定义为从简单先验 \(p_0(x_0)\) 出发，通过ODE或SDE演化到 \(t=1\)，然后通过似然函数 \(p(y|x_1)\) 将潜变量映射到观测数据。
  - 用变分后验 \(q_1(x_1|y)\)（或更一般的路径变分分布 \(q(X|y)\)）推导ELBO，从而将最大似然学习转化为可优化下界。
  - 通过Fokker–Planck方程，建立ODE和SDE之间转换的关系（概率流ODE），从而允许在同一边际分布下定义确定性/随机过程。
- **关键技术细节**：
  - **ODE ELBO**：利用连续归一化流（CNF）的变分公式（式22-25），通过积分对数概率的散度迹来计算 \(p_1(x_1)\)。
  - **SDE ELBO**：引入与生成模型共享扩散系数的变分SDE，将KL散度分解为时均扩散损失（式36）。关键公式：
    \[
    \mathcal{L}_{\text{SDE ELBO}}(y) = \mathbb{E}_{q_1(x_1|y)}[\log p(y|x_1)] - D_{\text{KL}}(q_0\|p_0) - \frac12\int_0^1 \mathbb{E}_{q_t(x|y)}[\|f - g\|^2_{D^{-1}}] \, dt.
    \]
  - **扩散/分数/流匹配作为特定参数化**：
    - 扩散模型：选择高斯变分边缘 \(q_t(x|y) = \mathcal{N}(x|a_t y, b_t^2 I)\)，将生成网络参数化为信号预测或噪声预测。
    - 分数匹配：令生成漂移为 \(\vec{f}_{\text{score}} = \vec{g} + \nabla\sigma^2 + \sigma^2 s(x,t)\)，损失退化为 \(\mathbb{E}[\|s - \nabla\log q_t\|^2]\)。
    - 流匹配：定义条件流场 \(g(x,y,t)\)，生成速度场 \(v(x,t)\)，损失退化为 \(\mathbb{E}[\|v - g\|^2]\) 的加权积分。

### 3. 实验设计
- **数据集/场景**：一维密度建模，数据来自5个Laplace分布的混合（位置为-2(k-1),...,2(k-1)，尺度=1）。使用 \(n=1000\) 个训练数据点，\(n_{\text{val}}=8000\) 验证点，真实对数似然为 \(-2.9862\)。
- **Benchmark**：以真实数据分布的期望对数似然作为参考。
- **对比方法**：
  - 潜变量ODE（确定性流，Dirac似然）。
  - 流匹配（SDE变体，等权重 \(\sigma=1\)）。
  - 潜变量SDE（两种参数化）：
    - 变分扩散模型（VDM）：线性log-SNR，学习端点。
    - 一般参数化：自由学习 \(\alpha(y,t),\beta(y,t)\)（用神经网络），并在 \(t=1\) 匹配似然宽度 \(\delta\)。
- **评价指标**：验证集/训练集对数似然（ELBO，对于ODE可计算准确似然；对于SDE使用ELBO；流匹配未计算ELBO，仅展示密度图）。

### 4. 资源与算力
- 文中未明确说明使用的GPU型号、数量或训练时长。仅给出了每个方法训练的epoch数和batch size（ODE: 200 epochs, batch=125; 流匹配: 10000 epochs, batch=125; SDE: 2000 epochs, batch=125）。因此无法进行算力对比。

### 5. 实验数量与充分性
- **实验数量**：只进行了一组一维密度建模实验，包含四种模型设置。没有进行消融实验、高维数据集验证或超参数敏感性分析。
- **充分性与公平性**：
  - 优点：示例简单，能直观显示学习到的密度形状，结果与真实分布吻合，验证了框架的可行性。
  - 不足：由于一维问题难度低，所有方法结果接近（ODE: -3.025, 流匹配: 未提供数值, VDM: -3.256, 一般SDE: -3.052），无法充分体现不同方法在复杂任务上的优劣。此外，实验未控制所有可能变量（如VDM和一般SDE使用了不同的网络结构/学习率/epoch数），公平性有限。
  - **偏差风险**：实验仅展示单一随机种子？未提及重复实验次数，可能存在偶然性。

### 6. 论文的主要结论与发现
- **统一变分视角**：扩散、分数匹配、流匹配均可从同一个SDE ELBO出发，通过不同的参数化（信号预测、分数近似、条件流场）导出，具有内在一致性。
- **理论关系**：ODE ELBO ≥ SDE ELBO (因反向KL损失非负)，且SDE ELBO可能因似然函数离散而产生发散。
- **实践观察**：在一维示例上：
  - ODE和流匹配产生相似密度。
  - VDM收敛较慢，方差较大，且学习的 \(\alpha, \beta, \sigma\) 与一般参数化不同，但最终密度相似。
  - 一般参数化的SDE获得更接近参考似然的值（验证 -3.052 vs VDM -3.256）。
- **开放性**：超越单观测点（如时间序列场景）的变分构造仍是未探索的挑战。

### 7. 优点
- **统一框架**：论文成功将多种流行生成模型整合到一个变分下界框架中，推导清晰，减少了读者的认知负担。
- **自包含性**：不需要Itô微积分或Girsanov定理，仅用泰勒展开和有限差分离散化即可导出Fokker–Planck方程和反向SDE，降低了入门门槛。
- **方法论扩展**：展示了如何从任意边际分布（如正态流或其混合）推导出对应的ODE/SDE漂移，允许设计更灵活的变分分布。
- **实验简单有启发性**：一维示例虽然简单，但能直观展示不同参数化的行为差异（如VDM中信号-噪声比的作用）。

### 8. 不足与局限
- **实验覆盖不足**：
  - 仅一维数据集，无法评估高维图像/视频生成任务的性能。
  - 没有消融实验（如不同变分分布、扩散系数参数化、噪声调度的影响）。
  - 没有与同类教程/综述论文进行定性比较。
- **理论深度与实用性的折中**：由于采用非正式推导，某些步骤不够严谨（如路径积分、KL散度分解的严格性），可能让需要严格数学背景的读者感到不足。
- **未覆盖实际应用细节**：如神经网络架构设计、高效采样技术（DDIM、consistency model）、条件生成、分子生成中的对称性约束等。作者明确说明这不是综述，但缺点是对实践指导有限。
- **SDE ELBO的发散问题**：在流匹配部分提到，当似然噪声 \(\delta \to 0\) 或扩散系数 \(\sigma \to \infty\) 时，ELBO可能发散，但未给出解决方法或讨论其实际影响。
- **资源与可重复性**：未提供GPU信息，且Jupyter notebook链接（github.com/olewinther/generative-ode-sde）是否可访问未经验证，可能影响可重复性。
- **单一随机性评估**：实验未报告多次运行的平均值与方差，结论的统计可靠性存疑。

（完）
