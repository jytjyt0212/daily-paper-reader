---
title: Language-Driven Cost Optimization for Autonomous Driving
title_zh: 语言驱动的自动驾驶成本优化
authors: "Diego Martinez-Baselga, Khaled Mustafa, Javier Alonso-Mora"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10974v1"
tags: ["query:self-driving"]
score: 9.0
evidence: 自动驾驶成本优化，利用大语言模型
tldr: 自动驾驶车辆的运动规划依赖于成本函数，但参数调优需要专业知识，难以适应动态场景和用户偏好。本文提出语言驱动框架，利用大语言模型解析结构化场景描述和自然语言用户查询，自动生成风险感知MPPI控制器的成本参数。系统引入人工在环验证，以非技术语言描述拟议行为变更并在部署前确认，用户还可提供反馈迭代优化。仿真实验表明，该方法能直观地将用户意图转化为符合要求的驾驶行为，有效桥接了智能车辆控制系统与最终用户。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10974v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1492, \"height\": 518, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10974v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 881, \"height\": 416, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10974v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1845, \"height\": 866, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10974v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 704, \"height\": 318, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10974v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 790, \"height\": 517, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10974v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 892, \"height\": 405, \"label\": \"Table\"}]"
motivation: 成本函数参数调优依赖专家经验，难以快速适应动态场景和用户偏好，且缺乏直观的交互方式。
method: 提出语言驱动框架，利用LLM解析自然语言查询和场景描述，生成MPPI控制器的成本参数，并加入人工验证与反馈迭代。
result: 在多种真实场景仿真中，该方法成功将用户意图转化为符合要求的驾驶行为，实现直观的行为调整。
conclusion: 该框架以直观方式桥接自动驾驶控制系统与用户，支持自适应行为调整，提升可用性。
---

## 摘要
自动驾驶车辆的驾驶行为通常由其运动规划器的成本函数决定，该函数编码了速度跟踪、平滑性、车道保持和碰撞避免等目标。然而，调整构成该成本函数的参数是一项需要技术专长的挑战性任务，限制了车辆适应不断变化的交通场景或终端用户偏好的能力。本文提出了一种用于自动驾驶中自适应成本设计的语言驱动框架。大语言模型（LLM）解释结构化场景描述和自然语言用户查询，生成应用于风险感知模型预测路径积分（MPPI）控制器的参数。该系统包含一个人机交互验证阶段，在该阶段中，拟议的行为变化以非技术语言描述，并在部署前进行确认。用户还可以在部署前后提供反馈，从而实现对车辆运动行为的迭代优化。该框架在多个查询场景下，在逼真的驾驶环境中评估其有效性。仿真结果表明，该方法能够以直观的方式成功引发与预期要求一致的行为变化，从而弥合智能车辆控制系统与终端用户之间的差距。

## Abstract
The driving behavior of autonomous vehicles is typically governed by the cost function of their motion planner, which encodes objectives such as speed tracking, smoothness, lane keeping, and collision avoidance. However, tuning the parameters that shape this cost function is a challenging task that requires technical expertise, limiting the vehicle's ability to adapt to evolving traffic scenarios or end-user preferences. This work presents a language-driven framework for adaptive cost design in autonomous driving. A Large Language Model (LLM) interprets structured scenario descriptions and natural language user queries to generate the parameters applied to a risk-aware Model Predictive Path Integral (MPPI) controller. The system incorporates a human-in-the-loop validation stage in which the proposed behavioral changes are described in non-technical language and confirmed prior to deployment. Users may additionally provide feedback either before or after deployment, enabling iterative refinement of the vehicle's motion behavior. The framework is evaluated across multiple queries in realistic driving scenarios to assess its effectiveness. Simulation results demonstrate that the method successfully induces behavioral changes that align with the intended requirements in an intuitive manner, thereby bridging the gap between intelligent vehicle control systems and end users.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，生成结构化的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：自动驾驶车辆的运动规划行为由其成本函数决定，但成本函数参数的调优通常需要专家离线手动完成，且一旦部署便固定不变。这导致车辆无法动态适应变化的交通场景或非专业用户的个人偏好（如“开快点”、“开稳点”）。如何将用户用自然语言表达的驾驶意图，直观、安全地转化为运动规划器的定量参数调整，是本文试图解决的核心问题。
- **整体含义**：论文提出一种**语言驱动的自适应成本设计框架**，利用大语言模型（LLM）作为“翻译器”，将自然语言查询与场景描述映射到风险感知模型预测路径积分（MPPI）控制器的成本权重上。该系统通过引入人机交互验证与迭代反馈机制，旨在弥合智能车辆控制系统与最终用户（尤其是非专家用户）之间的鸿沟，实现直观、灵活且安全的驾驶行为个性化。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将LLM作为高层推理与翻译模块，将人类的抽象语言指令转化为底层控制器（MPPI）的定量参数。
- **关键技术细节**：
    - **整体架构**：系统包含一个在闭环中高频运行（100ms）的**MPPI运动规划器**，和一个按需运行的**LLM模块**。两者通过参数更新连接。
    - **成本函数设计**：MPPI的成本函数 `Sk` 是多个加权项的线性组合，包括：
        - `ωt * C_tracking` (车道保持/偏离惩罚)
        - `ωs * C_speed` (速度跟踪)
        - `ωst * C_steering` (转向角平滑性)
        - `C_control` (控制输入平滑性，由 `ωa`、`ωstr` 加权)
        - `C_risk` (碰撞风险，分为软惩罚 `ωsoft` 和硬约束 `ωhard`)
    - **LLM模块设计**：
        - **结构化提示**：使用精心设计的提示模板（Chain-of-Thought），引导LLM先输出**意图解释**，再输出**整数权重组**（`λi ∈ {1,...,10}`），最后输出给用户的**非技术语言描述**。
        - **权重离散化**：为解决LLM在浮点数处理上的缺陷，将权重空间离散化为整数，实际连续权重 `ω = ω̂ * (λ / λ̄)`，其中 `ω̂` 是专家定义的标称值。
        - **场景与用户输入**：LLM的输入包括当前场景描述 (`st`) 和用户查询 (`qt`)，并通过保留对话历史实现上下文感知。
    - **用户交互机制**：
        - **部署前验证循环**：LLM生成的参数变更必须先以非技术语言向用户描述，用户确认后才能部署。用户可提供反馈，LLM据此修正，直到用户满意。
        - **运行时在线细化**：车辆运行时，用户可随时提出新查询，与历史对话合并，实现迭代调整。

### 3. 实验设计：数据集、场景、基准、对比方法

- **数据集与场景**：基于 **NuPlan** 数据集中的场景。主要使用了两个场景：
    1.  **合作变道场景**：自车需并入相邻车道，有交互车流。
    2.  **匝道合并场景**：自车需先减速让行，再加速汇入主流。
- **基准/对比方法**：**本文未设置显式的数值基准方法进行对比**。实验主要是**定性+定量地展示**其方法本身在不同用户查询下的行为变化有效性。对比主要体现在不同查询（如平稳 vs 快速）产生的参数和行为指标的差异，以及不同场景描述（保守 vs 激进）产生的结果的对比。
- **关键实现参数**：
    - 基础LLM：`GPT-4o-mini` (温度参数为0)。
    - 预测模型：使用SMART (`[38]`) 进行周围车辆轨迹预测。
    - MPPI控制器：使用 `PyTorch` 实现。

### 4. 资源与算力

- **文中未明确说明**所用的GPU型号、数量或训练时长。仅提及在PyTorch中实现的MPPI能达到100ms的控制频率，但未提及硬件规格。LLM调用的是商用模型 `GPT-4o-mini`，其算力消耗主要在网络延迟而非用户本地算力。

### 5. 实验数量与充分性

- **实验数量**：
    - **查询适应实验**：进行了8个独立查询（q1到q8），其中4个带有顺序查询（两个查询为一组，如q1-q2），共计12种参数配置。每个配置在同一合作变道场景下运行一次，收集指标。
    - **验证循环实验**：展示了4组用户-LLM交互对话（图5），并给出了对应的参数（q1-q2、q3-q4等）和指标。
    - **场景规范实验**：在匝道合并场景下，比较了两种特定场景描述（保守s1 vs 激进s2）以及它们的组合（s1-s2）的效果。
- **充分性评估**：
    - **优点**：实验覆盖了不同类型的用户查询（舒适、快速、保守、安全优先等），并验证了顺序查询和场景切换的适应性。指标（平均速度、加速度、转向率、行驶距离等）能从多个维度量化行为变化。
    - **不足**：
        - **缺乏统计可靠性**：每个查询或参数配置**仅运行一次**，未提及多次重复实验后取平均值，无法评估结果的稳定性和随机性。
        - **缺乏基准对比**：没有与传统的固定参数、基于规则的参数调整、或其他LLM方法进行量化对比。只说明了“能行”，没有说明“更好”或“更差”。
        - **场景单一**：主要在两个特定场景（合作变道、匝道合并）中评估，未覆盖更多元的城市交通场景（如环岛、十字路口、行人密集区等）。
        - **自由度有限**：用户查询仅调整了权重和参考速度 `vref`，未评估对其他复杂参数（如风险阈值 `σ`）的调整能力。

### 6. 论文的主要结论与发现

1.  **方法有效**：所提出的语言驱动框架能够**系统地**根据用户的不同自然语言查询（如“头晕”、“快点”、“保持安全”），自动调整MPPI成本函数的参数，产生与之匹配的驾驶行为。
2.  **直观性**：生成的参数变化（如降低舒适性相关权重、提高速度权重）与用户的直观预期一致，证明了LLM能正确理解并转化抽象指令。
3.  **人机交互的有效性**：验证循环使用户能够**在部署前纠正**LLM的误解（如用户说“保守”但LLM调得太慢，用户可反馈“别太慢”进行修正），有效避免了不期望的行为被执行，增强了安全性和用户信任。
4.  **场景适应性**：框架能够根据不同的场景描述（如“安全优先” vs “快速汇入”），生成截然不同的驾驶策略（保守避让 vs 激进汇入），并支持在场景切换时动态调整方案。

### 7. 优点：方法或实验设计上的亮点

- **方法论层面**：
    - **直观的接口**：用自然语言作为人与控制系统的接口，极大地降低了使用门槛，尤其对非专业用户友好。
    - **安全性设计**：引入人机交互验证环节，是本文的一大核心亮点。它将LLM的“不完美”控制在一个安全边界内，避免错误的参数直接导致危险行为。
    - **风险感知**：在MPPI成本函数中明确建模了概率性的碰撞风险（软惩罚+硬约束），确保了底层控制器的安全性基础。
    - **上下文记忆**：支持对话历史的连续查询，允许用户进行渐进式的精细化调整。
- **实验设计层面**：
    - **多维度指标**：不仅报告了最终参数，还通过平均速度、加速度、转向率等指标量化了行为变化，使结果可视化、可比较。
    - **具体交互示例**：通过图5展示了具体的用户-LLM对话过程，直观展示了验证循环的工作机制及如何纠正误解，增强了论文的可信度。

### 8. 不足与局限

- **LLM的可靠性问题**：如论文自身引用文献`[36]`所指出的，LLM生成的解释可能并不可靠（“不忠的解释”）。虽然验证环节可以缓解，但用户仍可能被错误解释误导，或需要多次反馈才能得到正确结果，效率有待验证。
- **实验的局限性**：
    - **场景覆盖面窄**：仅测试了两个基础场景，缺乏在复杂交通情况下的泛化能力验证。
    - **缺乏统计严谨性**：单次运行的实验结果无法证明方法的稳定性，存在随机噪声影响结果的可能性。
    - **缺失基准对比**：未与传统方法（如专家调参、手动规则）或其他LLM驱动方法（如端到端生成控制指令）进行定量对比，无法清晰定位其性能水平。
- **应用限制**：
    - **依赖外部LLM**：需要联网调用GPT模型，存在延迟和成本问题，不适用于完全离线的车载系统。
    - **简单参数空间**：仅调整了成本权重和参考速度，对于更复杂的驾驶行为（如明确的路径选择、换道时机等）可能能力不足。
    - **未集成感知**：如论文结论所述，场景描述是手动输入的，未与自动的感知和理解模块（视觉语言模型）集成，限制了其全自主运行的能力。

（完）
