---
title: "CLEAR: Cognition and Latent Evaluation for Adaptive Routing in End-to-End Autonomous Driving"
title_zh: CLEAR：端到端自动驾驶中用于自适应路由的认知与潜在评估
authors: "Yining Xing, Zehong Ke, Zhiyuan Liu, Yanbo Jiang, Wenhao Yu, Jianqiang Wang"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06219v1"
tags: ["query:self-driving"]
score: 9.0
evidence: 端到端自动驾驶结合生成规划和VAE
tldr: 端到端自动驾驶扩散模型迭代采样延迟高，难以满足实时需求。CLEAR框架采用单步条件漂移替代多步去噪，在VAE潜空间生成候选轨迹，结合微调Qwen3.5-0.8B提取场景隐状态，自适应调度条件系数与样本数。在NAVSIM v1上PDMS达93.7，实现高效高保真多模态规划，无需密集标注或迭代采样。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06219v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1168, \"height\": 689}, {\"url\": \"assets/figures/arxiv/2606.06219v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1442, \"height\": 743}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06219v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1456, \"height\": 550}, {\"url\": \"assets/tables/arxiv/2606.06219v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1456, \"height\": 373}, {\"url\": \"assets/tables/arxiv/2606.06219v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1458, \"height\": 210}]"
motivation: 扩散模型迭代去噪延迟过高，无法满足端到端自动驾驶的实时推理要求，需要一种平衡多模态多样性与速度的规划方案。
method: CLEAR使用Drive-JEPA编码器，在VAE潜空间单步条件漂移生成轨迹，微调Qwen3.5-0.8B提取场景特征，自适应调度α与N，交叉注意力选择最优轨迹。
result: 在NAVSIM v1上PDMS达93.7，推理速度快，实现高保真多模态规划。
conclusion: 高效多模态规划无需几何标注或迭代采样，为端到端自动驾驶提供实时部署方案。
---

## 摘要
端到端自动驾驶模型通常难以平衡多模态机动生成与实时推理约束。虽然扩散模型成功捕捉了多样化的驾驶行为，但其迭代去噪过程对于安全关键部署会带来不可接受的延迟。为解决此问题，我们提出了CLEAR（用于自适应路由的认知与潜在评估），一个结合超快速生成规划与深度语义推理的框架。CLEAR采用Drive-JEPA作为视觉编码器，并用VAE潜在空间中的单步条件漂移替代多步去噪链，引入条件系数以平衡多样性与专家精度。同时，我们在驾驶问答对上完全微调Qwen~3.5~0.8B，以提取场景感知的隐藏状态。这些状态同时指导自适应调度器（从预定义方案的离散集合中选择条件系数α和样本数N）和交叉注意力评分器（从候选中选择最优轨迹）。在NAVSIM v1基准上，CLEAR达到了最先进的PDMS 93.7。我们的结果表明，无需密集的几何标注或迭代采样，即可高效执行高保真度、多模态规划。

## Abstract
End-to-end autonomous driving models often struggle to balance multi-modal maneuver generation with real-time inference constraints. While diffusion models successfully capture diverse driving behaviors, their iterative denoising process incurs unacceptable latency for safety-critical deployment. To address this, we propose CLEAR (Cognition and Latent Evaluation for Adaptive Routing), a framework that combines ultra-fast generative planning with deep semantic reasoning. CLEAR employs Drive-JEPA as the visual encoder and replaces the multi-step denoising chain with a single-step conditional drift in a VAE latent space, introducing a conditioning coefficient to balance diversity and expert precision. Meanwhile, we fully fine-tune Qwen~3.5~0.8B on driving QA pairs to extract scene-aware hidden states. These states guide both an Adaptive Scheduler, which selects the conditioning coefficient $α$ and sample count $N$ from a discrete set of predefined schemes, and a cross-attention scorer that selects the optimal trajectory from candidates. On the NAVSIM v1 benchmark, CLEAR achieves a state-of-the-art PDMS of 93.7. Our results demonstrate that high-fidelity, multi-modal planning can be executed efficiently without dense geometric annotations or iterative sampling.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：端到端自动驾驶中，扩散模型虽然能生成多模态轨迹（如左转、直行、让行），但其迭代去噪过程需要数十至数百次前向传播，延迟远超安全关键场景要求的100ms控制预算，无法实时部署。
- **背景**：现有方法要么采用确定性回归导致模式平均（物理上不可行），要么使用加速采样器（牺牲质量），或依赖大型多模态语言模型直接生成轨迹（因自回归延迟和格式不稳定而不可行）。
- **整体含义**：CLEAR提出一种兼顾多模态覆盖与实时性的新范式，证明**高保真多模态规划可无需密集几何标注或迭代采样**，通过单步生成+轻量LLM语义推理实现高效闭环规划。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将扩散模型的多步去噪替换为**VAE潜空间中的单步条件漂移**，利用预训练视觉编码器提取几何特征，微调轻量LLM（Qwen 3.5 0.8B）提取深层语义特征，驱动自适应采样策略和轨迹评分。
- **关键技术细节**：
  - **单步条件漂移**：在VAE潜空间中，每个候选轨迹通过一个目标向量 \( V_i = A_i - R_i \) 进行单步漂移。其中吸引力项 \( A_i = (1-\alpha) \cdot V_{\text{pos}(i)} + \alpha \cdot V_{\text{GT}} \) 融合了正样本多样性（低α）和专家精度（高α）；排斥项 \( R_i \) 为其他候选的平均潜码，促进多样性。损失包括漂移损失和赢家通吃损失。
  - **自适应调度器**：基于LLM隐藏状态，从K个预定义方案（α, N）中选择当前场景最优的采样参数（α控制漂移强度，N为候选数）。通过交叉熵训练，监督信号来自NAVSIM官方评分器。
  - **CLEAR解码器**：采用MLP-Mixer架构，通过可学习场景查询交叉注意力压缩视觉特征，注入条件系数α（通过自适应层归一化）。输出经预拟合的PCA投影（低通滤波）生成物理轨迹。
  - **交叉注意力评分器**：以解码器输出特征为查询，LLM隐藏状态为记忆，通过Transformer解码器预测每个候选的PDMS分数，结合排序损失和MSE损失训练，最终选择最高分轨迹。
- **算法流程**（文字说明）：
  1. 输入前视图像+导航命令 → 冻结的Drive-JEPA提取视觉特征，微调后的Qwen提取语义隐藏状态。
  2. 自适应调度器根据隐藏状态输出(α, N)。
  3. 解码器在VAE潜空间内单次前向生成N个候选轨迹，经PCA投影得到物理轨迹。
  4. 交叉注意力评分器评估候选，选出最优轨迹执行。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：NAVSIM（包含闭环仿真指标，无需3D标注）。训练数据：约13万条轨迹用于生成预训练（VAE+解码器）；来自ReCogDrive的15万对驾驶QA用于LLM微调（覆盖1.7万个场景）；1万个场景用于合成对比数据集（网格搜索N∈{16,64,256}，α取6个等级，共约(16+64+256)×6个轨迹/场景）以训练调度器和评分器。
- **基准**：NAVSIM v1（PDMS）和 v2（EPDMS）。
- **对比方法**：GoalFlow、DiffusionDrive、ReCogDrive、iPAD、DriveSuprim、Drive-JEPA（v1）；Transfuser、HydraMDP++、DriveSuprim、iPAD、Drive-JEPA（v2，区分ResNet34和ViT/L骨干）。均为近两年SOTA方法。

### 4. 资源与算力

- **论文未明确说明使用的GPU型号、数量、训练时长**。仅提到多阶段训练（VAE预训练、解码器500轮、LLM微调20轮、调度器+评分器100轮），但具体硬件配置（如A100数量）和总计算时间未提及。

### 5. 实验数量与充分性

- **实验数量**：
  - 主实验：NAVSIM v1（表1）和v2（表2）的完整指标对比。
  - 消融实验（表3）：3组，分别剔除LLM评分器和自适应调度器，分析各模块贡献。
  - 可视化分析（图2）：展示不同α下训练过程中的轨迹分布演变（500个epoch，4个场景）。
- **充分性判断**：
  - **充分**：对比了多种最新方法（扩散、流匹配、蒸馏、JEPA等），覆盖多指标；消融实验清晰验证了LLM驱动组件的必要性。
  - **客观公平**：所有方法在同基准（NAVISOM闭环节评测）下比较，且CLEAR使用了更轻量级的LLM（0.8B）而非更大模型，对比公平。
  - **不足**：缺少在实际物理车辆或开放道路上的测试；v2中CLEAR在车道保持（LK）和交通信号灯（TL）指标上落后于部分方法，论文未深入分析原因。

### 6. 论文的主要结论与发现

- **主要结论**：单步条件漂移可代替扩散模型的迭代去噪，在VAE潜空间生成多模态轨迹，且速度达99FPS。
- **发现**：
  - LLM隐藏状态（而非其文本输出）是驱动自适应调度和轨迹评分的关键，能够编码场景复杂度和交通语义。
  - 相比于视觉特征，LLM语义特征能显著提升轨迹选择质量（消融实验显示PDMS从93.1提升至93.3，再至93.7）。
  - 高精度闭环规划无需密集3D感知或大规模MLLM，紧凑编码器+单步解码器即可达到SOTA。

### 7. 优点

- **方法创新性**：
  - 首次将单步漂移生成（源于Generative Modeling via Drifting）引入轨迹规划，实现速度与多样性的权衡。
  - 巧用轻量LLM（0.8B）作为语义特征提取器，而非直接生成轨迹，避免延迟和格式不稳定，且通过隐藏状态参与后续模块。
  - 自适应调度器动态调整α和N，按需分配计算资源：简单场景用确定性采样（α高, N少），复杂场景用多模态覆盖（α低, N多）。
- **实验结果**：
  - PDMS 93.7（v1）和EPDMS 88.6（v2）均为同设置下最优，尤其安全相关指标提升明显（TTC从95.9→97.2）。
  - 推理速度高达99FPS，满足实时要求。
- **实验设计**：消融实验设计清晰，可视化（图2）直观展示了训练过程中α对轨迹分布的影响，验证了理论设计的有效性。

### 8. 不足与局限

- **实验覆盖**：
  - 仅在NAVSIM仿真环境测试，未在真实道路或更高难度基准（如nuScenes、Waymo Open Motion）上评估，泛化性存疑。
  - 缺少对极端场景（极端天气、传感器故障、动态障碍物剧烈变化）的鲁棒性测试。
- **方法局限**：
  - 自适应调度器采用**离散预定义方案**，可能错过全局最优(α, N)组合，导致性能损失。
  - **多阶段训练**（VAE、PCA、LLM微调、调度器/评分器）分离，缺乏端到端联合优化，训练复杂度高且可能产生累积误差。
  - 在v2中车道保持（LK）和交通信号灯（TL）指标较低，说明对精细横向控制和高层规则（红绿灯）的建模仍需改进。
- **资源与复现性**：未公开代码（从论文描述推测），且未提供计算资源细节，限制学术社区复现和后续改进。
- **应用限制**：依赖LLM的预训练知识（ReCogDrive的QA数据），对于未覆盖的本地交通规则或特殊场景可能表现不佳。

（完）
