---
title: Reinforcement Learning for Flow-Matching Policies with Density Transport
title_zh: 基于密度传输的流匹配策略强化学习
authors: "Boshu Lei, Kostas Daniilidis, Antonio Loquercio"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08602v1"
tags: ["query:gen-models"]
score: 9.0
evidence: 用于流匹配策略的强化学习
tldr: 针对连续控制中微调流匹配策略的困难，提出RLDT算法，将策略改进视为动作密度向高奖励区域传输，利用SVGD构建传输场并微调预训练流匹配策略对齐。通过期望目标估计避免反向传播不稳定性。实验表明在多种连续控制任务中奖励质量和收敛速度均优于基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08602v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1382, \"height\": 347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08602v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1437, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08602v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1444, \"height\": 388, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08602v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1434, \"height\": 720, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08602v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1423, \"height\": 402, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08602v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1439, \"height\": 592, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08602v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1441, \"height\": 457, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08602v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1438, \"height\": 482, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08602v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1443, \"height\": 216, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08602v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1442, \"height\": 186, \"label\": \"Table\"}]"
motivation: 现有强化学习微调流匹配策略的方法存在梯度偏差或损失多模态建模能力，亟需一种能直接对齐传输场的稳定算法。
method: 使用SVGD从最大熵RL目标构建传输场，通过期望目标估计近似中间去噪步骤的动作，稳定地微调预训练流匹配策略。
result: 在稠密/稀疏奖励、状态/视觉机器人操作任务中，RLDT在奖励质量和收敛速度上超越基线。
conclusion: RLDT有效结合传输理论和流匹配模型，实现稳定高效的在线RL微调，适用于复杂连续控制。
---

## 摘要
我们提出了一种在线强化学习（RL）算法，用于微调连续控制问题中的流匹配策略。我们的关键洞察是将基于RL的策略改进视为将动作密度向高奖励区域的传输，这自然地与流匹配模型的传输形式化相一致。先前的方法要么近似当前或最优策略分布，要么求助于蒸馏，这会引入有偏梯度或牺牲多模态建模能力。相比之下，我们提出的基于密度传输的强化学习方法（称为RLDT）通过使用斯坦因变分梯度下降（SVGD）从最大熵RL目标构建传输场，然后微调预训练的流匹配策略以与该场对齐。使用此对齐目标进行训练并非易事，因为流匹配策略通过多步过程生成动作，使得直接的基于梯度的优化具有挑战性。为克服这一挑战并稳定训练，我们通过期望目标估计从中间去噪步骤近似策略动作，这使得传输场更新能够传播到网络参数中，而无需通过时间进行不稳定的反向传播。实验结果表明，RLDT在奖励质量和收敛速度上优于竞争基线。这一性能在多种连续控制任务中保持，包括密集和稀疏奖励，以及基于状态和视觉的长时域机器人操作。项目网页为https://rpfey.github.io/rldt/。

## Abstract
We present an online reinforcement learning (RL) algorithm for fine-tuning flow-matching policies in continuous-control problems. Our key insight is to view RL-based policy improvement as a transport of action densities towards regions of high reward, which naturally aligns with the transport formulation of flow matching models. Prior methods either approximate the current or optimal policy distribution or resort to distillation, which introduces biased gradients or sacrifices multimodal modeling capacity. In contrast, our approach for RL with Density Transport, which we name \emph{RLDT}, constructs a transport field from a maximum-entropy RL objective using Stein Variational Gradient Descent (SVGD). Then, it finetunes a pretrained flow matching policy to align with this field. Training with this alignment objective is nontrivial because flow-matching policies generate actions via a multi-step process, making direct gradient-based optimization challenging. To overcome this challenge and stabilize training, we approximate policy actions from intermediate denoising steps via expected-target estimation. This allows the transport-field update to propagate into the network parameters without unstable backpropagation through time. Experimental results demonstrate that RLDT outperforms competitive baselines in reward quality and convergence speed. This performance holds across diverse continuous-control tasks, encompassing both dense and sparse rewards, as well as state- and vision-based long-horizon robot manipulation. The project webpage is \href{https://rpfey.github.io/rldt/}{https://rpfey.github.io/rldt/}.

---

## 论文详细总结（自动生成）

# 基于密度传输的流匹配策略强化学习（RLDT）论文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：视觉-语言-动作模型（VLA）在机器人控制中展现出强大的先验知识，但预训练的基策略（base policy）可能对特定任务并非最优（如训练数据次优、分布外情况）。强化学习提供了一种微调基策略的途径，但针对**流匹配策略**（flow-matching policies）存在两大挑战：
  1. 无法直接获取任务特定的最优策略样本。
  2. 由于多步去噪过程，策略对数似然估计计算昂贵。
- **现有方法局限**：
  - 近似对数似然的下界（如DPPO、FPO++）引入梯度偏差。
  - 蒸馏方法（如ReinFlow、SACFlow）压缩多步策略，牺牲表达能力。
  - 逆向ODE或通过时间反向传播（如QAM）导致梯度不平衡或不稳定。
- **作者洞察**：将RL策略改进视为**动作密度向高奖励区域的传输**，自然匹配流匹配模型的传输形式化，从而避免对数似然计算和耗时反向传播。

## 2. 方法论

- **核心思想**：利用斯坦因变分梯度下降（SVGD）从最大熵RL目标构建传输场 ϕ(a)，然后微调预训练流匹配策略 vθ 以与该场对齐。
- **关键技术细节**：
  - **最大熵RL目标**：软Q迭代得到最优策略 π*(a|s) ∝ exp(Q(s,a)/κ)。
  - **SVGD构建传输场**：ϕ*(a) = E_{a'~πθ}[k(a',a)∇_{a'}log p(a') + ∇_{a'}k(a',a)]，其中 p(a) = exp(Q(s,a)/κ)/Z，k为RBF核；包含吸引项（critic梯度）和**排斥项**（防止粒子坍塌，促进探索）。
  - **绕过反向传播**：通过期望目标估计 a†_{t,τ} = a_{t,τ} + (1-τ)vθ(a_{t,τ}, τ) 将中间去噪步骤映射回动作流形，直接在此流形上计算SVGD更新，梯度通过链式法则传播回网络参数（公式12-13），无需通过时间反向传播。
  - **最终损失**：L_actor = L_RL + λ1 L_consist + λ2 L_constraint，其中：
    - L_RL = E[vθ⊤ϕ*(sg(a†))/(T(1-τ))] 对齐流场与传输场。
    - L_consist：保持流场直线性（最优传输性质）。
    - L_constraint：Fisher散度约束，使微调策略靠近预训练模型。
- **算法流程**（附录Algorithm 1）：
  1. 用当前策略采样，存入回放缓冲区。
  2. 更新Q网络（双Q集成，按true online TD）。
  3. 对每个状态，采样K个动作粒子，随机选取去噪时间步τ，计算期望目标 a†，计算SVGD传输方向，通过L_actor更新策略参数。

## 3. 实验设计

- **场景与基准（Benchmark）**：
  - **OpenAI Gym**（稠密奖励）：HalfCheetah、Hopper、Walker2d，动作维度6-17，使用状态输入，动作块大小Ta=4。
  - **Furniture-Bench**（稀疏奖励、长时域、多阶段）：One-Leg、Lamp（低/中随机性），状态输入，Ta=8。
  - **Robomimic**（视觉输入、稀疏奖励）：Square、Transport，RGB+本体感受输入，Ta=4或8。
- **对比方法**：
  - DPPO（扩散策略PPO，对数似然下界）
  - ReinFlow（蒸馏流策略）
  - FPO++（使用ELBO下界）
  - QAM（近端伴随匹配，构建逐步骤目标）
  - 所有基线使用相同的预训练流匹配策略（除DPPO使用原DDPM目标外）。
- **评价指标**：环境回报（Gym）或成功率（Furniture-Bench、Robomimic），每个设置3个随机种子。

## 4. 资源与算力

- **明确提及的训练时间和硬件**（附录B.2）：
  - OpenAI Gym：约 **1.5 GPU小时**（单NVIDIA 2080Ti，70次迭代，100并行环境）。
  - Furniture-Bench：约 **48 GPU小时**（单NVIDIA A40，500/250次迭代，1000并行环境）。
  - Robomimic：约 **30 GPU小时**（单NVIDIA A40，200次迭代，64并行环境）。
- **超参数**：详见附录Table 1和Table 2，包括网络架构、学习率、批量大小等。
- **粒子数**：默认K=8，消融显示K=4即可。

## 5. 实验数量与充分性

- **主要实验**：三个benchmark共7个任务，每个任务3个种子，比较5种方法，共约105条学习曲线。
- **消融实验**：
  - 核函数对比（RBF vs Delta），在Gym和Robomimic、Furniture-Bench上（Fig.5）。
  - 温度κ0消融（Robomimic Square/Transport，Fig.6）。
  - 粒子数K消融（Gym三任务，Fig.7）。
  - 梯度平衡性分析（对比QAM，Fig.4）。
  - 额外训练统计（约束损失、传输场范数，Fig.8）。
- **充分性评价**：
  - 覆盖了密集/稀疏奖励、状态/视觉输入、短/长时域任务，公平性较好（使用相同基策略和训练设置）。
  - 消融不足：未在Furniture-Bench上比较FPO++和QAM（“未调整到合理性能”），但RLDT与最强的DPPO对比。
  - 超参数鲁棒性有分析。
- **客观性**：基线使用官方代码或重新实现（FPO++参数扫描），结果与原文一致，报告了均值和标准差。

## 6. 主要结论与发现

- RLDT在所有设置中**显著超越**基线（Gym奖励提升10-20%，Robomimic成功率~90% vs 其他~75%）。
- **训练稳定**：梯度在各去噪步骤间更均衡（对比QAM），收敛更快。
- **稀疏奖励任务优势明显**：在Lamp-Med上RLDT达~70%成功率，DPPO仅~30%；RBF核的排斥项至关重要（Delta核后期性能下降）。
- **对超参数鲁棒**：温度、粒子数在一定范围内稳定。

## 7. 优点

- **方法论创新**：首次将RL策略改进显式建模为概率传输，自然适配流匹配模型，规避对数似然计算和长时间反向传播。
- **训练稳定**：期望目标估计+SVGD排斥项使梯度平衡，避免模式坍塌。
- **性能优越**：在多种复杂任务中一致领先现有方法。
- **实验设计全面**：涵盖不同奖励类型、观测模态和任务难度，消融充分。
- **可扩展性**：代码和网页公开（https://rpfey.github.io/rldt/）。

## 8. 不足与局限

- **计算开销**：SVGD需要并行模拟K个粒子，虽然K=4即可，但对计算资源有限的真实机器人RL可能仍是瓶颈。
- **算法适用性**：仅针对连续动作的流匹配策略；未探讨离散动作或扩散模型场景。
- **研究不足**：
  - 未在真实机器人上部署验证（仅仿真）。
  - 未与基于蒸馏的蒸馏方法（如Flow Q-Learning）对比（但指出其牺牲表达能力）。
  - 在Furniture-Bench上未与FPO++和QAM对比（声称难以调整），削弱了全面性。
- **理论分析**：虽给出梯度推导，但未严格证明收敛性（依赖于SVGD的近似性质）。

（完）
