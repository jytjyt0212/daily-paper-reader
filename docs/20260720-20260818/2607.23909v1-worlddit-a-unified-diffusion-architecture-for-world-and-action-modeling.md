---
title: "WorldDiT: A Unified Diffusion Architecture for World and Action Modeling"
title_zh: WorldDiT：用于世界与动作建模的统一扩散架构
authors: "Sen Wang, R. Gnana Praveen, Bidhan Roy, Marcos Villagra"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23909v1"
tags: ["query:world-models"]
score: 9.0
evidence: 用于世界与动作建模的统一扩散Transformer，直接属于世界模型架构
tldr: 机器人策略常依赖大规模预训练视觉-语言模型作为动作骨干，模型庞大且训练复杂。本文提出WorldDiT，一种统一的扩散Transformer架构，无需大型预训练VLM即可同时进行动作生成与视觉世界建模。训练时单个扩散Transformer输出连续动作块，并预测未来相机帧的归一化RGB patch目标。在四个LIBERO仿真套件上，WorldDiT位于报告全部套件的方法中模型参数与平均成功率的Pareto前沿。该工作以亚十亿参数提供强基线，对后续规模扩展具有参考意义。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23909v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1674, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1614, \"height\": 1192, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1683, \"height\": 669, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1685, \"height\": 670, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23909v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1730, \"height\": 1165, \"label\": \"Table\"}]"
motivation: 现有机器人策略常依赖大型预训练VLM作为动作骨干，模型庞大、训练复杂。WorldDiT探索无需大骨干的联合动作与世界建模。
method: 训练时单个扩散Transformer生成连续动作块，并预测未来相机帧的归一化RGB patch目标，实现动作与视觉世界建模统一。
result: 在四个LIBERO仿真套件上，WorldDiT位于报告全部套件的方法中模型参数与平均成功率的Pareto前沿。
conclusion: WorldDiT以亚十亿参数提供强基线，证明无需预训练大模型也能达前沿性能，为规模扩展研究奠基。
---

## 摘要
许多最近的机器人策略通过使用大型预训练视觉-语言模型（VLM）作为动作骨干来追求更强的控制。我们提出了WorldDiT，一种统一的扩散Transformer架构，它将动作生成与视觉世界建模相结合，并且在没有大型预训练VLM动作骨干的情况下实现了强大的性能。在训练过程中，一个单一的扩散Transformer生成连续的动作块，并从未来的相机帧中预测归一化的RGB补丁目标。在四个LIBERO模拟套件中，WorldDiT位于所报告的总模型参数和平均成功率帕累托前沿上，这些方法报告了所有四个套件。这些结果为未来的扩展研究提供了一个强大的亚十亿参数基线。

## Abstract
Many recent robot policies pursue stronger control by using large pretrained vision-language models (VLMs) as the action backbone. We introduce WorldDiT, a unified diffusion transformer architecture that couples action generation with visual world modeling and achieves strong performance without a large pretrained VLM action backbone. During training, a single diffusion transformer generates continuous action chunks and predicts normalized RGB patch targets from future camera frames. Across four LIBERO simulation suites, WorldDiT lies on the reported Pareto frontier for total model parameters and mean success among methods reporting all four suites. These results provide a strong sub-billion-parameter baseline for future scaling studies.

---

## 论文详细总结（自动生成）

以下是基于给定论文内容生成的中文总结：

## 1. 论文的核心问题与整体含义（研究动机和背景）

当前机器人操作策略普遍遵循“大型预训练视觉-语言模型（VLM）作为动作骨干”的范式（如π0、OpenVLA等）。这种方法虽然带来了强大的感知与语言理解能力，但也带来了一个重要问题：当模型包含数十亿参数时，其强大的控制性能究竟来自精心设计的动作头、预训练骨干，还是二者结合，变得难以分离。此外，庞大的参数量也增加了训练和部署的成本。

论文作者因此提出了一个核心问题：**是否可以在不使用大型预训练VLM作为动作骨干的情况下，通过一个统一的扩散Transformer架构，将连续动作生成与辅助的未来视觉预测目标结合，从而在保持强控制性能的同时实现紧凑的模型规模？** WorldDiT正是对这一问题的探索性回答，旨在为未来的规模扩展研究提供一个亚十亿参数（sub-billion-parameter）的强基线。

## 2. 论文提出的方法论：核心思想、关键技术细节与算法流程

**核心思想**：WorldDiT采用一个单一的扩散Transformer（DiT）骨干网络，在训练时同时建模机器人动作（连续动作块）和未来的视觉观测（归一化RGB补丁），但在推理时仅保留动作路径，从而在不依赖大型预训练VLM骨干的前提下实现强大的操控性能。

**关键技术细节**：
- **统一多模态编码**：
  - 视觉观测：冻结的MAE图像编码器 + Perceiver Resampler，分别处理主相机和腕部相机RGB图像，每个时间槽生成34个视觉token。
  - 语言指令：冻结的CLIP文本编码器 + 投影层，生成1个语言token。
  - 机器人状态：可训练的机械臂与夹爪编码器，拼接后投影生成1个状态token。
  - 每个观测时间槽总计36个token（34视觉 + 1语言 + 1状态）。
- **统一骨干架构**：
  - 深度4层、隐藏维度1024、16个注意力头、4个寄存器token（register tokens）。
  - 采用严格的块因果注意力（block-causal attention），动作查询无法读取加噪的RGB目标token，确保训练时辅助视觉目标不会泄漏到当前/过去的动作推理中。
- **流匹配训练目标**：采用Flow Matching方法。
  - 训练窗口包含C=3个观测上下文步骤和H=7个目标动作步骤（总窗口N=10）。
  - 动作目标为7步动作块 (A_i ∈ ℝ^{7×7})。
  - 视觉目标取自未来第i+H帧的归一化RGB补丁：从主相机和腕部相机各取64个补丁token，共128个目标token。
  - 训练时期望最小化加权总损失：L_total = w_action * L_action_flow + w_rgb * L_rgb_flow，其中动作损失权重为0.1，RGB补丁损失权重为0.001。
  - 仅最后一个时间槽（对应i+H帧）参与视觉损失计算。
- **推理与部署**：
  - 推理时仅保留动作路径，不提供未来RGB目标或动作标签。
  - 从高斯噪声初始化动作token，通过20步欧拉积分从τ=0到τ=1求解学到的动作速度场。
  - 采用滚动时域控制（Receding-horizon control）：一次预测7个动作，但仅执行前3个动作，更新上下文窗口后重新规划。
  - 同时应用时间集成（temporal ensembling）以平滑重叠的绝对时间动作预测。

## 3. 实验设计：数据集、基准与对比方法

**数据集与基准**：
- 使用**LIBERO**机器人操控基准的四个下游套件：`libero_spatial`、`libero_object`、`libero_goal` 和 `libero_10`（简称LIBERO Long）。
- 使用LIBERO大规模多任务 `libero_90` 拆分进行预训练，然后在各下游套件上独立微调。

**对比方法**：
- 报告了24种方法的对比结果（表1），主要分为两类：
  - **大型预训练VLM动作骨干**：ACoT VLA、MMaDA VLA、VLANeXt、VLA Adapter、OpenVLA OFT、π0.5、MemoryVLA、Discrete Diffusion VLA、VLA0、π0、GR00T N1、UD VLA、π0 FAST、CoT VLA、WorldVLA、SpatialVLA、OpenVLA。
  - **无大型预训练VLM动作骨干**：WorldDiT、DreamVLA、FlowVLA、DiT Policy、Octo、Diffusion Policy、Seer。

**实验规模**：每个套件评估500个模拟回合，所有WorldDiT得分均为4个套件500次回合的汇总结果。

## 4. 资源与算力

论文明确说明了训练资源配置：
- **硬件**：单节点8块 **NVIDIA RTX Pro 6000 GPU**。
- **预训练**：在libero_90上训练30个epoch，per-GPU批量大小为40，梯度累积为2，学习率1e-4（带余弦调度和1个warmup epoch）。
- **微调**：每个下游套件独立进行，有效批量大小为512（8卡，per-GPU批量32，梯度累积2步），学习率1e-4，使用bf16混合精度。
- **模型规模**：WorldDiT总参数量为 **399.084M**（包括推理所需冻结模块），可训练参数量为 **135.107M**。
- 论文未明确说明微调的准确时长，但提供了上述配置信息。

## 5. 实验数量与充分性

- **实验数量**：在4个LIBERO套件上进行了评估，与24种方法进行对比。**未进行消融实验**来验证各组件（如RGB补丁损失权重、视觉目标的作用等）的贡献。
- **充分性评估**：
  - **优点**：覆盖了标准化的LIBERO四个下游套件，与报告全部四个套件的多种现代VLA/扩散策略进行了系统对比，并绘制了参数-性能帕累托前沿，具备一定的参考价值。
  - **不足与公平性风险**：论文明确承认，每个套件的500个回合中包含**300个用于阶段式检查点选择的回合**，因此报告的平均成功率（94.9%）**并非完全留出（fully held out）**，不应被视为无偏的测试估计。此外，因为无法复现所有方法，对比使用了引用文献中的得分，且参数计数部分为重建的近似值，这可能导致跨方法对比存在一定的系统偏差。

## 6. 论文的主要结论与发现

- WorldDiT在四个LIBERO套件上分别取得了**Spatial 98.0%、Object 97.0%、Goal 92.8%、Long 91.8%** 的成绩，平均成功率为**94.9%**。
- 尽管WorldDiT仅使用约399M总参数（135M可训练参数），且没有大型预训练VLM骨干，其性能仍超过了部分使用大型VLM骨干的模型（如GR00T N1、OpenVLA等）。
- 在所有报告了四套件均值的模型中，WorldDiT位于**报告参数的帕累托前沿**上：任何报告成绩更高的方法都拥有更多参数，而参数相仿或更少的方法报告成绩更低。
- 这一结果证明，**统一的世界-动作建模可以作为紧凑的、无大型VLM骨干的机器人策略的强大基础**，为未来的规模扩展研究提供了有力依据。

## 7. 优点

- **架构简洁统一**：使用一个共享的DiT骨干同时处理动作和未来视觉预测，避免了复杂的VLM骨干依赖，提供了一种清晰的亚十亿参数基线。
- **训练-推理解耦**：未来RGB补丁预测仅在训练时作为辅助监督信号，推理时完全移出计算图，保持了部署的高效性。
- **强大的实证性能**：在不依赖大规模预训练VLM的情况下取得了94.9%的LIBERO平均成功率，显著超越了同量级的其他无VLM方法（如Diffusion Policy、Octo），甚至与规模大得多的VLA方法竞争力相当。
- **诚实的结果报告**：论文明确指出了其评估协议中的非完全留出风险（300回合用于检查点选择），并在参数计数上标注了近似值，相较同类论文更加透明。

## 8. 不足与局限

- **评估协议局限**：如上文所述，评估中包含了用于检查点选择的数据，导致94.9%的平均成绩并非完全无偏的测试结果；未来需要完全留出的评估集来获得更可信的结论。
- **缺乏消融与组件分析**：论文未针对损失权重、未来RGB预测、上下文长度C、动作块长度H等关键设计进行消融实验，难以量化每个组件对最终性能的具体贡献，在一定程度上削弱了方法论的深入说服力。
- **场景局限**：实验仅在LIBERO仿真环境中进行，未涉及真实世界机器人部署，其泛化能力和实际可用性有待验证；此外，LIBERO本身任务相对局限性较大，结论的普适性需要更广泛基准（如真实操作台任务）验证。
- **对比公平性风险**：依赖引用报告中的分数进行跨方法对比，在不同训练协议（如预训练数据、微调技巧）下，严格意义上的公平比较难以保证。
- **规模效应未验证**：论文明确指出提供的是一种“基线”而非“规模行为的证据”。关于未来RGB目标如何塑造控制、该架构在更大规模下（数据/容量）是否能保持优势，仍需后续研究。
- **应用部署限制**：虽然推理路径轻量，但20步欧拉积分和128个辅助训练token可能意味着相对较高的推理开销或训练显存占用，论文未对实际的推理时延和训练显存峰值进行详细报告。

（完）
