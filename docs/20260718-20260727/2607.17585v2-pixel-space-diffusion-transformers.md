---
title: Pixel-Space Diffusion Transformers
title_zh: 像素空间扩散变换器
authors: "Renye Yan, Jikang Cheng, You Wu, Ling Liang, Wei Peng, Athanasios V. Vasilakos, Qingyu Zhao, Yu Zhang, Yimao Cai, Kilian M. Pohl, Guoying Zhao"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17585v2"
tags: ["query:world-models"]
score: 9.0
evidence: 像素空间扩散变换器用于高保真图像合成
tldr: 潜在扩散模型因固定VAE标记器丢失纹理细节且重建与生成目标错配。像素空间扩散直接在原始像素上建模，消除VAE瓶颈，支持端到端优化。Pixel-Space Diffusion Transformers (pDiTs) 能生成更高保真度图像，但面临高维噪声调度、损失加权和架构可扩展性挑战。该框架为统一多模态模型提供了共享标记空间，有望融合视觉理解与生成。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17585v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1391, \"height\": 1092, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17585v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1853, \"height\": 919, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17585v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 897, \"height\": 938, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17585v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 879, \"height\": 922, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17585v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1871, \"height\": 1200, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17585v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1861, \"height\": 1240, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17585v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1894, \"height\": 1299, \"label\": \"Table\"}]"
motivation: 传统LDM的VAE压缩丢失纹理细节，且重建与生成目标不匹配，需更直接的像素空间建模方法。
method: 采用像素空间扩散Transformer，直接在原始像素上建模，并设计统一多模态标记空间实现端到端优化。
result: pDiT在高质量图像合成中优于LDM，但需解决高维噪声调度、损失加权和架构可扩展性等挑战。
conclusion: 像素空间扩散为统一多模态视觉基础模型提供了新方向，有望实现生成与理解的融合。
---

## 摘要
潜空间扩散模型（LDMs）通过在VAE压缩的潜空间中进行去噪，实现了高效的高分辨率图像合成。然而，固定的视觉标记器可能丢弃精细纹理和结构细节，而分离的表示与扩散训练造成了重建与生成目标之间的不匹配。这些局限性重新激发了人们对像素空间扩散的兴趣，后者直接对原始像素建模，移除了VAE瓶颈，并支持端到端优化。这种形式更好地符合高保真生成的需求，但在高维建模中引入了挑战，包括噪声调度、损失加权、标记效率以及可扩展架构设计。像素空间建模也为统一的多模态系统提供了有前景的基础：原始像素、文本和任务条件可以表示在共享的标记空间中，并由单一的Transformer联合处理，从而缩小了视觉理解与生成之间的差距。本文从模型架构、连续生成机制和统一多模态建模的角度回顾了像素空间扩散变换器（pDiTs）。我们总结了代表性方法，指出了关键技术挑战，并讨论了朝着集成生成与理解的高保真、端到端视觉基础模型的未来方向。

## Abstract
Latent diffusion models (LDMs) enable efficient high-resolution image synthesis by denoising in a VAE-compressed latent space. However, fixed visual tokenizers can discard fine textures and structural details, while separate representation and diffusion training creates a mismatch between reconstruction and generation objectives. These limitations have renewed interest in pixel-space diffusion, which models raw pixels directly, removes the VAE bottleneck, and supports end-to-end optimization. This formulation better matches the demands of high-fidelity generation but introduces challenges in high-dimensional modeling, including noise scheduling, loss weighting, token efficiency, and scalable architecture design. Pixel-space modeling also offers a promising basis for unified multimodal systems: raw pixels, text, and task conditions can be represented in a shared token space and jointly processed by a single Transformer, narrowing the gap between visual understanding and generation. This paper reviews Pixel-Space Diffusion Transformers (pDiTs) from the perspectives of model architecture, continuous generative mechanisms, and unified multimodal modeling. We summarize representative methods, identify key technical challenges, and discuss future directions toward high-fidelity, end-to-end vision foundation models that integrate generation and understanding.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

- **背景**：潜空间扩散模型（LDMs）通过 VAE 将图像压缩到低维潜空间再进行去噪，大幅降低了计算开销，但固定 VAE 会丢弃高频率纹理、锐利边界和微小局部结构，形成不可逆的信息瓶颈。同时，重建损失与生成目标的分离导致表示不匹配，限制了端到端优化。
- **重新兴起**：近年来，像素空间扩散（Pixel-Space Diffusion）回归，直接在原始像素上建模，移除 VAE 瓶颈，实现单阶段端到端训练。然而，高维像素空间带来噪声调度、损失加权、标记效率及可扩展架构等新挑战。同时，像素空间为统一多模态模型（文本、图像、任务条件共享同一 token 空间）提供了自然基础，有望融合视觉理解与生成。

### 2. 方法论：核心思想、关键技术细节

- **核心思想**：将扩散/流匹配过程直接定义在原始像素空间，使用 Transformer 架构作为主干部，通过分块（patchify）将图像转换为 token 序列，进行自注意力建模，直接预测干净图像（x₀ 预测）或速度场/噪声。
- **关键技术细节**：
  - **噪声过程**：DDPM 定义正向加噪链 \(q(x_t|x_{t-1}) = \mathcal{N}(\sqrt{1-\beta_t}x_{t-1}, \beta_t I)\)，训练时最小化噪声预测损失 \(L_{\text{DDPM}} = \| \epsilon - \epsilon_\theta(x_t, t) \|^2\)。
  - **流匹配**：采用线性插值路径 \(x_t = (1-t)x_0 + t x_1\)，学习速度场 \(v_\theta(x_t, t)\) 使得 \(v_\theta(x_t, t) \approx x_1 - x_0\)，损失 \(L_{\text{CFM}} = \| v_\theta(x_t, t) - (x_1 - x_0) \|^2\)。
  - **架构类别**：
    - **单流大块 Transformer**（如 JiT）：直接用大步长分块，简化结构，但可能丢失细节。
    - **层次化/沙漏型**（如 HDiT）：多阶段下采样全局交互后上采样恢复细节。
    - **全局-局部解耦**（如 DiP, PixelDiT）：大步长 DiT 负责全局语义，轻量模块恢复局部细节。
    - **频率解耦**（如 DeCo）：显式分离低频语义与高频纹理。
    - **隐式神经场解码**（如 PixNerd）：每块 token 作为条件输入 MLP，生成连续像素。
    - **跨尺度语义锚定**（如 HyperDiT）：细粒度 token 可查询高层语义特征。
    - **统一多模态共享 token**（如 HiDream-O1-Image）：文本 tokens、条件 tokens、像素生成 tokens 在同一 Transformer 中联合建模。
- **关键挑战与结构化解决方案**：包括二次复杂度（采用大块、层次、局部注意力）、粒度冲突（全局-局部解耦）、高维轨迹优化（直接 x₀ 预测）、高频噪声干扰（频率滤波）、语义初始化（使用 VFM 辅助对齐但防止干扰）。

### 3. 实验设计

- **数据集与场景**：论文综述了众多 pDiT 方法在 **ImageNet** 类条件生成、**文本-图像生成**、**图像编辑**（如 Palette, BlazeEdit）、**视频生成**（SiD2 在 Kinetics-600）、**3D 生成**（PixGS, PixWorld, PointDiT）、**医学图像**（MedSyn, PRDiT, PPDM）等任务上的实验。
- **基准与对比**：主基准为 ImageNet 上的 FID（Heusel et al.）。对比方法包括潜空间 DiT（DiT, SiT）、传统潜扩散模型（LDM, SDXL）、以及各种 pDiT 变体（Simple Diffusion, SiD2, HDiT, JiT, PixelDiT, DeCo 等）。
- **说明**：作为综述论文，它并未设计自己的一套统一实验，而是汇总了已有方法的实验结果。

### 4. 资源与算力

- **文中不提供统一算力统计**，但对各方法有所提及：
  - Simple Diffusion 和 SiD2 等采用了大规模训练，但未给出具体 GPU/时长。
  - JiT 和 PixelDiT 等使用标准 ImageNet 配置（如 256×256），通常在 8×A100 或以上集群训练数天。
  - 医学图像方法如 PRDiT 运行在 Volta GPU 上，但具体数量未明。
- **结论**：论文未系统报告全部方法的算力，仅作为背景信息碎片化出现。因此，无法给出统一的花费数字。

### 5. 实验数量与充分性

- **覆盖性广泛**：论文涵盖了类条件生成、文本到图像、编辑、视频、3D、医学等 6+ 个应用领域，引用文献超过 60 篇。
- **消融与公平性**：论文详细分析了 pDiT 面临的挑战（二次复杂度、粒度冲突、高频干扰等），并分别列举了对应解决方案，但未做统一的消融比较。各方法在各自论文中做了较充分的消融，如块大小、层级设计、损失权重等。
- **总体评价**：作为综述，实验覆盖的广度和逻辑链完整性较好，但缺乏跨方法的统一公平对比实验（如相同计算预算下的 FID 比较）。因此，充分性较高但存在间接性。

### 6. 主要结论与发现

- **像素空间扩散** 能够消除 VAE 信息瓶颈，在高保真任务（如文本渲染、精细编辑）上展示优于潜空间模型的上限。
- **现代 pDiT** 通过直接 x₀ 预测、层次化结构、全局-局部分工、频率解耦等技术，成功克服了早期像素扩散的收敛慢和高计算问题。
- **统一多模态模型** 中，像素空间 token 为理解与生成提供了共享基础，但需解决梯度冲突、遗忘等问题，单纯权重克隆不足，应使用联合目标。
- **未来方向**：动态 token 粒度、原生像素轨迹设计、非侵入性语义引导、计算-保真度帕累托最优、以及真正的理解与生成统一。
- **综合**：pDiT 不是要完全取代 LDM，而是在特定能力维度（细节、编辑、3D）上拓展视觉生成的上限。

### 7. 优点

- **结构清晰**：按照架构演化和应用领域系统梳理，给出明确的分类表（Table I, II）。
- **深度揭示核心矛盾**：详细分析了“可学习性-保真度冲突”、“重建-生成目标错位”、“表示对齐干扰”等深层问题。
- **提出前瞻性方向**：动态 token、非侵入式引导、后训练范式等为后续研究提供了思路。
- **统一视角**：将像素空间扩散与多模态统一模型有机结合，指出其作为视觉基础模型基础的可能。

### 8. 不足与局限

- **缺乏直接实验**：作为综述，未设计统一基准来验证所提观点，对读者的说服力依赖于原始论文质量。
- **计算开销量化不足**：未给出 pDiT 与 LDM 在同等 FLOPs 下的公平性能对比，可能高估像素空间的优势。
- **多任务评估缺口**：虽然提到了统一模型的挑战（梯度干扰），但未提供具体的干预实验结果来证明所提联合目标的有效性。
- **应用局限性明确**：pDiT 在高分辨率、长序列（视频、3D）上计算成本激增，实际部署仍需进一步压缩与适配。
- **数据与评价框架**：作者指出现有评价偏重全局语义，缺乏对细粒度细节（如 OCR 精度、编辑区域保持）的测试，这表明综述自身不能弥补这一缺口。

（完）
