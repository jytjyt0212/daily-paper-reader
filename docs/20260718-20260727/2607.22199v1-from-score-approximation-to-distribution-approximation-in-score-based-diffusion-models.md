---
title: From Score Approximation to Distribution Approximation in Score-Based Diffusion Models
title_zh: 从得分逼近到得分扩散模型中的分布逼近
authors: Lan V. Truong
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.22199v1"
tags: ["query:gen-models"]
score: 9.0
evidence: 建立了扩散模型中分数逼近与分布逼近之间的理论联系
tldr: 分数扩散模型在生成建模中表现优异，但其逼近理论基础尚不完整。现有定理仅保证神经网络可逼近分数函数，但尚不清楚能否转化为生成分布的逼近。本文首次严格建立两者间的定量联系：若神经网络足够精确地逼近真实分数函数，则对应反向扩散模型生成的分布与目标数据分布在KL散度上接近，仅受前向扩散终端与先验失配的不可约误差限制。通过结合Hornik通用逼近定理、Girsanov定理和相对熵的数据处理不等式，推导出分布逼近误差的显式上界，为分数扩散模型提供了理论保证。
source: arxiv
selection_source: fresh_fetch
motivation: 现有理论仅保证分数函数可被神经网络逼近，但缺乏分数逼近转化为生成分布逼近的定量连接。
method: 结合Hornik通用逼近定理、Girsanov路径空间定理与相对熵的数据处理不等式，推导分布逼近误差上界。
result: 分布逼近误差在KL散度上以分数逼近误差、扩散噪声计划和先验失配的显式函数为上界。
conclusion: 建立了神经网络分数逼近到扩散模型生成分布逼近的显式理论保证，填补了分数扩散模型的逼近理论基础空白。
---

## 摘要
基于得分的扩散模型在生成建模中取得了显著的实证成功，但其逼近理论基础仍不完整。特别是，尽管经典通用逼近定理保证神经网络可以逼近得分函数，但仍不清楚这样的逼近保证是否能转化为由逆扩散过程生成的概率分布的逼近。在本文中，我们在这两个概念之间建立了严格的定量联系。具体而言，我们证明如果神经网络足够准确地逼近真实得分函数，那么由相应逆扩散模型生成的概率分布与目标数据分布在Kullback-Leibler（KL）散度上接近，直到正向扩散过程的终止分布与用于初始化逆过程的先验之间存在不可约的失配。更精确地说，我们根据得分逼近误差、扩散噪声调度和终止先验失配，推导出分布逼近误差的显式上界。我们的分析结合了Hornik的通用逼近定理、路径空间上的Girsanov定理以及相对熵的数据处理不等式。与最近在有限样本统计设置和数据分布结构假设下研究得分逼近的工作互补，我们的工作基于经典神经网络逼近理论发展了一种逼近理论分析。所得定理提供了一个简单而显式的保证，将神经网络对得分函数的逼近与逆扩散模型生成的概率分布的逼近联系起来。

## Abstract
Score-based diffusion models have achieved remarkable empirical success in generative modeling, yet their approximation-theoretic foundations remain incomplete. In particular, although classical universal approximation theorems guarantee that neural networks can approximate score functions, it remains unclear whether such approximation guarantees translate into approximation of the probability distributions generated by reverse diffusion processes. In this paper, we establish a rigorous quantitative connection between these two notions. Specifically, we prove that if a neural network approximates the true score function sufficiently accurately, then the probability distribution generated by the corresponding reverse diffusion model is close to the target data distribution in Kullback-Leibler (KL) divergence, up to an irreducible mismatch between the terminal distribution of the forward diffusion process and the prior used to initialize the reverse process. More precisely, we derive an explicit upper bound on the distribution approximation error in terms of the score approximation error, the diffusion noise schedule, and the terminal prior mismatch. Our analysis combines Hornik's universal approximation theorem, Girsanov's theorem on path space, and the data processing inequality for relative entropy. Complementary to recent work that studies score approximation under finite-sample statistical settings and structural assumptions on the data distribution, our work develops an approximation-theoretic analysis based on classical neural network approximation theory. The resulting theorem provides a simple and explicit guarantee linking neural network approximation of score functions to approximation of the probability distributions generated by reverse diffusion models.

---

## 论文详细总结（自动生成）

### 论文的核心问题与整体含义（研究动机和背景）

- 得分扩散模型在图像、音频、分子设计等生成任务中表现优异，但其理论基础仍有不足：经典的神经网络通用逼近定理仅保证了得分函数可被神经网络逼近，但未阐明这种函数逼近能否转化为由逆扩散过程生成的**概率分布**的逼近。
- 本文旨在建立**得分逼近**与**分布逼近**之间的严格定量联系，即证明：如果神经网络足够精确地逼近真实得分函数（在L2意义下），那么由学习到的逆扩散模型生成的分布与目标数据分布在Kullback-Leibler（KL）散度上必定接近（仅受前向扩散终端与先验之间的不可约失配限制）。
- 该工作为得分扩散模型的表达能力提供了逼近理论基础，补充了Chen等人（ICML 2023）从统计学习角度进行的研究。

### 论文提出的方法论：核心思想、关键技术细节

**核心思想**：将分布逼近误差分解为两个层级——路径测度逼近误差和边缘分布逼近误差，并分别用Girsanov定理和数据压缩不等式进行上界估计，最后结合Hornik通用逼近定理得到整体保证。

**关键技术细节与公式（文字说明）**：

1. **前向扩散与逆扩散过程**：
   - 前向：\( dX_t = f(X_t,t) dt + g(t) dW_t \)，从数据分布μ开始，得到边际密度\( p_t(x) \)，得分函数\( s(x,t) = \nabla_x \log p_t(x) \)。
   - 真实逆扩散：\( dX_t = [f(X_t,t) - g^2(t) s(X_t,t)] dt + g(t) d\bar{W}_t \)。
   - 学习逆扩散：用神经网络\( s_\theta \)取代真实s，从先验\( p_{\text{prior}} \)开始。

2. **路径测度KL散度的显式表达式**（Proposition 1）：
   - 通过Girsanov定理得到：
     \[
     \text{KL}(P\|Q_\theta) = \text{KL}(p_T\|p_{\text{prior}}) + \frac12 \int_0^T g^2(t) \mathbb{E}_{p_t}[\|s_\theta(X_t,t)-s(X_t,t)\|^2] dt
     \]
     其中P和Q分别是真实和学习逆扩散的路径测度。

3. **路径测度到边缘分布的KL上界**（Proposition 2）：
   - 利用数据压缩不等式（相对熵在映射下不增）：
     \[
     \text{KL}(\mu\|\mu_\theta) \le \text{KL}(P\|Q_\theta)
     \]
     其中μ和μ是数据分布和学习生成分布（t=0时刻的边际）。

4. **主定理（Theorem 1）**：
   - 假设：正则性（Lipschitz）、Novikov条件、先验失配\( \delta = \text{KL}(p_T\|p_{\text{prior}}) \)有限。
   - 对任意ε>0，存在神经网络s使得\( \|s_\theta - s\|^2_{L^2(\mu)^d} < \epsilon \)，则：
     \[
     \text{KL}(\mu\|\mu_\theta) < \delta + \frac12 g_{\max}^2 \epsilon
     \]
     其中\( g_{\max} = \max_{t\in[0,T]} |g(t)| \)。
   - 证明流程：Hornik定理保证L2逼近存在且满足Lipschitz条件 → 代入Proposition 1和Proposition 2 → 利用g(t)有界得到最终不等式。

**算法流程**：无具体算法，论文为理论分析，不涉及迭代训练步骤。

### 实验设计

- **论文没有进行任何数值实验或仿真**。全文为纯理论推导，不涉及数据集、benchmark或对比方法。
- 文中仅通过理论示例（如VP扩散、VE扩散）说明假设的合理性，未设置实验验证。

### 资源与算力

- 论文未提及任何计算资源（GPU型号、数量、训练时长等）。这符合其理论论文的性质。

### 实验数量与充分性

- 无实验，因此不涉及实验数量与充分性评估。
- 论文的理论推导逻辑清晰，假设合理（正则性、Novikov条件等），但未通过实验验证实际中这些假设是否容易满足或逼近误差的实际大小。

### 论文的主要结论与发现

- **建立了得分逼近误差与生成分布KL散度之间的显式线性关系**：分布逼近误差 ≤ 终端先验失配 + 常数×得分L2逼近误差。
- **证明了神经网络通用逼近能力可以直接迁移到扩散模型的分布生成能力**：只要得分函数被充分近似（在L2(μ)范数下），生成的分布可以在KL散度意义上任意逼近真实数据分布（忽略不可约先验失配）。
- 该结果提供了得分扩散模型表达能力的理论支撑，将函数逼近理论与生成式建模的理论连接起来。

### 优点

- **理论贡献突出**：首次在一般欧几里得空间下，不依赖低维结构假设，建立了得分逼近与分布逼近的严格定量关系。
- **推导简洁优雅**：仅用三个经典工具（Hornik定理、Girsanov定理、数据处理不等式）即可得到显式上界，逻辑链条清晰。
- **适用范围广**：假设条件（Lipschitz、Novikov）在常见扩散模型（VP、VE）和网络架构（前馈、CNN、Transformer）中可满足。
- **与现有研究互补**：区别于Chen等人的统计学习视角，本文从逼近论角度出发，为理论分析提供了新的维度。

### 不足与局限

- **缺乏实验验证**：纯理论分析，未通过数值实验展示实际逼近效果或检验假设的严格性。
- **假设较强**：要求得分函数及神经网络均为全局Lipschitz（对x一致，对t均匀），对于非光滑数据分布（如离散点、重尾分布）可能不成立；Novikov条件也需要指数可积性，实际中需验证。
- **只考虑了KL散度**：KL散度对零测集敏感，可能不反映感知质量（如图像生成）。文中虽提及可扩展到其他度量（Wasserstein、TV），但并未给出具体结果。
- **未给出逼近速率**：Hornik定理只保证存在性，不提供网络尺寸与逼近精度的定量关系；论文未探索速率上界。
- **数据分布局限于欧几里得空间**，未覆盖流形、图结构等更一般情形。
- **先验失配项不可消除**：除非\( p_T = p_{\text{prior}} \)，否则即使得分完全精确，分布逼近仍有δ误差。实际中\( p_T \)通常与高斯先验接近但不完全相等，δ可能非零。

（完）
