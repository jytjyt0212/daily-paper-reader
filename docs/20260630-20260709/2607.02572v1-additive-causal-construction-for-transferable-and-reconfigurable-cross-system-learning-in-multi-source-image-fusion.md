---
title: Additive Causal Construction for Transferable and Reconfigurable Cross-System Learning in Multi-Source Image Fusion
title_zh: 多源图像融合中可迁移和可重构跨系统学习的加性因果构建
authors: "Zhizhong Fu, Wei Zhou, Zhaoyang Jiang, Yulong Lin, Yifu Hou, Xiaorong Ding, Qiang Yan, Yifan Chen"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2607.02572v1"
tags: ["query:causal-rep"]
score: 9.0
evidence: 在多源图像融合中提出加性因果构建框架
tldr: 多源图像融合中异构输入由不同生成机制驱动，存在跨系统差异(CSD)和跨系统纠缠(CSE)问题，导致分布外预测性能下降。本文提出加法因果构造(ACC)框架，通过干预一致性建立共享因果锚点实现图可迁移性，并利用不确定性量化建模构建路径可靠性实现图可重构性。基于ACC的因果表示学习方法(ACC-CRL)通过内容-机制解耦学习联合因果表征，在共享锚点下进行响应对齐以缓解CSD，并引入结构不确定性自适应调节融合过程以抑制CSE。在ColorMNIST合成数据和多中心医学影像微血管侵犯预测任务上，该方法显著提升分布外泛化性能，同时保持分布内性能，验证了机制对齐与不确定性建模在开放环境中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02572v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1867, \"height\": 1050, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02572v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 848, \"height\": 438, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02572v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 896, \"height\": 910, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02572v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 934, \"height\": 380, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02572v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 929, \"height\": 762, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02572v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1853, \"height\": 537, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02572v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 759, \"height\": 210, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02572v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1238, \"height\": 429, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02572v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1628, \"height\": 389, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02572v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1627, \"height\": 389, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02572v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1634, \"height\": 258, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02572v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1624, \"height\": 259, \"label\": \"Table\"}]"
motivation: 现有方法忽略多源图像异构生成机制，导致跨系统差异和纠缠，分布外泛化性能差。
method: 提出ACC框架，通过干预一致性建立共享因果锚点，并利用不确定性量化实现可重构因果路径构建；ACC-CRL进行内容-机制解耦和响应对齐。
result: 在合成数据和真实医学图像任务上，显著提升OOD泛化，保持ID性能，优于基线。
conclusion: 基于机制对齐与不确定性建模的因果构造策略，有效解决跨系统差异和纠缠，增强开放环境鲁棒性。
---

## 摘要
在多源图像融合场景中，异构输入通常由不同的生成机制驱动，可视为多个因果系统的组合。然而，融合过程中常出现跨系统差异（CSD）和跨系统纠缠（CSE），导致在分布外（OOD）预测下性能显著下降。为解决CSD和CSE问题，我们提出加性因果构建（ACC）框架，该框架在两个层面刻画信息融合：首先，通过干预一致性建立多系统共享的因果“锚点”，实现因果图可迁移性（CGT）；其次，将融合过程形式化为因果构建，并通过不确定性量化建模构建路径的可靠性，确保因果图可重构性（CGR）。在此基础上，我们结合ACC重新审视传统因果表示学习（CRL），提出ACC-CRL作为该框架的可学习实例化。该方法通过内容-机制解耦探索跨系统的联合因果内容表示，并在共享锚点下进行响应对齐以缓解CSD；同时引入结构不确定性自适应调节融合过程，从而抑制不稳定的CSE。我们在合成数据（ColorMNIST）和实际多中心医学影像任务（微血管侵犯（MVI）预测）上进行了系统实验。结果表明，所提方法在保持分布内（ID）性能的同时显著提升了OOD泛化能力，验证了基于机制对齐与不确定性建模的ACC-CRL策略在开放环境中的有效性和鲁棒性。

## Abstract
In multi-source image fusion scenarios, heterogeneous inputs are typically driven by distinct generative mechanisms and can be viewed as a composition of multiple causal systems. However, cross-system discrepancy (CSD) and cross-system entanglement (CSE) commonly arise during the fusion process, often leading to significant performance degradation under out-of-distribution (OOD) predictions. To address the CSD and CSE issues, we propose the additive causal construction (ACC) framework, which characterizes information fusion at two levels: firstly, it establishes causal "anchors" shared among multiple systems through intervention consistency to enable causal graph transferability (CGT); and secondly, it formalizes the fusion process as causal construction and models the reliability of constructed paths through uncertainty quantification to ensure causal graph reconfigurability (CGR). Building upon this, we revisit the traditional causal representation learning (CRL) with ACC and propose ACC-CRL as a learnable instantiation of the framework. The method explores joint causal content representations across systems via content-mechanism decoupling, and performs response alignment under shared anchors to mitigate CSD. Furthermore, it incorporates structural uncertainty to adaptively regulate the fusion process, thereby suppressing unstable CSE. We conduct systematic experiments on synthetic data (ColorMNIST) and real-world multi-center medical imaging tasks (microvascular invasion (MVI) prediction). The results demonstrate that the proposed method significantly improves OOD generalization while maintaining in-distribution (ID) performance, validating the effectiveness and robustness of the ACC-CRL strategy based on mechanism alignment and uncertainty modeling in open environments.

---

## 论文详细总结（自动生成）

# 多源图像融合中加性因果构建（ACC）框架的详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：多源图像融合场景中，不同输入来源（如不同设备、不同病灶区域）由异构的生成机制驱动，可看作多个因果系统。融合过程中存在两个关键挑战：
  - **跨系统差异（Cross-System Discrepancy, CSD）**：缺乏统一的因果参考，导致语义对齐困难。
  - **跨系统纠缠（Cross-System Entanglement, CSE）**：融合引入不稳定的跨系统依赖（如虚假因果路径），在分布外（OOD）场景下性能大幅下降。
- **研究动机**：现有方法（如多模态OOD检测、不变风险最小化、数据增强等）多基于统计一致性假设，将因果结构视为固定、先验的，未能显式建模生成机制差异及融合后因果结构的演化，因此难以应对CSD和CSE。
- **整体含义**：论文从因果角度重新审视多源融合，提出将融合视为动态的因果构建过程，通过建立共享因果锚点（CGT）和不确定性驱动的结构重构（CGR）来实现鲁棒泛化。

## 2. 论文提出的方法论
### 2.1 核心思想：加性因果构建（Additive Causal Construction, ACC）框架
- 将每个输入源建模为具有不同生成机制的因果系统 `G(m) = (V(m), E(m), U(m))`。
- 融合不是简单拼接，而是通过“添加”互补的跨系统因果关系，逐步构建统一的因果结构。
- 两大支柱：
  - **因果图可迁移性（Causal Graph Transferability, CGT）**：通过干预一致性建立多系统共享的因果锚点。要求候选节点在共享因果内容空间中满足语义可比性，且干预后下游响应分布一致。
  - **因果图可重构性（Causal Graph Reconfigurability, CGR）**：将融合视为拓扑重组，利用结构不确定性（如机制兼容性、锚点响应一致性）评估候选因果边的可信度，选择性保留、削弱或抑制不可靠边。

### 2.2 关键技术细节：ACC-CRL可学习实现
- **内容-机制解耦**：每个系统使用编码器 `Cm` 将观测分解为共享因果内容 `X(m)` 和系统特有机制变量 `U(m)`。通过对比学习（公式20）保持跨系统内容一致性，通过HSIC约束（公式21）解耦内容与机制。
- **双向响应对齐**：在共享锚点下，构建跨系统响应映射 `Ψ`，使一个系统的内容与另一系统的机制能重构对方响应。对齐残差 `estr` 作为结构失配信号。
- **不确定性感知融合**：结构不确定性 `ustr` 由 `estr` 经Sigmoid得到；模型不确定性 `uunc` 由轻量估计器从机制差异和内容差异计算。融合强度 `γ = 1 - (uunc + ustr)/2`。
- **自适应融合**：融合表示 `cfused = γ·(X(1)+X(2))/2 + (1-γ)·X(1)`。当结构一致且预测可靠时加强双系统融合，否则回退到保守的单系统表示。
- **观测补充**：额外保留原始图像特征 `F(m)`，最终表示 `˜c = γ·cfused + (1-γ)·cobs`，平衡因果抽象与视觉细节。
- **联合优化目标**：`L = Lsup + λconLcon + λdisLdis + λaliLali + λuafLuaf`，其中 `Luaf = E[γ·estr]` 惩罚大融合权重下的高结构失配。

## 3. 实验设计
### 3.1 数据集与场景
- **合成数据**：ColorMNIST，构建双模态数据：模态A为彩色数字图像（颜色与标签强相关作为显式捷径），模态B为灰度/纹理表示。训练时设置不同偏差强度（0.95, 0.98, 0.99），测试时OOD数据偏差为0.1。
- **真实医学影像**：多中心微血管侵犯（MVI）预测任务。来自两家医院共264例肝癌患者（160例+104例），使用动脉期MRI的瘤内和瘤周区域作为双输入（跨区域融合）。以患者级划分，切片级评估。

### 3.2 Benchmark与对比方法
- 对比方法包括：MultiOOD、SupContrast、IRM、Mixup、Group DRO、VREx、CDANN等现有OOD鲁棒学习和多源融合方法。
- 评估指标：AUC、ACC、BACC（平衡精度）、Specificity、F1，分别在ID和OOD设置下报告。

### 3.3 实验分组
- ColorMNIST：不同偏差强度下的OOD性能、方向性偏差、反事实一致性、检索准确率（R@1）、k-NN性能、t-SNE可视化。
- MVI：内部ID测试、外部OOD（跨中心）测试、消融实验（机制对齐、UAF不同变体）、不确定性分析（误差相关性、跨域敏感性、概率校准）。

## 4. 资源与算力
- **论文未明确说明**：未提及使用的GPU型号、数量、训练时长或具体算力配置。仅在实验设置中提及切片级训练与评估，但未交代硬件环境。需要指出这一点。

## 5. 实验数量与充分性
- **实验数量**：共进行约5组主要实验（ColorMNIST偏差实验、MVI ID实验、MVI OOD实验、消融实验、不确定性分析），每组包含多个子实验（不同偏差、不同方法、不同指标）。
- **充分性与客观性**：
  - 合成数据实验明确构造了捷径相关性，从因果结构层面分析（方向性偏误、反事实一致性），较好验证了方法对CSE的抑制能力。
  - 医学实验采用严格的患者级划分、独立外部测试集，避免了数据泄露。
  - 消融实验系统评估了机制对齐和UAF各变体对ID/OOD的影响，结果清晰显示了互补性。
  - 不确定性分析与校准提供了额外验证。
  - 不足：ColorMNIST仅考虑双模态简单场景，未在多模态更复杂设置下测试；MVI数据量有限（共812+416个切片），且类别不平衡（正例少），结果方差较大；未与其他因果表示学习方法（如MMNAR）对比。

## 6. 论文的主要结论与发现
- **ACC框架与ACC-CRL有效解决了CSD和CSE问题**：通过内容-机制解耦和双向响应对齐，建立了可迁移的共享因果锚点；通过不确定性感知融合，动态调节跨系统信赖程度，抑制不稳定因果路径。
- **OOD泛化显著提升**：在ColorMNIST上，方向性偏差（h-diff）大幅降低，R@1提升；在MVI外部测试集中，ACC-CRL的AUC（0.684）和BACC（0.6345）均优于多数基线（如MultiOOD的AUC仅0.5211），且ID性能下降最小。
- **机制对齐与UAF互补**：机制对齐提升CGT，UAF增强CGR；两者联合在所有指标上取得最佳平衡。
- **不确定性建模有效**：预测不确定性正相关于分类误差，OOD样本不确定性更高，校准后ECE低（总体0.042），表明模型能可靠地估计自身风险。

## 7. 优点
- **理论框架创新**：将多源融合从静态统计建模提升为动态因果构建，明确区分CSD和CSE并分别提出解决原则（CGT、CGR）。
- **可学习实现设计精巧**：通过内容-机制解耦、双向响应对齐、不确定性门控，实现了对因果结构在表示空间中的近似构造与自适应调节，且整体端到端可训。
- **实验设计全面**：合成数据从因果结构层面分析（方向性偏误、反事实），医学实验引入外部跨中心测试，消融、不确定性分析充分。
- **代码与可重复性**：论文给出公式和算法细节较完整，便于复现。
- **医疗应用价值**：在MVI预测上展示了跨中心泛化能力，具有潜在临床转化意义。

## 8. 不足与局限
- **实验覆盖有限**：仅使用两个数据源（ColorMNIST和单序列双区域MRI），未在更多模态（如CT-MRI、不同序列）上验证。
- **偏差风险**：医学数据集样本量较小（仅264例患者），且极度类别不平衡（阳性少），结果方差较大，可能高估或低估真实泛化能力。
- **对比方法不够全面**：未与最新的因果表示学习方法（如SI-CRL、MMNAR）或更先进的OOD检测方法（如能量分数法）比较。
- **计算资源未说明**：缺乏训练时间、GPU型号等信息，影响实际部署可行性评估。
- **理论基础部分**：附录中的数学证明基于局部线性近似和独立干预假设，在非线性、高维场景下可能不够严谨。
- **超参数敏感性**：文中未讨论温度τ、损失权重λ等超参数对结果的影响，缺乏鲁棒性分析。
- **单一任务验证**：仅验证MVI预测一个医学任务，在其他医学图像融合任务（如病变分割、多模态分类）中的表现未知。

（完）
