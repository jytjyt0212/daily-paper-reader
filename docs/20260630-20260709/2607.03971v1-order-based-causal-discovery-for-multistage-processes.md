---
title: Order-based Causal Discovery for Multistage Processes
title_zh: 基于顺序的多阶段过程因果发现
authors: "Eun-Yeol Ma, Junsub Jung, Heeyoung Kim"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03971v1"
tags: ["query:causal-rep"]
score: 9.0
evidence: 多阶段过程的因果发现方法
tldr: 多阶段过程广泛存在于工业、生物等领域，但现有因果发现方法忽略其层次和顺序结构，易产生反直觉结果且计算效率低。本文提出OCDM方法，通过阶段信息引导推断因果顺序，并利用随机门控神经网络高效剪枝虚假边。实验证明OCDM能准确还原多阶段因果结构，显著优于现有基准方法。该工作将过程知识融入因果发现，对复杂系统分析有重要价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03971v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1315, \"height\": 1089, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03971v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 792, \"height\": 1206, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03971v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1585, \"height\": 1040, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03971v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1585, \"height\": 1039, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03971v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1586, \"height\": 1040, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03971v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1872, \"height\": 1381, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03971v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1586, \"height\": 1036, \"label\": \"Figure\"}]"
motivation: 现有因果发现方法未利用多阶段过程的已知阶段顺序，导致推断结果反直觉，且处理大规模数据计算效率低。
method: 提出OCDM：基于结构知识的排序推断算法保留阶段顺序，再用随机门控神经网络剪枝消除虚假边。
result: 在合成和真实多阶段数据集上，OCDM的因果图准确性（如SHD、F1）均优于PC、GES等基线方法。
conclusion: OCDM通过显式融入过程知识，显著提升多阶段因果发现的准确性和计算效率，拓展了因果发现的应用场景。
---

## 摘要
因果关系已成为深入理解复杂系统日益重要的工具。在各种因果分析方法中，因果发现通过从数据中识别变量间的因果关系，已被广泛用于揭示不同过程中的潜在因果机制。然而，尽管多阶段过程在许多领域普遍存在，现有的因果发现方法在已知过程知识的情况下可能产生反直觉的结果，并且对于多阶段过程中常见的大规模数据集可能计算效率不高。为解决这一问题，我们提出了一种新颖的因果发现方法，称为基于顺序的多阶段过程因果发现（OCDM）。OCDM旨在推断多阶段数据的因果结构，同时通过将过程知识明确纳入因果发现过程来保留其固有的层次和顺序结构。具体而言，我们提出了一种结构知识引导的顺序推断算法，该算法基于自然适用于固有顺序多阶段数据的顺序因果发现框架，通过结合每个变量来源的阶段信息来推断变量的因果顺序。此外，为了消除基于推断出的因果顺序生成的初始因果图中的虚假边，我们引入了一种使用随机门控神经网络的新型剪枝技术，与现有方法相比，该技术具有更高的计算效率。通过在多种数据集上的实验，我们证明了OCDM能够有效推断多阶段过程的因果结构，优于现有方法。

## Abstract
Causality has become an increasingly important tool for gaining a deeper understanding of complex systems. Among various causal analysis methods, causal discovery, which identifies causal relationships among variables from data, has been widely used to uncover underlying causality in diverse processes. However, while multistage processes are prevalent in many fields, existing causal discovery methods may produce counterintuitive results, given the known process knowledge, and may not be computationally efficient for handling large datasets typical of multistage processes. To address this gap, we propose a novel causal discovery method called Order-based Causal Discovery for Multistage Processes (OCDM). OCDM is designed to infer the causal structure of multistage data while preserving their inherent hierarchical and sequential structure by explicitly incorporating process knowledge into the causal discovery process. Specifically, we propose a structural knowledge-informed order-inferring algorithm that infers the causal order of variables by incorporating information about the stage from which each variable originates, based on an order-based causal discovery framework naturally suited for inherently ordered multistage data. Furthermore, to eliminate spurious edges from the initial causal graph generated based on the inferred causal order, we introduce a novel pruning technique using stochastic gated neural networks, which offers greater computational efficiency compared to existing methods. Through experiments on various datasets, we demonstrate that OCDM effectively infers the causal structure of multistage processes, outperforming existing methods.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化、深入、客观的中文总结。

---

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究对象**：多阶段过程（如制造流水线、信息系统、个性化服务等），这类过程变量间具有天然的层次和顺序结构（后阶段变量不能影响前阶段）。
- **现存问题**：现有因果发现方法（包括约束基、打分基、顺序基）大多忽略该结构信息，容易产生反直觉的因果方向（例如下游变量被误判为上游变量的原因），且随着变量数增加，搜索空间超指数增长，计算效率低。
- **核心目标**：提出一种专门面向多阶段过程的因果发现方法，能够显式利用已知的阶段顺序信息，准确推断因果结构，同时保持较高的计算效率。

## 2. 论文提出的方法论

### 核心思想
- 基于**顺序因果发现**框架，先推断变量的因果顺序，再构建顺序连接图，最后通过剪枝去除虚假边。
- 将已知的阶段结构信息显式融入因果顺序搜索，确保后阶段变量不会出现在前阶段之前。
- 提出基于**随机门控神经网络（STG-NN）** 的剪枝方法，替代传统稀疏样条回归（CAM-pruning），以更好地处理高维、大样本数据。

### 关键技术细节
1. **多阶段引导的因果顺序搜索**：
   - 采用 DiffAN 作为基础，利用扩散模型估计 score 函数（对数似然的梯度）。
   - 根据 score 的 Jacobian 的方差：叶节点的方差为 0（因无子节点）。
   - **阶段约束**：从最后阶段开始逐阶段向前搜索，在每阶段内迭代识别叶节点，保证阶段顺序不被破坏。
   - 每确定一个叶节点即将其掩码（mask）设为 0，后续梯度计算忽略该变量。

2. **STG-NN 剪枝**：
   - 对每个变量 $Y_i = X_{\pi_i}$，以其因果顺序前面的所有节点 $X_{\pi_{0:i-1}}$ 作为输入，训练一个神经网络。
   - 网络第一层使用**随机门控层**：$z_j = \max(0, \min(1, \alpha_j + \epsilon_j))$，$\epsilon_j \sim N(0, \sigma_z^2)$。
   - 训练后，若 $\alpha_j \leq 0$ 则 $z_j=0$，对应边被剪枝。
   - 正则化强度随候选父节点数增加而逐渐增大，以提升剪枝效果。

### 算法流程
1. 初始化因果顺序 $\pi = []$，所有 mask 为 1。
2. 训练扩散模型估计 score。
3. 从最后阶段 $L_w$ 开始到第一阶段 $L_1$，对每阶段：
   - 反复计算 score Jacobian 方差，选择方差最小的节点作为叶节点，加入 $\pi$，并从该阶段集合中移除，更新 mask。
4. 生成顺序连接图 $G_\pi$。
5. 对每个变量 $i=2..d$，训练 STG-NN，剪枝 $z_j=0$ 的边，得到最终 DAG $G_\pi^*$。

## 3. 实验设计

### 使用的数据集/场景
1. **模拟数据**：
   - **Simulation 1**：3 阶段 × 10 节点/阶段 = 30 变量，低维场景。
   - **Simulation 2**：20 阶段 × 10 节点/阶段 = 200 变量，高维场景。
   - **噪声阶段标签**：在 Simulation 1 基础上，随机重分配 10%~50% 变量的阶段标签。
   - **混合类型数据**：利用 Simulation 1 的 DAG，将 30% 变量设为二值（通过 logistic 函数），其余连续。

2. **伪真实数据**：
   - **causalAssembly**：模拟真实自动化装配线，5 阶段，98 个变量，因果结构由领域专家定义。

### Benchmark 与对比方法
- 约束基：PC 算法
- 连续优化基：NOTEARS-MLP、DAG-GNN、GraN-DAG
- 顺序基：CAM、SCORE、DAS、DiffAN
- 评估指标：AUROC、AUPRC、SHD（结构汉明距离）、SID（结构干预距离）

## 4. 资源与算力

文中明确给出了硬件环境：
- **CPU**：Intel Core i5-11400
- **GPU**：NVIDIA RTX 3070
- **RAM**：64GB DDR4
- **运行时间举例**：OCDM（STG-NN 剪枝）处理 200 节点数据约 2.75 小时，其中剪枝 1.2 小时；CAM 剪枝需 2.2 小时；PC 算法 2.1 小时；SCORE 超 12 小时。
- 未提及 GPU 数量、具体训练 epoch 数等细节。

## 5. 实验数量与充分性

- **实验组数**：模拟数据 2 组 + 噪声标签 5 组 + 混合类型 1 组 + 伪真实 1 组，共 9 组主要实验。每个方法重复 10 次。
- **充分性**：
  - 覆盖了低维、高维、噪声信息、混合数据类型、伪真实场景，对比了 8 种以上的基线方法。
  - 使用了 4 个不同侧面的评价指标（AUROC、AUPRC、SHD、SID）。
  - 消融：对比了 OCDM 使用 CAM 剪枝 vs STG 剪枝。
- **客观性**：提供均值统计，且与基线相比表现一致优越，结果可信。
- **潜在不足**：真实数据仅一个伪真实案例（causalAssembly），缺乏更多真实世界多阶段过程的验证。

## 6. 论文的主要结论与发现

1. **OCDM 在所有场景下均优于现有方法**，尤其在因果顺序推断上，拓扑顺序发散度（Dtop）显著降低（Simulation 1 降低 67%，Simulation 2 降低 90%）。
2. **阶段信息显式利用有效**：能防止反直觉因果顺序，且对部分噪声标签（p<0.3）仍保持鲁棒性。
3. **STG-NN 剪枝优于传统 CAM 剪枝**：在高维场景下（200 节点）显著提升 AUC 和 SHD，同时计算耗时更短（由于并行化和 GPU 加速）。
4. **OCDM 对混合类型数据具有鲁棒性**，即使在违反非线性加性高斯噪声假设时仍表现良好。
5. **OCDM 计算效率高**，200 节点耗时仅 2.75 小时，远优于多数梯度基方法（>12 h）。

## 7. 优点

- **方法创新**：首次将多阶段结构知识显式融入顺序因果发现 + 首次在顺序基剪枝中使用神经网络（STG-NN）。
- **计算效率**：阶段级搜索降低搜索空间，STG-NN 剪枝可利用 GPU 并行，实际运行时间短。
- **鲁棒性强**：对噪声阶段标签、混合类型数据均有较好适应性，实用价值高。
- **实验全面**：多个维度（维度、噪声、数据类型）的对比验证，指标丰富。

## 8. 不足与局限

- **模型假设限制**：依赖非线性加性高斯噪声模型和无混淆因子假设，在真实场景中常被违反。
- **剪枝缺乏理论保证**：STG-NN 和 CAM 剪枝均无父集恢复的理论保证，可能过度剪枝。
- **实验维度有限**：最大 200 节点，未测试更大规模（如 1000+ 节点）场景；真实数据仅一个伪真实案例。
- **对高度错误的阶段信息敏感**：当 40% 以上标签错误时，OCDM 性能低于基线。
- **未讨论可迁移性**：未说明若阶段信息完全未知时如何应用（虽然文中提到可结合高置信度推断）。
- **计算资源细节不足**：未给出训练 epoch、batch size 具体设置，影响了可复现性。

（完）
