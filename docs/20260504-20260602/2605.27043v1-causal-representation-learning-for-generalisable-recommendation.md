---
title: Causal Representation Learning for Generalisable Recommendation
title_zh: 因果表示学习用于可泛化的推荐
authors: "Yorgos Felekis, Michael O'Riordan, Oriol Corcoll, Ciarán M. Gilligan-Lee"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27043v1"
tags: ["query:causal-rep"]
score: 9.0
evidence: 用于可泛化推荐的因果表示学习和解耦
tldr: 训练数据由已部署策略、用户行为和平台过滤共同产生，导致训练与候选分布严重偏移，使离线评估不可靠。本文提出基于因果表示学习的信息论解耦准则，理论证明最优解仅依赖因果成分，并推导可优化的变分下界，仅需混杂日志即可训练。在Spotify数百万用户的A/B测试中，因果模型离线持平但在线听众参与度显著提升，在KuaiRand和合成数据上同样验证了分布偏移下的增益。该方法适用于现有标准模型，无推理时成本，为推荐系统泛化提供了实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27043v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 820, \"height\": 652, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27043v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 852, \"height\": 399, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27043v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1371, \"height\": 562, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27043v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1082, \"height\": 668, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27043v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1087, \"height\": 778, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.27043v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1192, \"height\": 265, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27043v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 802, \"height\": 223, \"label\": \"Table\"}]"
motivation: 推荐系统训练数据受混淆影响，导致离线指标不可靠，亟需实用的分布偏移泛化方法。
method: 提出信息论解耦准则，推导变分下界，从观测数据优化仅依赖因果成分的表示。
result: 在Spotify百万用户A/B测试中在线听众参与度显著提升，KuaiRand和合成数据上离线持平、分布偏移下增益。
conclusion: 无需改变模型或增加推理成本，即可提升推荐系统的分布偏移泛化能力。
---

## 摘要
基于观测数据训练的预测模型往往无法泛化到部署时遇到的分布，尤其是当训练数据是正在优化的系统的产物时。推荐系统是一个典型例子：它们是在被部署策略、过去用户行为和平台过滤所混淆的交互日志上训练的。因此，训练分布与在服务时评分的候选分布存在显著差异，这种差异使得离线指标成为在线性能的不可靠预测器。我们通过一种受因果表示学习（CRL）启发的方法来解决分布偏移问题。我们提出了一种信息论解缠标准，并证明其最优性仅依赖于输入的因果成分。然后，我们推导出一个可处理的变分下界，使得该标准仅从有限的观测数据中即可优化。我们的方法范围比大多数CRL文献更窄，我们的目标是在分布偏移下实现更好的泛化，而不是完全识别所有潜在因果因素。这个较窄的目标使该方法实用，仅需要现有的混淆日志，适用于任何标准监督模型，且不增加推理时间成本。我们的主要评估是在Spotify上对数百万用户进行的A/B测试，应用于个性化播放列表生成的生产排名器。一个容量匹配的CRL变体在离线性能上相当，但在听众参与度上带来了显著的在线增益。在公共KuaiRand推荐数据集和具有已知因果结构的合成基准上的补充证据显示相同模式：与基线离线性能相当，在分布偏移下取得增益。在所有三种设置中，添加我们的因果解缠目标在分布外泛化上产生了有意义的改进。

## Abstract
Predictive models trained on observational data often fail to generalise to the distributions they encounter when deployed, especially when the training data is a product of the system being optimised. Recommender systems are a canonical example: they are trained on interaction logs confounded by the deployed policy, past user behaviour, and platform filtering. As a result, the training distribution differs substantially from the candidate distribution scored at serving time, a gap that makes offline metrics unreliable predictors of online performance. We address the distribution shift problem with a method motivated by causal representation learning (CRL). We propose an information-theoretic disentanglement criterion and prove that its optimum depends only on the causal components of the input. We then derive a tractable variational lower bound that makes the criterion optimisable from finite observational data alone. The scope of our method is narrower than that of much of the CRL literature, in that we target better generalisation under distribution shift, not full identification of all latent causal factors. This narrower target is what makes the method practical, requiring only the existing confounded logs, applying to any standard supervised model, and adding no inference-time cost. Our headline evaluation is an A/B test with millions of users on Spotify, applied to a production ranker for personalised playlist generation. A capacity-matched CRL variant performed on par offline but delivered substantial online gains in listener engagement. Complementary evidence on the public KuaiRand recommendation dataset and a synthetic benchmark with known causal structure shows the same pattern: offline parity with baseline, gains under distribution shift. Across all three settings, adding our causal disentanglement objective yields meaningfully better out-of-distribution generalisation.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **核心问题**：推荐系统训练数据由已部署策略、用户行为和平台过滤共同产生（暴露偏差），导致训练分布与服务时的候选分布存在显著偏移，离线评估指标无法可靠预测在线性能。
- **整体意义**：本文提出一种基于因果表示学习（Causal Representation Learning, CRL）的实用方法，旨在不改变模型架构、不增加推理成本的前提下，提升推荐模型对分布偏移的泛化能力，使离线指标与在线表现更一致。

### 论文方法论

#### 核心思想
通过信息论解耦标准，学习一个只保留治疗（item）中因果成分表示，同时抑制与混淆因子（用户/上下文）相关的非因果信息，从而切断由混杂导致的伪关联路径。

#### 关键技术细节
1. **结构因果模型（SCM）假设**：
   - 观测变量：治疗 \(T\)（item）、结果 \(Y\)（用户反馈）、混淆因子 \(Z\)（用户/上下文）。
   - 治疗由潜变量 \(T_C\)（因果成分）和 \(T_{nC}\)（非因果成分）混合而成 \(T=m(T_C, T_{nC})\)。
   - 假设 \(Y \perp T_{nC} \mid (T_C, Z)\)（结果仅依赖因果成分）且 \(I(T_{nC}; Z \mid T_C)>0\)（非因果成分含有混淆因子信息）。

2. **解耦准则**：
   \[
   J(g) = I(g(T); Y \mid Z) - \lambda I(g(T); Z)
   \]
   - 第一项鼓励表示预测结果，第二项惩罚表示与混淆因子的互信息。
   - 证明：在假设下，最优表示 \(\phi(T_C)\) 仅保留因果成分；对因果成分的压缩只有在惩罚权重足够大时才有用（“无损区间”）。

3. **可操作的变分下界**：
   - 对 \(I(g(T); Y \mid Z)\)：使用 InfoNCE 下界（即交叉熵排序损失）。
   - 对 \(I(g(T); Z)\)：使用 NCE-CLUB 上界（通过一个可学习的判别器估计）。
   - 最终损失：\(\mathcal{L}_{\text{final}} = \mathcal{L}_{\text{utility}} + \lambda \mathcal{L}_{\text{penalty}}\)，其中 \(\mathcal{L}_{\text{penalty}} = \hat{I}_{\text{NCE-CLUB}}(g_\theta(T); Z; d_\phi)\)。

4. **实现细节**：实验中采用梯度反转层（Gradient Reversal Layer）来最小化 InfoNCE 下界（对应惩罚项），并监控 NCE-CLUB 上界作为诊断。

### 实验设计

#### 数据集 / 场景
1. **合成 SCM**：线性高斯，已知因果/非因果成分，10维治疗（5因果+5非因果），结局由因果成分和混淆因子决定。
2. **KuaiRand-Pure**：真实推荐数据集，约1.4M算法曝光交互 + 1.2M随机曝光交互，27,285用户，7,583视频。
3. **Spotify 在线 A/B 测试**：个性化播放列表生成，数百万用户，两周时长。

#### 基准（Baseline）
- 合成 SCM：线性模型（CATE）
- KuaiRand：DeepFM（标准推荐模型）
- Spotify：生产级 session 排序模型（容量匹配的 CRL 变体与基线相同架构）

#### 对比方法
- 仅对比自身的容量匹配基线（基线 λ=0 vs CRL λ>0），未与其他 CRL 方法（如 DCCL、Rec4Ad）直接比较，因为作者强调实用性与泛化性。

### 资源与算力

论文**未明确说明**使用的 GPU 型号、数量或训练时长。仅在 KuaiRand 实验中提到使用 Adam 优化器、batch size 4096，但未提供硬件细节。合成实验和在线实验也只给出超参数细节。**因此无法量化算力消耗**。

### 实验数量与充分性

- **实验组数**：三个场景（合成、离线基准、在线 A/B 测试），每个场景包含多组设置（合成：扫描噪声强度 0~1，共 21 个点 ×2000 种子；KuaiRand：500 种子；在线：一次两周 A/B 测试）。
- **消融实验**：未单独做超参数 λ 扫描（仅固定 λ=0.3），但通过合成实验的理论验证和 KuaiRand 的 500 种子统计验证了方法有效性。
- **充分性评价**：
  - 合成实验验证了方法在已知因果结构下的行为，客观。
  - KuaiRand 展示了在真实数据上的分布偏移（算法 vs 随机曝光），500 种子统计可靠。
  - 在线 A/B 测试提供了最强证据，但仅在一个产品表面进行。
  - **客观性评价**：作者公开了 KuaiRand 实验细节，但未提供代码或完全可复现的配置；在线测试结果受商业机密限制无法完全透明。整体实验相对充分，但缺少与其他 CRL 方法的直接比较（作者解释为专注于实用性与通用性）。

### 主要结论与发现

1. **分布偏移下泛化提升**：CRL 方法在所有三个场景中：离线指标与基线持平，但在分布不同的评估（随机曝光、在线环境）下显著优于基线。
2. **具体数值**：
   - KuaiRand：随机测试 AUC 从 0.6453 提升至 0.6582（+1.29%），NDCG@5 +1.69%。
   - Spotify A/B 测试：曲目流 +0.75%、跳过 -0.61%、播放时长 +0.50%（95% 置信区间均显著）。
3. **理论验证**：合成 SCM 中 CRL 模型对非因果干预的敏感性接近零，而基线敏感。
4. **实用优势**：无需干预数据、无需多环境、无推理成本，可嵌入任意监督模型。

### 优点

- **理论扎实**：基于信息论给出了解耦准则的优化动机和收敛性质（Propositions 1-3），并推导了可操作的变分下界。
- **实用性强**：仅需在现有训练流程中添加一项互信息惩罚，无模型结构限制，适合工业部署。
- **实验证据链完整**：从合成数据（验证因果机制）→真实离线数据（KuaiRand）→在线 A/B 测试（Spotify），逐步增强说服力。
- **对分布偏移的鲁棒性**：在所有设置中离线指标无法预测在线收益，但 CRL 提供了有效鲁棒性（effective robustness）。
- **信息论诊断**：使用 InfoNCE 下界和 NCE-CLUB 上界监控互信息，提供了可解释的训练过程。

### 不足与局限

1. **假设不可验证**：假设 A1（结果仅依赖因果成分）和 A2（非因果成分有独特泄漏）在真实数据中无法直接验证，方法有效性依赖这些假设是否成立。
2. **单一在线产品**：在线测试仅覆盖 Spotify 的个性化播放列表表面，未在其他推荐场景（搜索、广告等）验证。
3. **缺乏与其它 CRL 方法比较**：虽然作者声称聚焦于实用性，但未与 DCCL、Rec4Ad 等工业 CRL 方法进行性能对比，无法判断相对优劣。
4. **超参数影响未充分探索**：λ 仅在 KuaiRand 上取 0.3，未进行系统调参或灵敏度分析。
5. **实验可复现性**：合成和 KuaiRand 实验细节较完善，但未提供公开代码（仅描述）；Spotify 实验无法复现。
6. **资源消耗未报告**：缺少对训练时长、GPU 内存等实际部署资源的量化，影响可迁移性评估。
7. **混淆因子选择依赖工程经验**：方法需要用户提供特征作为混淆因子 \(Z\)，但实际系统中哪些是真正的混淆因子不易确定。

（完）
