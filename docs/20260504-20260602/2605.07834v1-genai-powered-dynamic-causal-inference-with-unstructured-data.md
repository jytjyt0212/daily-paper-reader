---
title: GenAI Powered Dynamic Causal Inference with Unstructured Data
title_zh: 基于GenAI的非结构化数据动态因果推断
authors: "Kentaro Nakamura, Kosuke Imai"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07834v1"
tags: ["query:causal-rep"]
score: 9.0
evidence: 利用GenAI表示进行动态因果推断
tldr: 现有因果推断方法难以处理动态非结构化数据，如文本和视频中的序列特征。本文利用生成式AI提取内部表示，提出结合神经网络和边际结构模型的动态因果推断框架，可估计治疗特征的位置效应。模拟实验表明该估计量能准确恢复因果效应，置信区间覆盖良好。应用于香港抗议文本实验，发现特征位置显著影响其因果效应。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07834v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1038, \"height\": 690, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.07834v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1586, \"height\": 452, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.07834v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1686, \"height\": 896, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.07834v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1652, \"height\": 1101, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.07834v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1684, \"height\": 919, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.07834v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1590, \"height\": 630, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.07834v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1366, \"height\": 1412, \"label\": \"Table\"}]"
motivation: 现有方法将非结构化数据对象视为静态观测，无法估计动态序列中治疗特征的位置效应。
method: 从生成式AI模型提取非结构化数据的内部表示，用神经网络联合学习每步治疗特征的去混淆因子，估计边际结构模型。
result: 模拟中估计量恢复目标因果效应，置信区间达到名义覆盖；在香港抗议实验中，治疗特征位置显著影响效应。
conclusion: 本文提出了一个有效的动态因果推断框架，能够处理非结构化数据序列并揭示特征位置的关键影响。
---

## 摘要
越来越多的学者试图估计文本、图像和视频等非结构化数据的因果效应。然而，现有方法通常将每个对象视为单一的静态观测。我们通过利用生成式人工智能（GenAI）模型，开发了一个用于非结构化数据动态因果推断的统计框架。我们的方法使研究人员能够估计治疗特征序列的因果效应，包括它们在文本和视频中的位置。我们首先从GenAI模型中提取非结构化对象的内部表示，然后使用神经网络架构估计边际结构模型，该架构联合学习序列中每个治疗特征的去混杂因子。我们的半参数推断框架产生有效的渐近置信区间。模拟研究表明，所提出的估计量能够恢复目标因果效应，并且置信区间在有限样本中达到名义覆盖。我们进一步将我们的方法应用于香港抗议活动的随机实验，表明治疗特征的效果很大程度上取决于它在文本中的位置。

## Abstract
A growing number of scholars seek to estimate causal effects of unstructured data such as text, images, and video. However, existing methods typically treat each object as a single, static observation. We develop a statistical framework for dynamic causal inference with unstructured data by leveraging generative artificial intelligence (GenAI) models. Our approach enables researchers to estimate the causal effects of sequences of treatment features, including their positions within text and video. We first extract internal representations of unstructured objects from a GenAI model and then estimate a marginal structural model using a neural network architecture that jointly learns a deconfounder for each treatment feature in the sequence. Our semiparametric inference framework yields valid asymptotic confidence intervals. Simulation studies demonstrate that the proposed estimator recovers the target causal effects and that the confidence intervals achieve nominal coverage in finite samples. We further apply our method to a randomized experiment on the Hong Kong protests, showing that the effect of a treatment feature depends critically on its position within the text.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **现有局限**：因果推断中，非结构化数据（文本、图像、视频）通常被视为静态的、完整的对象，忽略了其内在的序列结构（如文本的句子顺序、视频的帧序列）。因此无法估计治疗特征出现位置（例如文本中第几句）对因果效应的影响。
- **研究动机**：实际场景中（如政治劝说、医疗、教育），非结构化数据的特征往往动态展开，后期特征的出现可能与前期特征相关，导致高维、时序性混淆。传统方法难以处理。
- **本文目标**：利用生成式AI（GenAI）模型的内部表示，提出一套动态因果推断框架，能够识别和估计序列中治疗特征在不同位置的因果效应，并给出有效的统计推断。

## 2. 方法论：核心思想、关键技术细节与算法流程

### 核心思想
- 将每个非结构化对象（如文本）视为一个有序序列（如图1所示），每个单元由多个分段（如句子）组成。
- 定义治疗特征 \( W_{is} \)（例如“提及美国对香港的承诺”）为第 \( s \) 段的二进制变量；存在高维、未观测的混杂特征 \( U_{is} \)（其他所有影响结果的文本特征）。
- 引入**去混杂因子（deconfounder）**：通过GenAI模型的内部表示 \( R_{is} \) 学习一个低维函数 \( f(R_{is}) \)，使其能够替代真正的 \( U_{is} \) 满足条件交换性，从而识别因果效应。
- 采用**动态随机干预（stochastic intervention）** 而非确定性干预，以避免高度重叠假设被严重违反。干预由增量参数 \( \delta_s \) 控制，改变每个分段治疗分配的概率。

### 关键技术细节
- **提取内部表示**：使用开源GenAI模型（如LLaMa3.1-8B）重新生成文本，提取每个分段最后一个token的隐藏状态向量 \( R_{is} \)（维度4096）。
- **神经网络架构**（图2）：输入 \( R_{is} \) 和分段索引 \( s \)，通过共享参数映射得到去混杂因子 \( f(R_{is}, s; \lambda) \)；然后将所有分段的去混杂因子与治疗历史 \( W^{S_i} \) 及序列长度 \( S_i \) 输入结果模型 \( \mu(\cdot; \theta) \) 预测结局 \( Y_i \)。参数通过最小化平方损失联合估计。
- **估计与推断**：基于影响函数（Theorem 2）构建半参数估计量，采用K折交叉拟合（K-fold cross-fitting）获得 \( \hat{\Psi}(\delta) \)，并计算渐近协方差矩阵。
- **理论保证**：在正则性条件（Assumption 8）下，证明估计量具有渐近正态性（Theorem 3），且各 nuisance 函数仅需以 \( N^{-1/4} \) 速率收敛即可。

### 算法流程简述（以K折为例）
1. 设定增量参数 \( \delta = (\delta_1, \dots, \delta_{S_{\max}}) \)。
2. 随机将数据分为K折。
3. 对于每个折 \( k \)：
   - 用其余K-1折的数据训练神经网络，估计去混杂因子 \( \hat{f} \) 和结果模型 \( \hat{\mu} \)。
   - 估计倾向得分 \( \hat{\pi}_s \)、条件治疗概率 \( \hat{p}_s \)。
   - 计算每段的权重 \( \hat{\omega}_s \) 和递归定义的结果 \( \hat{m}_s \)。
4. 整合各折的影响函数，解估计方程得到 \( \hat{\Psi}(\delta) \)，并计算方差。

## 3. 实验设计

### 模拟实验
- **数据生成**：模拟的文本内部表示 \( R_{is} \) 服从自回归高斯过程（维度512），从中构造治疗特征 \( W_{is} \)（二进制）和混杂特征 \( U_{is} \)（16维）。结局由治疗效应（随时间衰减）和混杂的线性和二次项加噪声构成。
- **样本量**：\( N \in \{2000, 3000, 4000, 5000\} \)。
- **评估范围**：增量参数 \( \delta_s \in \{0.5, 0.75, 1.0, 1.25, 1.5, 2.0\} \)（对 \( s=1,\dots,S_{\max} \) 相同）。
- **基准**：使用独立的蒙特卡洛样本（\( N_{\text{oracle}}=5000 \)）近似真实干预下的平均潜在结果作为真值（oracle）。
- **方法**：仅本文提出的动态GPI方法，未与其他方法对比（因为缺乏成熟的动态非结构化数据因果推断方法）。

### 真实数据应用：香港抗议实验
- **数据来源**：Fong and Grimmer (2023) 随机实验，包含两个调查波次（2019.12和2020.10），选取只含两个句子的被试（\( N=2085 \)）。
- **治疗特征**：是否在文本中提及“美国对香港的法律承诺（1992香港政策法案）”。
- **结局**：被试对“美国应支持香港抗议者”的同意程度（0-100分）。
- **干预设计**：分别估计仅干预第一句（\( \delta_1 \) 变化，\( \delta_2=1 \)）和仅干预第二句（\( \delta_1=1 \)，\( \delta_2 \) 变化）的因果效应。
- ****无基准对比**：作为应用示范，未与其他方法比较。

## 4. 资源与算力

- **文中未明确说明**：没有给出使用的GPU型号、数量、训练总时长。仅提及使用LLaMa3.1-8B模型提取内部表示（单次前向推理），以及训练神经网络时的超参数（如epoch=300，batch size=1024，学习率3e-5）。作者也提及计算约束，例如模拟中采用了2折而非全交叉拟合。因此，硬件资源细节缺失。

## 5. 实验数量与充分性

- **模拟实验充分性**：
  - 共进行了4种样本量 × 6个δ值 = 24个设置，每个设置200次蒙特卡洛重复，总计4800次独立模拟。
  - 报告了偏差、RMSE、95%置信区间覆盖率和平均区间长度。
  - 结果图表（图3、图4）展示了随样本量增加性能改善，覆盖接近名义水平（但部分设置欠覆盖，作者解释为数据生成中非Lipschitz导致）。
  - 但**未进行消融实验**（例如不使用去混杂因子的简化模型；不同神经网络结构；不同干预定义等），也未与静态GPI或其他方法（如文本嵌入+逆概率加权）对比。
- **真实实验**：仅用了单个数据集（香港抗议），且仅分析了2句文本。虽然能说明问题，但缺乏跨数据集或不同序列长度的验证。
- **总结**：模拟实验较全面，但缺乏对比基线；真实实验作为示例，推广性有限。总体而言，实验设计可视为充分但非绝对充分。

## 6. 主要结论与发现

- **方法论有效**：模拟结果表明，动态GPI估计量能准确恢复目标因果效应，随着样本量增大偏差和RMSE减小，置信区间覆盖率趋向名义水平。
- **位置效应显著**：在香港抗议实验中，干预第一句中的治疗特征（提及美国承诺）对平均潜在结果有明显的正向偏移（δ>1时效应更大），而干预第二句时效应极弱（几乎平坦）。这表明同一特征出现位置决定了其说服效果。
- **理论贡献**：为非结构化数据的动态因果推断提供了理论基础（非参数识别、影响函数、渐近正态性），扩展了静态GPI框架。

## 7. 优点

- **创新性**：首次将GenAI内部表示用于动态（序列）因果推断，打开研究非结构化数据位置效应的大门。
- **理论严谨**：提供了完整的识别条件（sequential separability）、影响函数形式、渐近性质证明，统计基础扎实。
- **灵活处理重叠问题**：采用随机干预，减少对严格重叠假设的依赖，更贴合实际高维设置。
- **实现可行**：提出具体的神经网络架构和交叉拟合流程，并开源（推测，论文未明确但根据作者以往工作）。
- **实际应用价值**：实验揭示文本顺序影响，对政治传播、调查设计等有直接指导意义。

## 8. 不足与局限

- **实验覆盖不全面**：
  - 仅测试了二进制治疗特征和最多5个时间步（模拟）或2个时间步（真实），尚未在更长序列（如完整文章、视频帧）中验证。
  - 没有与现有静态方法（如词袋+DR、BERT嵌入+逆概率加权）进行对比，无法量化动态框架带来的改进。
- **模拟设计偏差**：数据生成过程中的治疗分配机制为非Lipschitz（阶跃函数），导致去混杂因子估计误差传播到倾向得分，产生轻微欠覆盖。尽管作者指出，但未提出修正方案。
- **计算开销**：训练神经网络需要足够样本，小样本下可能不稳定。作者未讨论计算效率或给出最小样本需求。
- **依赖GenAI模型质量**：需要生成式模型能高质量复现输入文本并提取有意义的内部表示，对于其他模态（如视频）可能需要不同编码架构，泛化性待验证。
- **未讨论后门准则下的替代方案**：如直接用文本嵌入作为混淆代理，或采用双重机器学习框架是否更简便。
- **资源信息缺失**：无法复现计算成本，不利于研究可重复性。

（完）
