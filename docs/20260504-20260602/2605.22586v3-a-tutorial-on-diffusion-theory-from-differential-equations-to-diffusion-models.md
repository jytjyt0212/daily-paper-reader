---
title: "A Tutorial on Diffusion Theory: From Differential Equations to Diffusion Models"
title_zh: 扩散理论教程：从微分方程到扩散模型
authors: "Jiayi Fu, Yuxia Wang"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22586v3"
tags: ["query:gen-models"]
score: 9.0
evidence: 从微分方程统一扩散理论的综合教程
tldr: 扩散模型的数学基础分散于概率模型、基于分数的建模、随机微分方程和数值采样等视角。本教程从微分方程出发，以条件高斯加噪过程为起点，统一推导出正向与反向的ODE和SDE，识别边际分数为核心未知量，并通过分数匹配实现去噪训练。将DDPM、DDIM、流匹配等框架纳入统一框架，并扩展至连续嵌入空间的扩散语言模型和离散掩码扩散。旨在连接扩散过程的分析理论与现代生成算法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22586v3/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1691, \"height\": 311, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22586v3/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1700, \"height\": 642, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22586v3/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1711, \"height\": 586, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22586v3/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1548, \"height\": 665, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22586v3/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1697, \"height\": 979, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.22586v3/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1490, \"height\": 434, \"label\": \"Table\"}]"
motivation: 现有扩散模型教程对数学基础呈现分散，缺乏统一视角，导致理解困难。
method: 从条件高斯加噪过程出发，推导正向ODE/SDE，通过边际分数获得反向SDE和概率流ODE，并用分数匹配训练。
result: 统一了DDPM、DDIM、流匹配和基于分数的SDE，并扩展到连续空间的语言模型和离散掩码扩散。
conclusion: 本教程提供了从微分方程到扩散模型的完整统一视角，架起理论与算法之间的桥梁。
---

## 摘要
扩散模型已成为生成建模的主导框架，但其数学基础通常通过扩散概率模型、基于分数的建模、随机微分方程和数值采样方法分别呈现。本文旨在从微分方程的角度提供一个统一且自洽的视角。我们从条件高斯加噪过程出发，推导出常微分方程（ODE）和随机微分方程（SDE）表示，进而得到相应的边际正向动力学，然后获得使得生成成为可能的逆向时间SDE和概率流ODE。我们展示了逆向采样中的核心未知量是边际分数，解释了在噪声预测参数化下分数匹配如何成为标准的去噪目标，并讨论了实际的逆向时间采样和引导。我们进一步将DDPM、DDIM、流匹配和基于分数的SDE置于一个共同框架中，最后以连续嵌入空间中的扩散语言模型以及离散掩码标记扩散的简要讨论作为结束。本教程旨在成为扩散过程的分析基础与建立在其上的现代生成算法之间的桥梁。

## Abstract
Diffusion models have emerged as a dominant framework for generative modeling, but their mathematical foundations are often presented separately through diffusion probabilistic models, score-based modeling, stochastic differential equations, and numerical sampling methods. We write this tutorial to provide a unified and self-contained account of these viewpoints from the perspective of differential equations. Starting from a conditional Gaussian noising process, we derive ordinary differential equation (ODE) and stochastic differential equation (SDE) representations, pass to the corresponding marginal forward dynamics, and then obtain the reverse-time SDE and probability-flow ODE that make generation possible. We show that the central unknown quantity in reverse sampling is the marginal score, explain how score matching becomes the standard denoising objective under a noise-prediction parameterization, and discuss practical reverse-time sampling and guidance. We further place DDPM, DDIM, flow matching, and score-based SDEs in a common framework, and conclude with diffusion language models in continuous embedding space together with a brief discussion of discrete masked-token diffusion. The tutorial is intended as a bridge between the analytical foundations of diffusion processes and the modern generative algorithms built upon them.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，生成一份结构化、深入、客观的中文总结。

### 论文核心问题与整体含义（研究动机和背景）

*   **核心问题**：扩散模型（Diffusion Models）是现代生成建模的主导框架，但其数学基础（如扩散概率模型、基于分数的建模、随机微分方程和数值采样）的文献呈现较为分散。这导致读者在理解DDPM、基于分数的SDE、概率流ODE和流匹配等概念时，往往将它们视为独立的构造，而忽略了它们之间紧密的数学联系。
*   **整体含义**：本教程旨在提供一个**统一且自洽的视角**，从**微分方程**的角度出发，系统地阐述扩散模型的理论基础。它试图在扩散过程的**分析基础**（如Fokker-Planck方程、倒向时间理论）与现代**生成算法**（如DDPM、DDIM、流匹配）之间架起一座桥梁，使读者能够理解这些不同方向是如何从同一个数学框架中衍生出来的。

### 论文方法论

*   **核心思想**：论文以**条件高斯加噪过程**为起点，推导出其对应的**常微分方程(ODE)** 和**随机微分方程(SDE)** 表示。接着，通过边缘化（Marginalization），将条件动力学转化为描述数据总体分布演变的边缘动力学。关键的一步是，推导出正向过程的**逆向时间SDE**和**概率流ODE**，这两者构成了生成新样本的基础。论文的核心洞察在于，逆向动力学中的唯一未知量是**边际分数（Marginal Score，即`∇log pt(x)`）**，从而将生成问题转化为学习分数的任务。
*   **关键技术细节**：
    1.  **从条件高斯过程到微分方程**：定义了`Xt = αtX0 + σtϵ` 的条件正向路径，并推导出生成该路径的条件前向ODE（公式9，速度场`ut(x|x0)`）和条件前向SDE（公式15，`dXt = ftXt dt + gt dWt`）。
    2.  **边缘化与边际动力学**：通过对条件动力学中的`X0`进行积分，得到描述数据分布`pt(x)`演变的边际动力学。论文证明了边缘化后的正向SDE与条件SDE形式相同（公式22），但边际速度场和边际Fokker-Planck方程中的关键未知量正是边际分数`∇log pt(x)`（通过费希尔恒等式联结）。
    3.  **逆向动力学生成**：基于边际正向动力学，推导出用于生成新数据的逆向时间SDE（公式29）和逆向概率流ODE（公式33）。这两个方程都依赖于边际分数`∇log pt(x)`。
    4.  **学习分数：分数匹配与去噪目标**：由于边际分数不可直接计算，论文使用神经网络`Sθ(x,t)`来近似。通过后验均值噪声恒等式（公式35），将分数匹配目标（公式34）等价转化为更实用的**噪声预测目标**（公式38）：
        `L(θ) = ½ ∫ ω(t) E[‖ϵθ(αtX0 + σtε, t) - ε‖²] dt`
    5.  **统一DDPM和DDIM**：论文将离散的DDPM和DDIM算法与连续的逆向SDE/ODE框架精确对照。展示了DDPM训练目标与逆向SDE损失在离散网格上的等价性；DDPM的推理可以被看作逆向SDE的一阶离散化（尽管随机项略有差异）；而DDIM的确定性采样则是**逆向概率流ODE的一阶离散化**（公式88-89），从而完美统一。
*   **算法流程（文字描述）**：
    *   **训练**：采样数据`X0`和时间`t`，加入高斯噪声`ε`，训练一个神经网络`ϵθ`来预测添加的噪声`ε`，即`‖ϵθ(αtX0 + σtε, t) - ε‖²`最小化。
    *   **无条件采样（逆向SDE/ODE）**：从高斯噪声`X1`开始，按照时间从1到0，使用训练好的模型`ϵθ`代入逆向SDE（公式39）或逆向ODE（公式40）的离散化版本（如Euler-Maruyama或DPM-Solver），逐步去噪得到生成样本`X0`。
    *   **有条件采样（引导）**：
        *   **分类器引导**：联合使用无条件分数模型和一个额外的时序分类器，通过贝叶斯定理修改分数（`∇log pt(x|c) = ∇log pt(x) + γ∇log pφ(c|x,t)`）。
        *   **无分类器引导**：仅训练一个同时支持条件和无条件的单一模型`ϵθ(x,t,c)`，通过外推条件和无条件预测的差异来强化条件控制（`ϵcfg = ϵθ(x,t,∅) + s(ϵθ(x,t,c) - ϵθ(x,t,∅))`）。

### 实验设计

*   这是一篇**理论教程**性质的文章，**没有设计和进行任何新的数值实验**。
*   **验证方式**：论文的“实验”主要体现为对现有方法的**理论对照和案例分析**，包括：
    *   严格推导和证明DDPM的训练/推理与逆向SDE框架的联系（第7章）。
    *   严格推导和证明DDIM的推理是逆向概率流ODE的离散化（第7.4节）。
    *   将流匹配（Flow Matching）和基于分数的SDE生成模型**重新阐述**为其提出的框架的一种变体（第8章）。
    *   将连续嵌入空间的扩散语言模型**映射**回其逆向ODE/SDE框架（第9章）。

### 资源与算力

*   **论文未明确说明**使用了任何计算资源、GPU型号、数量或训练时长。作为一篇理论教程，这并非其关注点。

### 实验数量与充分性

*   **实验数量**：零。这是一篇纯理论推导和综述性文章，没有进行任何实验。
*   **充分性**：作为一篇**教程**，其验证方式的“充分性”不在于实验覆盖，而在于其理论推导的**完备性、严密性和统一性**。论文成功地将多个看似独立的模型（DDPM, DDIM, Score-based SDE, Flow Matching）置于一个统一的数学框架下，并通过严谨的公式推导建立了它们之间的等价或近似关系，从这个角度看，其论证是充分的。对于离散扩散语言模型（第9.5节），论文简要提及但指出其不在本教程范围内，这是一个诚实的边界界定。

### 主要结论与发现

*   **提供了统一视角**：成功地从微分方程角度统一了DDPM、DDIM、流匹配和基于分数的SDE等主流扩散模型框架。揭示出这些模型都源于同一个数学基础：即一个条件高斯加噪过程及其对应的边际和逆向动力学。
*   **明确了核心地位**：明确指出“边际分数`∇log pt(x)`”是逆向采样中的核心未知量，所有训练目标（分数匹配、去噪损失）最终都是为了学习这个量。
*   **澄清了模型关系**：
    *   DDPM训练等价于在离散时间点上学习分数。
    *   DDPM推理是逆向SDE的一阶离散化，而**DDIM推理则是逆向概率流ODE的一阶离散化**。
    *   流匹配的生成性ODE与分数匹配导向的概率流ODE本质上是对同一动力学的不同参数化。
*   **扩展了应用边界**：展示了该统一框架如何自然地延伸到连续嵌入空间的扩散语言模型，为相关领域的研究提供了坚实的理论基础。

### 优点

*   **结构清晰，逻辑连贯**：论文从最基础的假设出发，一步一步地构建理论框架，逻辑链条非常清晰，非常适合作为学习和研究扩散模型的入门教材。
*   **数学严谨**：论文包含了大量详细的公式推导和证明（如附录），确保了理论的严谨性和正确性。
*   **强大的统一性**：最大的亮点是将多个“地表最强”的生成模型框架统一，揭示其内在联系，这对于推动领域理论的整合和发展具有重要价值。
*   **实用性强**：虽然理论性很强，但论文紧扣实际算法（DDPM、DDIM），将理论与离散实现步骤（如Euler-Maruyama离散化、DDIM展开式）进行精确对比，使读者能理解算法背后的深层原理。

### 不足与局限

*   **缺乏实验验证**：作为一篇全理论教程，它缺乏在标准数据集（如CIFAR-10, ImageNet）上的定量实验来验证或展示其统一视角下的性能。读者无法直观地比较不同离散化方案（如DDPM vs. DDIM）在其框架下实际效果的优劣。
*   **未覆盖所有方向**：论文明确排除了离散状态空间的扩散模型（第9.5节），而这正是许多现代语言模型（如基于[MASK]的模型）所采用的方法。此外，对于最新的、更复杂的模型变体（如Video Diffusion, Consistency Models等）也只是在参考文献提及或未深入讨论。
*   **部分推导近似**：在将DDPM与逆向SDE离散化类比时（第7.3节），论文承认随机项（`ηEM`和`ηDDPM`）并不严格相等，仅证明一阶近似和在内隐步上的渐近一致性。对于更高阶的近似或误差分析，并未深入。
*   **对初学者门槛较高**：尽管是教程，但要求读者具备一定的随机微积分（Ito calculus）、概率论和微分方程（ODE/PDE）基础，对于完全零基础的读者可能仍然比较困难。

（完）
