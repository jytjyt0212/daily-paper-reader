---
title: Causal invariance in graphical models with latent variables
title_zh: 含潜变量图模型中的因果不变性
authors: "Marco Borriero, Monia Lupparelli, Giovanni M. Marchetti, Veronica Vinciotti"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13281v1"
tags: ["query:causal-rep"]
score: 8.0
evidence: 形式化潜变量图模型中的因果不变性条件，与因果表征学习相关
tldr: 因果发现中，因果不变性原理通过跨实验稳定性识别父节点，但当存在未观测的隐父节点时，观测变量上的诱导图可能不是有向无环图且不唯一。本文刻画了隐父节点配置下的诱导图结构，并形式化了因果不变性得以保留以识别观测父节点的条件。针对多变量高斯目标，推导了检验不变性的充要条件，为含隐变量的因果推断提供了理论基础和可操作准则。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13281v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1665, \"height\": 438, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13281v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1490, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13281v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1481, \"height\": 491, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13281v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1281, \"height\": 278, \"label\": \"Figure\"}]"
motivation: 现有因果不变性方法假设所有父节点可观测，但实际中隐变量普遍存在，导致诱导图结构复杂且不唯一，阻碍父节点识别。
method: 刻画隐父节点配置下的观测变量诱导图，形式化因果不变性保留的条件，针对多变量高斯模型建立检验不变性的充要条件。
result: 给出了存在隐父节点时因果不变性成立的充要条件，并证明可用于有效识别观测父节点。
conclusion: 扩展了因果不变性原理至含隐变量的场景，为因果发现提供了理论保障和实用检验方法。
---

## 摘要
因果发现旨在从观测或干预数据中识别变量间的因果关系，通常用有向无环图表示。因果不变性原则通过利用不同实验环境下因果效应的稳定性，能够识别目标变量的因果父节点。然而，当某些父节点未被观测到时，观测变量上的诱导图可能不再是DAG，且可能不唯一，从而使得因果推断复杂化。针对潜父节点的相关配置，我们刻画了诱导图，并形式化了因果不变性在识别观测父节点时得以保持的条件。对于多元高斯目标变量，我们正式建立了检验此类不变性的充分必要条件。

## Abstract
Causal discovery aims to identify causal relationships among variables from observational or interventional data, typically represented by a directed acyclic graph (DAG). The causal invariance principle enables the identification of the causal parents of target variables by exploiting the stability of causal effects across different experimental settings. When some parents are unobserved, however, the induced graph over the observed variables may no longer be a DAG, and it may not be unique, complicating causal inference. For relevant configurations of latent parents, we characterize the induced graph and formalize the conditions under which causal invariance is preserved for the identification of the observed parents. Necessary and sufficient conditions for testing such invariance are formally established for a multivariate Gaussian target.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：因果发现中的不变性原理（Invariant Causal Prediction, ICP）假设所有因果父节点可观测，从而利用不同实验环境下的条件分布不变性识别父节点。然而，当存在未观测的潜父节点（如隐藏中介或混杂）时，观测变量上的诱导图可能不再是DAG，且可能不唯一，导致不变性条件被破坏，父节点识别失败。
- **背景与动机**：现实数据中潜变量普遍存在（如隐藏混淆、缺失中介），传统方法要么假设因果充分性，要么依赖复杂的潜变量建模。本文旨在通过刻画诱导图结构，形式化地给出因果不变性在潜变量存在时得以保持的条件，从而扩展ICP的适用范围。

### 2. 方法论

- **核心思想**：基于有向混合图（acyclic directed mixed graphs）表示潜变量被边缘化后的观测变量图结构。将潜父节点分为两类：
  - **过渡节点**（transition node）：如隐藏中介 `X → H → Y`，忽略后仍为DAG，观测父节点增大（包含原本通向H的父节点）。
  - **源节点**（source node）：如隐藏混杂 `X ← H → Y`，忽略后产生双向边（`X ↔ Y`），且X与Y的误差项相关，导致 `ϵ_Y ⊥̸⊥ X`。
- **关键条件**：
  - 若无隐藏混杂（仅过渡或源节点且无倒U效应）：诱导模型仍满足**强不变性**（定义1），即条件分布 `p(Y|X_gPA)` 跨环境不变（**命题1**）。
  - 若存在隐藏混杂：需引入工具变量保证可识别性，此时诱导模型满足**弱不变性**（定义2），即函数形式不变但误差分布不变（**命题2**）。
- **检验充要条件**（**定理1**无混杂，**定理2**有混杂）：
  - 对候选父集 `X_S`，假设线性高斯结构 `Y = B^T X + ε`，不变性等价于条件 `E[(Y - B^T X)(Y - B^T X)^T] = Σ` 成立，且 **Bartlett恒等式**（score与外积信息阵相等）几乎必然成立。
  - 通过Box's M检验等统计检验跨环境残差协方差矩阵是否相等来验证。
- **算法流程**：枚举所有可能的父集子集 `X_S` → 对每个子集拟合线性回归 → 检验跨环境残差协方差矩阵相等性 → 接受不变性的子集中选择BIC最小的作为最终父集。

### 3. 实验设计

- **模拟数据**：
  - **无混杂场景**（图2）：生成DAG包含一个隐藏中介（H1）、一个隐藏源节点（H2）以及一个既作中介又作源节点（H3）。观测变量8个，目标为二元高斯Y。基准：使用**定理1**（无混杂假设）进行不变性检验。
  - **有混杂场景**（图3）：增加了两个隐藏混杂（H4, H5），分别混淆X1→Y2和X5→Y1，并引入工具变量Z1、Z5。基准：对比**定理1**（忽略混杂）与**定理2**（考虑混杂）。
- **真实数据**：Sachs等人（2005）的流式细胞术数据，选自两个环境（观测环境 + PMA干预PKC）。目标变量为二元{P38, Erk}，候选父集为9个其余蛋白。分为两个案例：无混杂假设（找到{Jnk, PKC, Akt}）和隐藏Jnk作为混杂（使用工具变量后找到{PKC, Akt}）。
- **对比方法**：主要对比朴素ICP（忽略混杂）与所提考虑混杂的检验；未与其他先进方法（如Causal Dantzig、Anchor Regression）直接对比。

### 4. 资源与算力

- 文中**未明确提及**使用的GPU型号、数量或训练时长。所有实验均基于蒙特卡洛模拟（50次重复），仅需CPU即可完成，计算资源需求较低。

### 5. 实验数量与充分性

- 模拟实验：两个生成模型，每个模型50次重复，报告了正确识别父集的成功率（无混杂时92%，忽略混杂时仅18%，考虑混杂后回升至92%）。
- 真实数据：仅一个案例研究（flow cytometry），未进行多次重复或基准对比。
- **充分性评价**：
  - **优点**：模拟实验明确展示了方法在有无混杂时的表现差异，验证了理论必要性。
  - **不足**：实验规模较小，仅测试了二元目标、有限维度（p=8或9）；未与其他因果发现方法（如CAM、PC、FCI）进行系统对比；真实数据仅为定性演示，缺乏量化指标（如干预验证）。

### 6. 主要结论与发现

- 在特定潜变量配置（隐藏中介、隐藏源节点）下，因果不变性可在观测变量的诱导图中保留。
- 对于线性高斯目标，给出了**充要条件**用于检验不变性，并给出了可操作的统计检验方法。
- 忽略隐藏混杂会严重降低父集识别准确率（从92%降至18%），而引入工具变量并采用弱不变性检验可有效恢复。
- 流式细胞术数据示例表明方法能识别出与已知生物学网络一致的父集。

### 7. 优点

- **理论严谨**：形式化了潜变量存在下不变性保持的图模型条件，提供充要条件，具有较强理论价值。
- **实用性强**：将复杂图论结果转化为可实现的统计检验（Box's M检验），便于实际应用。
- **考虑全面**：同时处理隐藏中介和隐藏源节点，并区分有/无混杂场景，覆盖了常见潜在结构。

### 8. 不足与局限

- **模型假设限制**：仅考虑线性高斯模型，且目标变量为（多元）高斯分布；未推广至非线性、非参数或离散分布。
- **潜变量配置局限**：仅考虑了过渡节点和源节点两类（即潜父节点要么是中介要么是源头），未涵盖更复杂的结构如多条路径、循环或非马尔可夫依赖。
- **实验覆盖不足**：模拟维度低（p≤9），真实数据仅有单案例，缺乏高维或大规模实验；未与其他稳健性方法（如Anchor Regression、Causal Dantzig）进行定量对比。
- **基准不完整**：仅对比了忽略混杂的朴素ICP，未引入同等条件下的最优干扰方法（如含工具变量的Causal Dantzig）。
- **可推广性疑问**：流式细胞术数据中的工具变量选择依赖先验知识（共识网络），在无先验时难以自动选择。

（完）
