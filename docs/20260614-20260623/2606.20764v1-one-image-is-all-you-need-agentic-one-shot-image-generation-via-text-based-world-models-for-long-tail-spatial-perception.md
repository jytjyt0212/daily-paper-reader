---
title: "One Image is All You Need: Agentic One-Shot Image Generation via Text-Based World Models for Long-Tail Spatial Perception"
title_zh: 一图足矣：基于文本世界模型的长尾空间感知智能单样本图像生成
authors: "Keqin Zeng, Shuting Su, Shihao Lin, Ziyue Li, Rui Zhao"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20764v1"
tags: ["query:self-driving"]
score: 9.0
evidence: 自动驾驶中用于长尾空间感知的世界模型
tldr: 针对长尾空间感知数据稀缺导致检测性能下降的问题，提出WMGen-v1框架，利用单张参考图像通过LVLM构建结构化场景表示，LLM在物理合理性约束下进行场景扩展，扩散模型生成多样长尾训练数据。在工业数据集、ROADWork和LaRS基准上，WMGen-v1优于基线方法，仅用合成数据训练的检测器在总体指标上接近真实数据性能，有效缓解了长尾数据稀缺对下游空间感知的影响。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20764v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 832, \"height\": 398, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20764v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1813, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20764v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1832, \"height\": 682, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20764v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 894, \"height\": 974, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20764v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 901, \"height\": 330, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20764v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 902, \"height\": 327, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20764v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 889, \"height\": 630, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20764v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 873, \"height\": 501, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20764v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 887, \"height\": 344, \"label\": \"Table\"}]"
motivation: 长尾空间感知数据稀缺导致检测性能下降，现有合成方法缺乏空间约束和物理一致性。
method: 提出WMGen-v1，使用LVLM从单图构建场景表示，LLM进行物理合理的场景扩展，扩散模型生成长尾数据。
result: 在工业数据集和基准上优于基线，合成数据训练的检测器接近真实数据性能。
conclusion: WMGen-v1有效缓解长尾数据稀缺，提升空间感知泛化能力。
---

## 摘要
可靠的空间决策自动化，如自动驾驶和海上监视，关键依赖于鲁棒的视觉感知。然而，真实世界的时空数据表现出严重异质性，常常表现为安全关键场景的极端长尾分布。这种数据稀缺性会导致数据集偏移，从而降低检测性能并带来安全风险。虽然合成数据生成提供了一种潜在解决方案，但现有的生成方法，如扩散模型和生成对抗网络（GANs），通常缺乏明确的空间基础结构和结构约束，导致生成场景中存在空间和物理上的不一致性。为了应对这些挑战，我们引入了WMGen-v1，一种用于长尾空间数据生成的基于文本的世界模型智能框架。WMGen-v1使用大型视觉语言模型（LVLM）从单张参考图像构建结构化的场景表示，同时大型语言模型（LLM）在物理合理性和常识约束下执行基于引导的场景扩展。随后，以这一推理过程产生的结构化语义表示为条件，扩散模型生成多样化且物理基础扎实的长尾训练数据。在内部工业数据集、ROADWork和LaRS基准上的实验表明，WMGen-v1优于基线方法。值得注意的是，仅使用WMGen-v1合成数据训练的检测器在数据集级别的聚合指标上接近仅使用真实数据的性能，突显了其缓解下游空间感知长尾数据稀缺性的潜力。

## Abstract
Reliable spatial decision automation, such as autonomous driving and maritime surveillance, critically depends on robust visual perception. However, real-world spatiotemporal data exhibits severe heterogeneity, often manifesting as extreme long-tail distributions for safety-critical scenarios. This data scarcity induces dataset shift that degrades detection performance and pose safety risks. While synthetic data generation offers a potential solution, existing generative approaches, such as diffusion models and Generative Adversarial Networks (GANs), often lack explicit spatial grounding and structural constraints, resulting in spatial and physical inconsistencies in generated scenes. To address these challenges, we introduce WMGen-v1, an agentic text-based world model framework for long-tail spatial data generation. WMGen-v1 employs a Large Vision-Language Model (LVLM) to construct a structured scene representation from a single reference image, while a Large Language Model (LLM) performs guidance-based scene expansion under physical plausibility and commonsense constraints. Subsequently, conditioned on the structured semantic representations produced by this reasoning process, a diffusion model generates diverse and physically grounded long-tail training data. Experiments on internal industrial datasets, ROADWork, and LaRS benchmarks demonstrate that WMGen-v1 outperforms baseline approaches. Notably, detectors trained solely on WMGen-v1 synthetic data approach real-only performance on aggregate dataset-level metrics, highlighting its potential to alleviate long-tail data scarcity for downstream spatial perception.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在自动驾驶、海上监视等空间自动化系统中，真实世界采集的视觉数据呈高度异质性，安全关键的罕见场景和物体类别（长尾分布）严重缺失，导致检测模型对稀有案例感知能力弱，存在漏检风险。
- **现有方法局限**：直接收集真实长尾数据成本高、不现实；现有合成数据生成方法（GAN、扩散模型）缺乏显式空间锚定和结构约束，生成的场景常出现空间不一致（如物体悬空、重叠）和物理不合理（如透视错误、尺度失真），对下游感知任务帮助有限。
- **研究目标**：提出一种只需单张参考图像和一条用户指令，即可生成物理合理、空间一致的长尾训练数据的框架，缓解数据稀缺，提升模型在罕见场景下的泛化能力。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 核心思想
将长尾数据生成问题分解为三阶段智能管线（Agentic Pipeline），以文本作为世界模型交互介质：
- **结构化场景建模**（LVLM）：从单张参考图像提取几何、语义、空间锚点。
- **智能世界模型扩展**（LLM）：在物理合理性和常识约束下，沿指定维度（天气、光照、上下文等）扩展场景，生成多样化但保留关键结构的语义描述。
- **参考条件图像生成**（扩散模型）：基于扩展后的结构化提示和原始参考图像，生成视觉一致、物理合理的合成图像。

### 2.2 关键技术细节
- **LVLM 模块**：输入参考图像 \( I_{\text{ref}} \) 和初始提示 \( P_{\text{init}} \)，输出结构化场景表示 \( J_{\text{spatial}} \)，包含：场景描述 \( S_{\text{des}} \)、全局抽象 \( W \)、相机视角约束 \( V \)、核心区域 \( A_{\text{core}} \) 与可扩展区域 \( A_{\text{expand}} \)、以及每个目标细粒度信息 \( T = \{ t_i \} \)（名称、标签、语义、索引、属性）。
- **LLM 模块**：输入 \( J_{\text{spatial}} \) 和扩展引导条件 \( e \in E \)（如"雨天"、"夜间"），输出扩展后的场景状态 \( J_{\text{expand}} = \{ (s_i, t_i^{\text{exp}}, v_i) \} \)，其中 \( s_i \) 为场景描述，\( t_i^{\text{exp}} \) 为目标描述，\( v_i \) 为渲染指令。最终构造生成提示 \( P_{\text{gen}} \)。
- **扩散模块**：使用 FLUX.1 Kontext Pro 模型，以 \( I_{\text{ref}} \) 和 \( P_{\text{gen}} \) 为条件，生成合成图像 \( I_{\text{syn}} \sim F_{\text{Diff}}(I_{\text{ref}}, P_{\text{gen}}, \sigma) \)。扩散过程保留参考图的结构锚点，同时注入环境变化。
- **形式化**：全局映射 \( I_{\text{syn}} \sim \Phi(\cdot | I_{\text{ref}}, e) \)，其中 \( e \) 为引导条件。

## 3. 实验设计

### 3.1 数据集与基准
- **ROADWork**：施工区自动驾驶感知数据集，5318训练/2098验证，长尾严重（尾类如Police Officer仅69张，Bike Lane 61张）。
- **LaRS**：海上障碍物检测数据集，2605训练/198验证，长尾类如Animal 72张、Float 20张。
- **内部工业监控数据集**：来自真实监控系统（水库、工业安全等），仅用于定性评估。

### 3.2 对比方法
- **Baseline 1**（LLM增强扩散）：使用相同扩散模型，但仅用LLM无约束改写文本提示替换结构化场景推理。
- **Baseline 2**（One-Shot GAN）：经典单样本GAN，基于内部图像统计生成。
- **消融：WMGen-v1 w/o LLM**：仅用LVLM场景表示，去掉LLM推理模块。
- **完整WMGen-v1**：三星管线。

### 3.3 检测器与评估指标
- 检测器：一阶段 YOLO11s、两阶段 Faster R-CNN ResNet-152。
- 指标：COCO协议下的 mAP@50、mAP@50:95，以及逐类 mAP@50。

### 3.4 生成数据量
- ROADWork：针对Police Officer、Police Vehicle、Bike Lane、Other Roadwork Objects生成280张。
- LaRS：针对Animal、Float生成60张（Animal 40张、Float 20张）。
注：原表I中LaRS生成总计60张，但文中其他处描述可能略有出入，但总体量较小。

## 4. 资源与算力
- **明确提及**：所有实验在 **NVIDIA V100 GPU** 上运行。
- **未明确**：未说明具体GPU数量、训练总时长。对Baseline 2（One-Shot GAN）提到采用60k迭代（超过150k后收益递减），其余扩散生成和检测器训练时长未给出。算力细节不够透明，但鉴于模型规模（FLUX、YOLO、Faster R-CNN）和数据集大小，推测为合理配置。

## 5. 实验数量与充分性
### 5.1 实验组数
- **两个公开数据集**（ROADWork、LaRS）上均进行：
  - 5种实验设置：Real-only、Real+Baseline1、Real+Baseline2、WMGen-v1-only、Real+WMGen-v1。
  - 两种检测器（YOLO、Faster R-CNN）。
- **消融实验**：WMGen-v1 w/o LLM（定性+定量？仅定性比较图中有展示，但定量结果表中未单独列出该消融的数值，仅在定性分析中提及）。
- **定性比较**：内部工业数据集、ROADWork、LaRS各展示一张案例图。
- **逐类分析**：给出COCO格式表格（表V、表VI），并汇总长尾类平均性能（表VII）。
- 总体实验数量：约2（数据集）×5（设置）×2（检测器）=20组定量实验（再加上消融定性组）。

### 5.2 充分性评价
- **优点**：覆盖了不同场景（道路、海事、工业），对比了两种主流扩散/GAN基线，检测器含一阶段和两阶段，评估了逐类性能，消融设计（去LLM部分）合理。综合来看实验设计较充分，评价公平。
- **不足**：
  - 内部数据集仅做定性分析，缺乏定量。
  - 消融（w/o LLM）在定量表中未给出数值，仅在定性比较；缺少针对LVLM模块的独立消融。
  - 生成数据量较少（ROADWork 280张/LaRS 60张），未测试不同生成数据规模对性能的影响。
  - 未与更先进的扩散控制方法（如GeoDiffusion、ControlAug）进行直接对比，尽管它们在related work中提到。

## 6. 论文的主要结论与发现
1. **WMGen-v1 显著优于基线**：在ROADWork和LaRS上，Real+WMGen-v1的mAP均高于Real+其他基线，且WMGen-v1-only性能接近Real-only（如ROADWork YOLO: 0.4270 vs 0.4330）。
2. **提升集中于长尾类**：尾类平均mAP从0.1558提升至0.2755（+0.1197），Police Officer提升0.093，Float从0.0提升到0.384。
3. **对结构敏感类提升有限**：Police Vehicle等需要精确几何和外形的类别收益较小或下降，说明文本到图像的转换会丢失部分细粒度信息。
4. **合成数据可有效替代部分真实数据**：仅用合成数据训练的模型在某些场景下可接近真实数据训练效果，对缓解数据稀缺有实际价值。

## 7. 优点：方法或实验设计上的亮点
- **创新框架**：首次系统地将LVLM+LLM+扩散模型协同用于长尾空间感知数据生成，形成“结构化场景建模→世界模型扩展→参考条件生成”的端到端管线。
- **物理合理性增强**：通过LLM显式注入物理约束（如物体不能浮空、尺度合理、透视一致），有效减少常见生成幻觉。
- **资源高效**：仅需单张参考图像和一条指令，无需大量真实数据或人工标注，自动生成并自动标注（使用Grounding DINO伪标签）。
- **实验设计严谨**：对比了当代主流合成方法（LLM扩散、One-Shot GAN），并采用两种不同范式检测器，确保结论的泛化性；逐类和长尾汇总分析深入揭示方法效果边界。
- **分析细致**：对类别性能差异（如Police Vehicle下降）给出了合理解释（几何信息丢失、类别混淆），对后续改进有指导意义。

## 8. 不足与局限
- **空间信息损失**：将视觉观察转为离散文本描述必然丢失细节，尤其影响需要精确几何形状的目标（如车辆、船只），导致部分刚性物体生成质量下降。
- **类别选择非单纯基于频率**：实验发现“Work Equipment”（中频类）仍难提升，而“Swimmer”因与大洋面物体视觉相似而受到干扰，说明仅凭少样本数量选择增强目标不够充分，需考虑视觉混淆度。
- **缺少闭环反馈**：当前管线是开环的，完全依赖LVLM/LLM的一次推理和扩散模型的单次生成，没有验证物理/语义正确性的机制，可能产生错误场景而不自知。
- **生成模型依赖**：依赖特定的LVLM（GPT-4o）、LLM（GPT-5.2）、扩散模型（FLUX.1 Kontext Pro），结果易受这些底层模型能力影响，可复现性受限于商用API。
- **计算资源说明不足**：未详细报告GPU数量、训练时长等硬件消耗，影响重复实验和能效分析。
- **消融实验不够完整**：缺少LVLM-only（即去掉LVLM直接输入原始图像和LLM）的定量比较，也未评估不同扩散基础模型的影响。
- **应用局限**：仅测试了目标检测任务，未在语义分割、实例分割或跟踪任务上验证；只覆盖了自动驾驶和海事两个具体场景，通用性尚需进一步验证。

（完）
