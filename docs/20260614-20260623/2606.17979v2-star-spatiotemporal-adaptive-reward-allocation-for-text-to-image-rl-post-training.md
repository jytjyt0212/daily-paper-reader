---
title: "STAR: SpatioTemporal Adaptive Reward Allocation for Text-to-Image RL Post-Training"
title_zh: STAR：用于文本到图像强化学习后训练的时空自适应奖励分配
authors: "Jinjie Shen, Wei Deng, Xian Hu, Daiguo Zhou, Jian Luan"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17979v2"
tags: ["query:gen-models"]
score: 9.0
evidence: 面向扩散和流模型的RL后训练时空自适应奖励分配
tldr: 针对文本到图像生成中RL后训练将整图奖励均匀分配给全部生成轨迹的问题，提出时空自适应奖励分配方法STAR。利用生成模型内的文本-图像注意力，从用户关注的核心内容出发，构建随去噪步骤动态变化的空间分配图，将分组相对优势分配给更相关的潜在区域，并通过空间解析策略目标对相关区域施加更强更新。在Stable Diffusion 3.5 Medium上，GenEval、OCR文本渲染和PickScore分别达到0.9759、0.9757和23.60，提升了组合语义对齐和文本渲染能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17979v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1651, \"height\": 555, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17979v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1535, \"height\": 618, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17979v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 769, \"height\": 472, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17979v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 678, \"height\": 458, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17979v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 767, \"height\": 489, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17979v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1635, \"height\": 931, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17979v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1643, \"height\": 1008, \"label\": \"Table\"}]"
motivation: 现有RL后训练将整图奖励均匀分配给全部生成轨迹，忽略了去噪步骤的时序结构和图像内容的空间结构，导致策略更新难以聚焦于真正影响奖励的生成组件。
method: 利用生成模型内的文本-图像注意力，构建随去噪步骤和采样动态变化的空间分配图，将分组相对优势分配给更相关的潜在区域，并通过空间解析策略目标实施差异化更新。
result: 在GenEval、OCR和PickScore上分别取得0.9759、0.9757和23.60，优于基线，且几乎无额外计算开销。
conclusion: STAR通过细粒度的时空奖励分配有效提升了文本到图像生成的语义对齐、文本渲染和偏好优化性能。
---

## 摘要
现有的文本到图像生成的强化学习后训练方法通常将最终图像奖励转换为单个标量优势，并以相同的强度应用于整个生成轨迹。然而，文本到图像生成自然具有时间和空间结构：不同的去噪步骤负责不同的生成阶段，而真正决定文本对齐的内容通常只出现在图像的一部分。这种粒度不匹配使得策略更新难以聚焦于实际影响奖励的生成组件。为了解决这个问题，我们提出了用于文本到图像扩散和流模型的强化学习后训练的**时空自适应奖励（STAR）分配**。STAR利用生成模型内部的文本-图像注意力，并从用户真正关心的提示中的核心内容开始。它构建了在去噪步骤和展开过程中动态变化的空间分配图，并将相同的组相对优势分配给更相关的潜在区域，几乎不增加额外的计算开销。然后，STAR通过空间解析的策略目标对这些区域应用更强的策略更新。我们使用Stable Diffusion 3.5 Medium作为基础模型，并在三个任务上进行了评估：GenEval、OCR文本渲染和PickScore。实验结果表明，STAR在不改变外部奖励源的情况下，改善了组合语义对齐、文本渲染和偏好优化，分别在GenEval、OCR和PickScore上达到了0.9759、0.9757和23.60。

## Abstract
Existing RL post-training methods for text-to-image generation usually convert the final-image reward into a single scalar advantage and apply it with the same strength to the entire generative trajectory. However, text-to-image generation naturally has temporal and spatial structure: different denoising steps are responsible for different generation stages, and the content that truly determines text alignment often appears only in part of the image. This granularity mismatch makes it difficult for policy updates to focus on the generative components that actually affect the reward. To address this issue, we propose \textbf{SpatioTemporal Adaptive Reward (STAR) Allocation} for RL post-training of text-to-image diffusion and flow models. STAR uses text-image attention inside the generative model and starts from the core content that the user truly cares about in the prompt. It constructs spatial allocation maps that dynamically vary across denoising steps and rollouts, and allocates the same group-relative advantage to more relevant latent regions with almost no additional computational overhead. STAR then applies stronger policy updates to these regions through a spatially resolved policy objective. We use Stable Diffusion 3.5 Medium as the base model and evaluate on three tasks: GenEval, OCR text rendering, and PickScore. Experimental results show that STAR improves compositional semantic alignment, text rendering, and preference optimization without changing the external reward source, achieving $\mathbf{0.9759}$, $\mathbf{0.9757}$, and $\mathbf{23.60}$ on GenEval, OCR, and PickScore, respectively.

---

## 论文详细总结（自动生成）

# 论文总结：STAR: SpatioTemporal Adaptive Reward Allocation for Text-to-Image RL Post-Training

## 1. 核心问题与整体含义（研究动机和背景）

- **现有问题**：当前文本到图像生成的强化学习后训练方法（如Flow-GRPO）将最终图像奖励转换为一个标量优势值，并以相同的强度应用于整个生成轨迹（包括所有去噪步骤和所有空间位置）。然而，扩散/流模型的生成过程具有天然的**时序结构**（不同去噪阶段负责布局、物体形成、细节细化）和**空间结构**（只有部分潜在区域与 prompt 中的关键主体、属性、关系直接相关）。这种**粒度不匹配**导致策略更新难以聚焦于真正影响奖励的生成组件，尤其当 prompt 包含复杂计数、属性绑定和空间关系时，背景区域与主体区域对奖励的贡献应被区别对待。
- **研究目标**：提出一种时空自适应的奖励分配方法，在**不改变外部奖励源**的前提下，将同一图像级别的优势信号更精确地路由到去噪时间步和潜在空间中的相关区域，使策略更新更关注决定文本对齐的关键生成组件。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
利用生成模型内部的文本-图像注意力（如 joint-attention 或 cross-attention），从 prompt 中提取用户关心的核心文本单元（subjects, attributes, spatial relations 等），构建随去噪步和 rollout 动态变化的**空间分配图**，将分组相对优势（group-relative advantage）**按空间位置重新加权**，并通过空间解析的策略目标实施差异化更新。

### 关键技术细节
1. **文本单元提取**：将 prompt 分解为核心组件集合 K(c)（如 "a futuristic-style building" 中的 "building" 和 "futuristic style"）。每种任务使用不同规则（GenEval：去除前缀如 "a photo of" 后剩余部分；PickScore：可选 LLM 分解；OCR：提取引号内的文字）。
2. **注意力引导的空间分配图**：
   - 在 rollout 的每个去噪步 t，从选定的注意力层（如 transformer blocks 10-15）读取图像到文本的注意力权重 αₜⁱ(u,j)（u: 空间位置，j: 文本 token）。
   - 对每个文本单元 qₖ，计算其空间概率图 Mₜⁱ·ᵏ(u) = (1/|Tₖ|) Σⱼ∈Tₖ αₜⁱ(u,j)。
   - 将所有文本单元热力图平均得到综合热力图 Hₜⁱ(u) = (1/K) Σₖ Mₜⁱ·ᵏ(u)。
   - 归一化到 [1, λ] 区间（λ 控制分配强度）：Cₜⁱ(u) = 1 + (λ-1) * (Hₜⁱ(u) - min) / (max - min + ε)，得到最终系数。
3. **空间解析策略目标**：
   - 定义空间优势图：Aₜⁱ(u) = Aⁱ * Cₜⁱ(u)（Aⁱ 为样本级优势）。
   - 计算空间比率 ρⁱ_θ,ₜ(u)，结合 stop-gradient 保持标量幅度，分配梯度到空间位置。
   - 优化 clipped spatial objective（类似 PPO）：
     J_STAR(θ) = E[ (1/T) Σₜ (1/|Ωₜ|) Σᵤ min( ρⁱ_θ,ₜ(u) Aₜⁱ(u), clip(ρⁱ_θ,ₜ(u), 1-ε, 1+ε) Aₜⁱ(u) ) ] - β D_{KL}
   - 其中 D_{KL} 是当前策略与冻结参考策略之间的 KL 正则项，防止奖励破解。

### 算法流程（文字）
1. 对于每个 prompt，采样一组（G=24）图像，获取 rollout 轨迹（潜变量序列）。
2. 在 rollout 的前向过程中记录选定的注意力层输出。
3. 根据 prompt 提取文本单元，计算每个去噪步的空间热力图 Hₜⁱ，归一化得到系数 Cₜⁱ。
4. 计算样本级优势 Aⁱ（通过组相对归一化）。
5. 构建空间优势图 Aₜⁱ(u) = Aⁱ * Cₜⁱ(u)。
6. 计算空间比率 ρⁱ_θ,ₜ(u)，并优化 clipped spatial objective 与 KL 正则。
7. 使用 LoRA 更新模型参数。

## 3. 实验设计：数据集/场景、benchmark、对比方法

- **基础模型**：Stable Diffusion 3.5 Medium (SD3.5-M)
- **训练参数**：LoRA rank=32, alpha=64；ODE-to-SDE 在线采样，采样步数 T=10，SDE噪声 a=0.7，分辨率 512×512；每组 G=24 张图；KL 系数 β 依任务不同（GenEval/OCR: 0.04, PickScore: 0.01）；注意力层选择 blocks 10-15；分配强度 λ=1.5。
- **评估任务**（三个 benchmark）：
  - **GenEval**：组合生成能力（对象计数、颜色、属性绑定、空间关系）。官方规则管线计算奖励。
  - **OCR 文本渲染**：生成图像中渲染指定文本，基于编辑距离计算准确率（OCR Acc.）。
  - **PickScore**：使用学习到的偏好奖励模型对 prompt-image 对打分。
- **对比方法**：
  - 基础扩散模型：LDM, SD1.5, SD2.1, SD-XL, DALL·E 2/3
  - 自回归模型：Show-o, Emu3-Gen, JanusFlow, Janus-Pro-7B
  - 近期扩散/流模型：FLUX.1 Dev, SD3.5-L, SANA-1.5, SD3.5-M, SD3.5-M+Flow-GRPO
  - 主要基线：SD3.5-M+Flow-GRPO（无 STAR）

## 4. 资源与算力

- **文中未明确说明**使用的 GPU 型号、数量及训练总时长。
- 仅提供了**计算开销对比**：STAR 相比 Flow-GRPO 增加不到 2% 的训练时间，峰值内存增加 1.56%，表明注意力分配操作非常轻量。
- 训练使用 LoRA，显著减少了可训练参数量。

## 5. 实验数量与充分性

- **主要实验**：三大任务（GenEval, OCR, PickScore）完整对比，报告了 GenEval 的 6 个子指标（单物、双物、计数、颜色、位置、属性绑定），结果全面。
- **消融实验**：
  1. **Prompt 热力图构建方式**：单热力图 vs. 逐组件热力图，对比在 PickScore 上的训练曲线（单热力图略优且实现简单）。
  2. **注意力层选择**：前层 (0-9)、STAR 层 (10-15)、后层 (16-23)、全部层，在 GenEval 上对比性能（STAR 层与全部层接近，且效率更佳）。
  3. **计算开销**：训练时间和峰值内存增加比例。
- **定性比较**：展示了 SD3.5-M 与 SD3.5-M+Flow-GRPO+STAR 的生成图像对比，说明后训练改善了主体和场景一致性。
- **充分性评价**：实验设计较为充分，覆盖了组合语义（GenEval）、精确文本渲染（OCR）和偏好优化（PickScore），并进行了关键设计选择的消融。对比方法包括多种主流模型，确保了客观性。但仅在一个基础模型（SD3.5-M）上验证，泛化性有待更多模型验证。

## 6. 主要结论与发现

- **STAR 在不改变外部奖励源的情况下，一致提升了所有三个任务的指标**：GenEval 0.9759（Flow-GRPO 0.95）、OCR 0.9757（Flow-GRPO 0.92）、PickScore 23.60（Flow-GRPO 23.31）。
- 提升主要来源于**更细粒度的组合语义约束**，尤其在计数（0.98）、颜色（0.96）、属性绑定（0.94）等子任务上提升明显。
- **几乎无额外计算开销**：训练时间增加 <2%，峰值内存增加 1.56%，因为直接复用模型内部的注意力，无需额外奖励模型或评估步骤。
- 消融表明：使用单热力图聚合多个文本单元、选择中间注意力层（blocks 10-15）是效果与效率的最佳平衡。

## 7. 优点

- **轻量级**：无需训练额外模块，仅利用现有注意力，实现简单。
- **通用性**：适用于任何具有文本-图像注意力的扩散/流模型，且可配合多种外部奖励（规则型、偏好模型型）。
- **可解释性**：通过热力图可视化哪些区域被赋予更大权重，有助于诊断模型行为。
- **有效性**：在三个不同类型任务上均获得增益，表明时空分配是一种稳健的改进。
- **公平对比**：所有对比均基于相同的 Flow-GRPO 训练管线，仅改变奖励分配方式，隔离了变量。

## 8. 不足与局限

- **模型泛化不足**：仅在 SD3.5-M 上验证，未测试在其他架构（如 FLUX、SDXL、自回归模型）上的表现。
- **依赖注意力可访问性**：对于不使用显式交叉注意力或 joint-attention 的模型（如某些效率型架构），方法可能不直接适用。
- **文本单元提取的手工成分**：GenEval 使用固定规则，PickScore/OCR 可选 LLM 辅助，规则的唯一性或 LLM 偏差可能影响其他 prompt 类型的泛化。
- **训练超参数探索有限**：分配强度 λ=1.5、注意力层选择等未进行系统扫描（仅对比了几种固定设置）。
- **缺少对多 prompt 或连续提示的评估**：实验在预定义的 prompt 集上进行，对开放域生成的泛化能力未验证。
- **离线采样与在线更新的交互**：未讨论 rollout 与策略更新之间的相关性偏差。

（完）
