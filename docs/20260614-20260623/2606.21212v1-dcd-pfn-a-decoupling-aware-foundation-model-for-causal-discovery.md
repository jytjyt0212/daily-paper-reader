---
title: "DCD-PFN: A Decoupling-Aware Foundation Model for Causal Discovery"
title_zh: DCD-PFN：一种面向因果发现的解耦感知基础模型
authors: "Zhengkang Guan, Yikang Chen, Yi He, Yunze Tong, Zijing Hu, Haoyuan Qian, Fei Wu, Kun Kuang"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21212v1"
tags: ["query:causal-rep"]
score: 9.0
evidence: 通过解耦感知PFN进行因果发现，与因果表征学习相关
tldr: 因果发现是理解复杂数据生成机制的关键，但传统算法在处理高非线性噪声系统或面临计算瓶颈。本文提出DCD-PFN，一个基于解耦感知的因果发现基础模型，通过先验数据拟合网络预训练于多样化的合成结构因果模型，学习样本级解耦权重实现马尔可夫边界识别，并利用并行化局部发现高效重建全局因果图。实验表明，该模型在零样本场景下展现出鲁棒的泛化性能，为因果发现提供了一种新的高效范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21212v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1424, \"height\": 511, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21212v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1235, \"height\": 1372, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21212v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 948, \"height\": 492, \"label\": \"Table\"}]"
motivation: 传统因果发现算法在高非线性噪声系统中表现不佳或计算开销大，且现有PFN模型未充分探索结构因果发现的零样本能力。
method: 提出DCD-PFN，通过预训练于合成SCM学习解耦权重，实现马尔可夫边界识别，并并行化局部发现重建全局因果图。
result: 在零样本泛化实验中，DCD-PFN展现出鲁棒的因果图重建性能，优于传统方法。
conclusion: DCD-PFN验证了解耦范式在因果发现基础模型中的有效性，为高效因果推断提供了新途径。
---

## 摘要
因果发现对于理解复杂数据生成机制至关重要，但传统算法在处理高度非线性和噪声系统时往往表现不佳，或者面临严重的计算瓶颈。最近基于先验数据拟合网络（PFN）的表格基础模型展现了卓越的零样本推理能力，但其在显式结构因果发现中的潜力尚未得到充分探索。为弥合这一差距，我们提出了DCD-PFN，一种面向因果发现的解耦感知基础模型。DCD-PFN不直接摊销全局图重建，而是通过基于解耦的范式聚焦于局部因果发现。通过在多样化的合成结构因果模型（SCM）上进行预训练，模型学习到样本级解耦权重，从而实现马尔可夫边界（MB）识别。此外，通过利用并行化局部发现，DCD-PFN在保持基于解耦的因果发现理论基础的同时，高效重建全局因果图。实验表明，我们的基础模型实现了稳健的零样本泛化能力。

## Abstract
Causal discovery is critical for understanding complex data-generating mechanisms, yet traditional algorithms often struggle with highly non-linear and noisy systems, or suffer from severe computational bottlenecks. Recent tabular foundation models based on Prior-Data Fitted Networks (PFNs) have demonstrated remarkable zero-shot inference capabilities, but their potential for explicit structural causal discovery remains underexplored. To bridge this gap, we propose DCD-PFN, a decoupling-aware foundation model for causal discovery. Instead of directly amortizing global graph reconstruction, DCD-PFN focuses on local causal discovery through a decoupling-based paradigm. Through pre-training on diverse synthetic Structural Causal Models (SCMs), the model learns sample-wise decoupling weights that enable Markov boundary (MB) identification. Furthermore, by leveraging parallelized local discovery, DCD-PFN efficiently reconstructs global causal graphs while remaining grounded in the theoretical foundations of decoupling-based causal discovery. Experiments demonstrate that our foundation model achieves robust zero-shot generalization.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义
- **研究动机**：传统因果发现算法（如基于约束、分数、函数的方法）在高非线性、强噪声的真实数据中性能退化严重，或面临高昂计算开销。现有PFN基础模型（如TabPFN）虽在表格预测任务中表现出色，但尚未被充分用于显式结构因果发现，尤其是缺乏零样本的全局因果图重建能力。
- **整体含义**：本文旨在将PFN与解耦式因果发现理论结合，构建一个既能高效识别局部马尔可夫边界（MB），又能通过局部-全局范式重建完整因果图的基础模型，实现零样本、开箱即用的因果结构学习。

### 2. 方法论
- **核心思想**：基于“解耦感知”范式，不直接学习全局图，而是先通过预训练学习样本级解耦权重，实现马尔可夫边界（MB）识别；再利用并行化局部发现算法，从所有变量的MB出发重建全局因果图（CPDAG）。
- **关键技术细节**：
  - **模型架构**：基于Dual-Axis Attention Block（DAB）的编码器，输入数据集 $D \in \mathbb{R}^{n \times d}$，经投影和特征ID嵌入后，由$L_0$层DAB编码得到全局表示$H$。
  - **特征解耦分支**：对非目标变量表示$H_{-t}$，经$L_1$层DAB和交叉注意力聚合器，得到每个样本的权重$w_i$。解耦损失$\mathcal{L}_w$采用加权HSIC（Centered Kernel Alignment），鼓励权重使变量之间独立。
  - **MB学习分支**：对完整表示$H$，经$L_2$层DAB后，利用解耦权重计算目标变量与其他变量间的加权相似度（式5），再经对数变换和MLP+Sigmoid输出每个变量属于MB的概率分数$s$。MB损失$\mathcal{L}_{MB}$为二元交叉熵。
  - **联合损失**：$\mathcal{L} = \mathcal{L}_w + \mathcal{L}_{MB}$，端到端优化。
  - **全局图重建算法（Algorithm 1）**：并行对每个变量获得初始MB；通过子集边际化检测“配偶节点”dropout来识别V-structure和骨架；最后应用Meek规则定向，输出CPDAG。
- **预训练数据生成**：采用Erdős-Rényi图采样DAG，结构方程包含随机神经网络、决策树、离散化等多元函数，噪声分布多样（高斯、拉普拉斯、均匀、t分布），方差随机。

### 3. 实验设计
- **数据集/场景**：
  - **合成数据集**：连续SEM，DAG为Barabási-Albert模型生成的Scale-Free网络，节点数$d \in \{10,15,20\}$，样本数$n=800$。测试时施加三重分布漂移：拓扑（ER→SF）、噪声强度（低→中高）、机制（隐式神经网络→显式高非线性函数）。独立重复25次。
  - **真实数据集**：Sachs流式细胞术数据集（$d=11$，免疫蛋白信号），采用领域专家验证的共识图作为ground truth。
- **基准对比方法**：
  - 约束法：PC（KCIT及集成变体E-KCIT）
  - 分数法：GES
  - 函数法：ICALiNGAM、DirectLiNGAM
  - 连续优化法：NOTEARS、GraNDAG、GOLEM
  - 其他摊销法：AVICI
- **评价指标**：将输出统一转为CPDAG后，计算SHD c（CPDAG下的结构汉明距离）、Precision、Recall、F1。

### 4. 资源与算力
- **论文未明确说明**：文中未提及具体GPU型号、数量、训练时长等重要算力信息。仅知道模型基于PyTorch实现，预训练需要生成大规模合成数据并端到端优化，但具体规模未披露。

### 5. 实验数量与充分性
- **实验组数**：
  - 合成数据：3种维度（10/15/20）各25次独立重复，共75次测试。每个设置报告均值与标准差。
  - 真实数据：Sachs单次评估（已足够标准）。
  - **未包含消融实验**（如去掉解耦分支、改变损失权重、不同预训练分布等），也未对比不同解耦方式或局部重建策略。
- **充分性评价**：
  - **优点**：对比方法全面（涵盖主流范式），合成测试设定零样本挑战（分布漂移），统计独立性较好。
  - **不足**：缺少消融研究，无法验证各组件贡献；合成图仅限Scale-Free且维度≤20，未扩展到更大或更密集的图；未测试含可观测混杂或干预设置；Sachs数据集规模小，单次结果可能存在偶然性。

### 6. 主要结论与发现
- DCD-PFN在合成数据和真实数据上均取得最优或接近最优的F1和最低SHD c，零样本泛化能力显著。
- 相比同为基础模型的AVICI，DCD-PFN的F1在三个维度上平均高出约90%，证明局部解耦范式优于直接全局重建。
- 在Sachs数据集上，DCD-PFN获得SHD c=14、Precision=1.0，优于所有对比方法，包括传统高精度方法（如DirectLiNGAM、GOLEM）。
- 模型在Precision保持极高（合成数据0.83–0.95）的同时，Recall提升至约0.30，显著优于其他方法（Recall多低于0.20），实现了更好的精度-召回平衡。

### 7. 优点
- **理论驱动**：基于解耦式因果发现的理论框架（文献[9]），模型具有可解释性和可识别性保证。
- **零样本泛化**：预训练后无需微调即可直接处理未见分布数据，实用性极强。
- **高效性**：局部发现可并行执行，全局重建通过Algorithm 1线性加速，推理仅需一次前向。
- **架构设计精巧**：通过DAB同时建模特征间和样本间依赖，解耦权重与MB学习联合优化，避免了传统两步法误差累积。

### 8. 不足与局限
- **实验覆盖不足**：仅测试小规模（≤20变量）和特定拓扑（Scale-Free），未验证在大图、高密度图或混合图场景下的表现。
- **缺少消融研究**：未分析解耦损失、权重机制、不同编码器深度等因素的影响，模型设计归因不清晰。
- **计算与训练成本未报告**：未给出预训练数据量、模型参数量、训练时间，不利于他人复现或评估资源需求。
- **真实数据验证单薄**：仅使用Sachs数据集（11变量），且未报告多次重复或交叉验证的统计量，可能存在过拟合争议。
- **噪声和机制外推边界不明**：虽然设计了三重漂移，但未分析漂移幅度对性能的定量影响，泛化脆弱的临界点未知。
- **应用限制**：假设数据由SCM生成（因果充分性、无隐变量等），违背假设时性能未知；模型输出为CPDAG，无法直接消除马尔可夫等价类歧义。

（完）
