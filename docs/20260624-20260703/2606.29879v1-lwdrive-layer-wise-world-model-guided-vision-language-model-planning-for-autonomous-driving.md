---
title: "LWDrive: Layer-Wise World-Model-Guided Vision-Language Model Planning for Autonomous Driving"
title_zh: LWDrive：分层世界模型引导的视觉-语言模型自主驾驶规划
authors: "Chen Yang, Yuhao Wei, Ze Xu, Ziheng Zou, Shuang Liang, Delin Ouyang, Lingfeng Qi, Jie Li, Guofa Li"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.29879v1"
tags: ["query:self-driving"]
score: 9.0
evidence: 层式世界模型引导的VLM规划用于端到端自动驾驶
tldr: 现有视觉语言模型直接生成轨迹仅编码粗略驾驶意图，缺乏几何精度和未来感知。为解决此问题，提出LWDrive框架，以VLM粗轨迹为意图，通过层叠世界模型引导扩展候选空间，并利用未来帧监督和多层特征逐步细化轨迹。在NAVSIM和NAVSIM-v2上分别取得92.0和89.6高分。该方法实现了从粗到精的轨迹规划，结合了大模型语义与多视图空间信息，提升了自动驾驶规划的准确性与前瞻性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29879v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 882, \"height\": 767, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29879v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1773, \"height\": 871, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29879v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 850, \"height\": 1185, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29879v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1682, \"height\": 549, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29879v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1847, \"height\": 877, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29879v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1849, \"height\": 553, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29879v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 731, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29879v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 885, \"height\": 307, \"label\": \"Table\"}]"
motivation: 现有VLM直接规划轨迹几何精度不足，缺乏未来感知和多视图融合能力。
method: 提出LWDrive，用VLM生成粗意图，扩展候选空间，经Foresight Cascade Planner逐步细化，融入未来帧监督和多层VLM特征。
result: 在NAVSIM上得分92.0，NAVSIM-v2上89.6，验证了粗到细规划的有效性。
conclusion: LWDrive结合VLM语义与层叠世界模型，实现了精确、前瞻的自动驾驶规划。
---

## 摘要
视觉-语言模型（VLM）为端到端自主驾驶（E2E-AD）规划提供了强大的语义理解和常识推理能力。然而，VLM直接生成的轨迹通常仅编码粗略的驾驶意图，不足以实现几何精确、未来感知和多视角感知的规划。为解决这些局限，我们提出了分层世界模型引导驾驶框架（LWDrive）。LWDrive是一个VLM规划框架，通过分层世界模型引导来优化粗糙轨迹。LWDrive不将VLM输出视为最终轨迹，而是将其作为意图感知的粗略规划，在其周围扩展一个多样化的候选空间，并通过预见级联规划器（FCP）逐步优化候选轨迹。具体而言，我们引入未来帧生成监督，鼓励VLM学习前瞻性的场景表示，从而将规划相关的预测动态注入其内部隐状态。基于这些世界模型监督的表示，FCP利用多层的VLM特征，并结合历史时间状态、动作查询表示和当前帧多视角鸟瞰图（BEV）特征，以从粗到细的方式优化候选轨迹。这种设计能够逐步修正空间位置和运动趋势，同时以多视角场景线索为基础进行轨迹优化，并保留大模型产生的高层驾驶意图。最后，一个评分头评估优化后的候选轨迹，并选择最佳轨迹作为最终规划输出。实验表明，LWDrive在NAVSIM基准上达到92.0分，在NAVSIM-v2上达到89.6分。代码和模型将公开发布。

## Abstract
Vision-Language Models (VLMs) provide powerful semantic understanding and commonsense reasoning for End-to-End Autonomous Driving (E2E-AD) planning. However, trajectories directly generated by VLMs often encode only coarse driving intentions and remain insufficient for geometrically accurate, future-aware, and multi-view-grounded planning. To address these limitations, we develop the Layer-Wise World-Model-Guided Driving framework (LWDrive). LWDrive is a VLM planning framework that refines coarse trajectories through layer-wise world-model guidance. Instead of treating the VLM output as the final trajectory, LWDrive uses it as an intent-aware coarse plan, expands a diverse candidate space around it, and progressively refines the candidates through a Foresight Cascade Planner (FCP). Specifically, we introduce future-frame generation supervision to encourage the VLM to learn forward-looking scene representations, thereby injecting planning-relevant predictive dynamics into its internal hidden states. Built upon these world-model-supervised representations, FCP exploits VLM features across multiple layers and integrates historical temporal states, Action-Query representations, and current-frame multi-view Bird's-Eye-View (BEV) features to refine candidate trajectories in a coarse-to-fine manner. This design enables progressive correction of spatial positions and motion trends while grounding trajectory refinement with multi-view scene cues and preserving the high-level driving intention produced by the large model. Finally, a score head evaluates the refined candidates and selects the best trajectory as the final planning output. Experiments show that LWDrive achieves a score of 92.0 on the NAVSIM benchmark and 89.6 on NAVSIM-v2. Code and models will be made publicly available.

---

## 论文详细总结（自动生成）

# 论文总结：LWDrive：分层世界模型引导的视觉-语言模型自主驾驶规划

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：现有的VLM（视觉-语言模型）在端到端自动驾驶规划中主要被用作直接生成轨迹或单次特征融合。直接生成的轨迹仅编码了粗略的驾驶意图，缺乏精细的几何精度、时间一致性和多视角场景约束感知；而单次特征融合方式则未能充分利用VLM丰富的分层语义信息进行后续校正。
- **背景**：自动驾驶需要结合语义理解与细粒度的空间运动规划。VLM具有强大的常识推理能力，但直接解码难以满足可靠闭环规划的需求。同时，世界模型通过预测未来场景状态，能为规划提供前瞻性监督信号。如何将VLM的语义先验与世界模型的预测能力有机结合，实现从粗到精的可执行轨迹规划，是当前研究的核心挑战。
- **整体含义**：论文提出LWDrive框架，重新定义VLM在规划中的角色——不再是直接输出最终轨迹，而是提供意图感知的粗轨迹及预测性内部表示，再通过分层世界模型引导的级联细化过程，逐步校正为几何精确、场景适应的执行轨迹。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- **两阶段粗到细规划**：第一阶段使用VLM生成意图感知的粗轨迹，并利用未来帧生成作为世界模型监督，使VLM隐藏状态学习前瞻性场景动态；第二阶段通过**Foresight Cascade Planner (FCP)**将粗轨迹扩展为候选池，并逐步注入分层VLM预测特征与多视图BEV约束进行细化，最终经评分头选择最佳轨迹。

### 关键技术细节
1. **世界模型监督的粗规划**：
   - 使用Qwen2.5-VL-3B作为主干，输入当前前视图、自车状态、导航指令和可学习动作查询。
   - 世界模型头预测未来帧的潜变量：使用冻结VAE编码未来图像为目标潜变量，通过去噪条件（由最终层视觉隐藏状态和动作查询隐藏状态拼接）预测干净潜变量，损失函数为 `L_wm = d(ẑ_{t+Δ}, z_{t+Δ})`，仅在训练时使用未来图像。
   - 同时训练轨迹解码器生成粗轨迹，损失为 `L_traj = SmoothL1(Ŷ_0, Y*)`。两损失加权组合得到 `L_stage1 = λ_wm L_wm + λ_traj L_traj`。

2. **Foresight Cascade Planner (FCP)**：
   - **候选初始化**：从动作查询潜变量、自车状态编码和可学习提议嵌入初始化候选池（含N_p条轨迹）。
   - **分层级联细化**：Qwen共有L个动作层，每M层进行一次细化（共R = L/M个阶段），选择中间层特征 `(H_V^{lr}, H_A^{lr})` 作为层间预测特征。这利用了不同层捕获不同语义深度（早期层保留局部运动线索，后期层编码高层意图）。
   - **桥接注意力块 (Bridge Attention)**：联合注意力机制，融合候选池自身交互、动作查询/自车状态记忆、以及VLM分层预测特征，使每个候选与多条源信息交换。
   - **BEV轨迹细化块**：桥接注意力增强后的提议查询，进一步关注当前帧多视图BEV特征（通过ResNet-34编码），获取车道、可行驶区域、障碍物等几何约束，然后预测残差更新：`ΔY_r^i = MLP_ref(Q_r^i), Y_r^i = Y_{r-1}^i + ΔY_r^i`。残差设计防止单步覆盖粗意图。
   - 经过R个阶段后输出细化候选池。

3. **评分与训练**：
   - 评分头对每个候选的最终提议特征进行池化，MLP输出标量分数 `S_i = MLP_score(Pool(Q_R^i))`，推理时选择最高分候选。
   - 评分头监督信号采用非反应性日志仿真计算的NAVSIM风格分数（含碰撞、可行驶区域、进展、TTC、舒适度等），损失为BCE。
   - 两阶段训练策略：
     - **Stage1**：仅训练Qwen-VL粗规划分支（世界模型头和轨迹解码器），冻结FCP和评分头。优化VLM的预测性表示。
     - **Stage2**：冻结VLM主干和动作查询嵌入，训练轨迹提议模块、FCP和评分头。轨迹细化损失使用最小化N的SmoothL1，并加入折扣因子β渐进放松早期阶段监督。

## 3. 实验设计

- **数据集与基准**：
  - **NAVSIM**：闭环规划基准，报告PDMS（综合分数：NC、DAC、TTC、C、EP）。
  - **NAVSIM-v2**：扩展闭环基准，报告EPDMS（额外考虑规则和车道行为）。
  - **nuScenes ST-P3**：开环规划基准，报告平均L2位移误差和碰撞率。
- **对比方法**：
  - **NAVSIM**：对比了E2E方法（UniAD、TransFuser、DiffusionDrive、Hydra-MDP++、iPad等）、世界模型方法（DriveWorld-VLA、WoTE、Epona等）、VLA方法（DriveVLM、AutoVLA、SGDrive、ReCogDrive等）。
  - **NAVSIM-v2**：对比TransFuser、Hydra-MDP++、PRIX、DiffusionDrive、DriveVLA-W0、DriveWorld-VLA等。
  - **nuScenes**：对比非自回归（ST-P3、VAD、UniAD等）和自回归（DriveVLM、GPT-Driver、OccWorld等）方法。

## 4. 资源与算力

- 论文明确提及训练配置：
  - Stage1：训练8,000步，学习率2×10⁻⁴，全局批次大小48，约需100小时。
  - Stage2：训练12个epoch，学习率3×10⁻⁵，全局批次大小64，约需80小时。
  - 优化器：AdamW。
  - **未明确说明GPU型号和数量**。仅提及“global batch size”，未提使用多少张卡。推测可能基于A100/RTX3090等常见硬件，但无法确认。

## 5. 实验数量与充分性

- **主要实验（结果表）**：在3个基准上报告结果，其中NAVSIM（Table1）对比了约20种方法，NAVSIM-v2（Table2）对比约12种方法，nuScenes（Table3）对比约16种方法，覆盖面较广。
- **消融实验（Table4）**：在NAVSIM上对核心模块（世界模型监督WM、分层特征LW、桥接注意力BA、BEV细化BR）进行6组消融，验证了每个组件的必要性；同时对比了只用最终层特征（Final）与分层设计的差异。
- **定性可视化**：图4展示了预测未来帧和规划结果。
- **充分性与客观性**：
  - 优点：在多个主流基准上对比多种类型方法，结果清晰体现LWDrive领先。
  - 不足：消融实验仅基于NAVSIM单一基准，未在NAVSIM-v2或nuScenes上重复；未对不同候选池规模、不同层选择间隔M进行敏感性分析；未对世界模型监督的图像时间间隔Δ进行调优实验；未与同类粗到细方法（如DiffRefiner）进行直接消融对比（只对比了最终结果）。总体充分但可进一步深入。

## 6. 主要结论与发现

- LWDrive在NAVSIM上达到PDMS 92.0，在NAVSIM-v2上达到EPDMS 89.6，均优于所有对比方法，包括最近的世界模型方法DriveWorld-VLA（91.3/86.8）和VLA方法SGDrive（91.1/-）。
- 在nuScenes开环规划中L2误差0.37m，碰撞率0.16%，达到最低误差，表明模型具有良好泛化能力。
- 消融实验证明：世界模型监督、分层层级特征、桥接注意力和BEV细化均对性能有显著贡献；仅使用最终层特征（而非分层）会导致PDMS从92.0降至91.8，说明不同层特征的有用信息互补。

## 7. 优点

- **方法设计创新**：
  - 将VLM从“直接轨迹生成”转变为“粗意图提供 + 分层预测特征引导”，通过残差细化保留高层语义，同时利用BEV进行几何校正。
  - 世界模型监督仅训练时需要未来帧，推理时无额外开销，且使VLM隐藏状态自然具备前瞻性。
  - 桥接注意力使候选间交互并与多种记忆源融合，提升候选多样性与协作。
- **实验全面性**：覆盖闭环（NAVSIM系列）和开环（nuScenes）两种评价范式，且对比方法涵盖主流各类型。
- **性能突出**：在两个主要闭环基准上均达到SOTA，尤其进展（EP）和安全（TTC）指标表现优异。
- **代码开源计划**：论文承诺公开代码和模型，促进可复现性。

## 8. 不足与局限

- **实验覆盖有限**：消融实验仅基于NAVSIM一个基准，未在NAVSIM-v2或nuScenes上验证消融结论的泛化性；未对超参数（如层选择间隔M、候选数N_p、未来帧间隔Δ）进行敏感性分析。
- **潜在偏差风险**：NAVSIM和NAVSIM-v2基于仿真日志评估，存在仿真与真实驾驶的差异；开环比拼的nuScenes ST-P3基准已较老，且只报告单一指标。
- **推理效率未知**：论文未讨论推理时间、显存消耗或实时性，FCP包含多个阶段和桥接注意力，可能延迟较高，不利于实际实时部署。
- **应用限制**：
  - 依赖Qwen2.5-VL-3B，模型规模较大（~3B参数量），计算成本高。
  - 仅使用前视图作为VLM输入，尽管BEV细化使用四视图，但VLM部分未利用多视图信息，可能损失全局感知。
  - 训练过程分为两阶段，总耗时约180小时，复现成本高。
- **与部分方法对比不够公平**：某些对比方法（如E2E方法）使用不同传感器配置（如3C+L，即3相机+激光雷达），而LWDrive使用4C（4相机），未进行传感器公平对齐。

（完）
