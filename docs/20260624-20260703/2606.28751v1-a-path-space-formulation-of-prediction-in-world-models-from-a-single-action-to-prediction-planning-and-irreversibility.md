---
title: "A Path-Space Formulation of Prediction in World Models: From a Single Action to Prediction, Planning, and Irreversibility"
title_zh: 世界模型中预测的路径空间公式：从单一动作到预测、规划与不可逆性
authors: Gunn Kim
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28751v1"
tags: ["query:world-models"]
score: 10.0
evidence: 世界模型预测与规划的理论公式
tldr: 世界模型的预测本质上定义了未来轨迹的概率测度。本文提出路径空间公式化，将预测、规划和不确定性统一于一个作用泛函。在注意力模型中，注意力不对称性与数据不可逆性呈正比，对称化会抑制熵产生并选择性破坏不可逆动力学预测。这表明不可逆性可作为预测世界模型的计算资源。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28751v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 615, \"height\": 223, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28751v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1831, \"height\": 1174, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28751v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1839, \"height\": 1187, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28751v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1835, \"height\": 1548, \"label\": \"Figure\"}]"
motivation: 现有世界模型将预测视为单步条件分布的序列，缺乏对轨迹整体分布和不可逆性的理论框架。
method: 提出路径空间公式化，将隐动力学分解为可逆与不可逆成分，并定义操作化的熵产生度量。
result: 小规模注意力模型训练中注意力不对称性与数据不可逆性正相关，对称化降低熵产生并破坏长时不可逆预测。
conclusion: 不可逆性是预测世界模型的关键计算资源，路径分布而非状态分布是基本预测对象。
---

## 摘要
我们提出了一种AI世界模型中预测的路径空间公式。我们论证，世界模型隐式地定义了未来轨迹上的概率测度，而不是单步条件分布的序列。在潜在动力学允许有效马尔可夫描述的局域范围内，该路径测度采取Onsager-Machlup形式。在此框架内，预测（最可能轨迹）、规划（约束优化）和不确定性（涨落）作为单个作用量泛函上的操作出现。我们将潜在动力学分解为可逆和不可逆分量，并引入从模型展开中得到的熵产生的操作性度量。在受控的小规模注意力模型中，我们发现注意力不对称性是在训练过程中与数据的不可逆性成比例地获得的。对称化学习到的注意力会抑制熵产生，并选择性地降低不可逆动力学的长时域预测，同时保留弛豫预测。这些结果表明，不可逆性可能为预测性世界模型提供一种计算资源。更一般地，基本预测对象是未来路径上的分布，而非状态上的分布。

## Abstract
We propose a path-space formulation of prediction in AI world models. Rather than sequences of one-step conditional distributions, we argue that a world model implicitly defines a probability measure over future trajectories. In the local regime where latent dynamics admit an effective Markovian description, this path measure takes the Onsager-Machlup form. Within this framework, prediction (most probable trajectory), planning (constrained optimization), and uncertainty (fluctuations) emerge as operations on a single action functional. We decompose the latent dynamics into reversible and irreversible components and introduce operational measures of entropy production from model rollouts. In controlled small-scale attention-based models, we find that attention asymmetry is acquired during training in proportion to the irreversibility of the data. Symmetrizing the learned attention suppresses entropy production and selectively degrades long-horizon prediction of irreversible dynamics while preserving relaxational prediction. These results suggest that irreversibility may serve as a computational resource for predictive world models. More generally, the fundamental predictive object is a distribution over future paths rather than states.

---

## 论文详细总结（自动生成）

# 中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有世界模型通常将预测视为单步条件分布（如 `p(x_{t+1}|x_t)`）的序列，缺乏对**未来轨迹整体分布**的理论框架。三个关键操作——预测、规划和不确定性——被当作独立模块处理，缺少统一的理论基础。
- **整体含义**：本文提出，世界模型的**基本预测对象是未来路径上的概率分布**（而非未来状态分布），从而将预测、规划和不确定性统一在单个路径作用量泛函下。进一步，通过引入非平衡统计力学的视角，论证了**不可逆性（熵产生）** 是预测动态环境的关键计算资源，并追溯到架构中的注意力不对称性（query–key 不对称）。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将世界模型的潜在动力学建模为随机过程，路径概率测度具有 Onsager–Machlup 形式 `P[Γ] ∝ e^{-A[Γ]}`，其中作用量 `A[Γ] = ∫ (1/(4D) |\dot{z} - f(z)|^2 + (1/2) ∇·f) dt`。
- **关键技术细节**：
  - **局部马尔可夫假设**：基于 Mori–Zwanzig 投影，当记忆时间远小于动力学时间时，有效动力学为局部扩散过程。
  - **可逆/不可逆分解**：漂移 `f = -∇U + v`，其中 `v` 为循环漂移（不可逆部分），熵产生率 `\dot{Σ} = (1/D)⟨|v|^2⟩`。
  - **注意力-行动对应**：单层注意力层中，query–key 矩阵 `M = W_Q^T W_K` 扮演度规角色，其反对称部分 `M_A` 直接驱动循环漂移 `v` 和注意力不对称性指标 `Q = ||M_A|| / ||M||`。
  - **漂移雅可比方程**：在 value‑tied 情形下，反对称雅可比 `J_A` 与 `M_A` 通过反交换子关联：`J_A ∝ -{C, M_A}`，因此对称化 `M` 可消除这一贡献。
- **算法流程**（实验测量）：
  1. 从模型 rollout 中估计局部漂移 `\hat{f}(z)` 和扩散 `\hat{D}(z)`（Kramers–Moyal 展开）。
  2. 估计稳态密度 `ρ_ss(z)`，计算概率流 `J(z)` 和循环漂移 `v(z)`。
  3. 计算熵产生率 `\dot{Σ}`、非正态性 `N` 和注意力不对称性 `Q`。
  4. 进行因果干预：对称化 `M → (M+M^T)/2`，观察 `\dot{Σ}` 和预测误差的变化。

## 3. 实验设计

- **使用的数据集/场景**：论文使用**可控的二维潜在动力学过程**，包括：
  - **线性注意力模型**：`f(z) = Wz`，其中 `W = -γI + M^T`，`M = S0 + θA0`（`θ` 控制不对称性）。
  - **softmax 注意力模型**：`f(z) = -γz + β ∑ softmax_j(z^T M m_j) m_j`，记忆槽 `m_j` 固定。
- **Benchmark**：无外部标准数据集；对于线性模型，熵产生率有精确的**Lyapunov 方程解**作为真值（ground truth）。
- **对比方法**：未与其他现有世界模型方法对比，而是内部对比**对称化干预前后**、以及**不同注意力不对称性 θ 的扫描**。此外，还训练了一个小模型，对比**训练过程中自身的学习效果**。

## 4. 资源与算力

- **文中未明确说明**具体使用的 GPU 型号、数量或训练时长。由于实验限于小规模二维模型（线性/softmax），推测算力需求极低，可在单台普通 GPU 或 CPU 上快速完成。

## 5. 实验数量与充分性

- **实验数量**：
  - 图1：线性与 softmax 两种模型下，扫描注意力不对称性 Q 从 0 到约 0.97，测量熵产生率，并进行对称化干预（共 4 个子图）。
  - 图2：在训练过程中监测 Q、熵产生率、非正态性随训练步数的变化；跨不同数据不可逆性 ω 测量；对称化干预并检测预测损失（共 6 个子图）。
  - 图3：在循环数据和弛豫数据上，比较训练模型与对称化模型的多步 rollout 误差，以及熵产生率与峰值误差的关系（共 4 个子图）。
- **充分性**：实验覆盖了线性与非线性（softmax）情形、不同不可逆性水平、训练过程动态、因果干预等多个维度，**较为充分**。但未在大规模真实世界模型或高维隐空间上验证，范围有限。
- **客观与公平**：实验设计有清晰的可控参数和真值对照（线性模型的 Lyapunov 解），且测量基于盲估计（不提供精确公式），**可靠且公平**。干预实验直接作用于权重，因果链清晰。

## 6. 论文的主要结论与发现

1. **路径空间公式化**：世界模型的预测本质上是定义在未来轨迹上的概率测度，Onsager–Machlup 作用量统一了预测、规划和不确定性。
2. **注意力不对称性驱动不可逆性**：在 value‑tied 注意力层中，`M_A` 是熵产生的主要架构源，对称化 `M` 会大幅抑制熵产生（从 7.71 降至 0.18）。
3. **不可逆性是预测资源**：对称化选择性地破坏循环（不可逆）动力学的长时预测，但保留弛豫（可逆）预测；预测误差损失与熵产生率单调正相关。
4. **训练中自发习得**：模型从对称初始化开始训练，会在学习过程中自动获得与数据不可逆性成比例的注意力不对称性、熵产生和非正态性。
5. **根本对象是路径分布**：工作呼吁从“预测下一个状态”转向“给未来路径分配权重”，为世界模型提供统计物理基础。

## 7. 优点

- **理论创新性**：首次将 Onsager–Machlup 作用量、非平衡熵产生与注意力架构系统联系起来，提供了统一语言。
- **因果干预设计**：通过对称化注意力权重直接验证了架构–热力学–预测性能之间的因果关系，而非仅相关性。
- **可操作定义**：每个关键量（漂移、熵产生、非正态性、注意力不对称性）都给出了明确的估算方法，并列出可验证的预测（P1–P5）。
- **小型可控模型验证机理**：在二维系统上即可清晰展示机制，避免大型模型的复杂性混淆根本原理。
- **清晰的局限性声明**：作者坦诚地讨论了模型尺度、value‑tied 假设、网格估计器可扩展性等限制，为后续研究指明方向。

## 8. 不足与局限

- **实验范围狭窄**：仅针对二维潜在动力学，未在真实世界模型（如 Dreamer、RSSM、大型 Transformer）上验证。高维隐空间下网格估计器不可行，且非注意力通道（残差连接、前馈层等）可能贡献不可逆性。
- **value‑tied 假设**：推导中假设值向量与键向量绑定，否则反对称雅可比还包含其他通道，论文未分析一般情况。
- **噪声地板**：熵产生测量在 Q→0 时残余约 0.2，部分来自有限格子偏差和状态依赖的非可积性，未严格分离。
- **规划与不确定性部分仅理论推导**：实验仅验证了预测部分，规划（固定端点最小作用）和不确定性（Hessian 逆）未作测量。
- **缺乏真实数据基准**：未在标准世界模型基准（如 Atari、DMControl、机器人任务）上测试，实际应用意义需要后续工作验证。
- **资源消耗未提及**：无算力成本分析，虽无碍理论价值，但影响实用性评估。

（完）
