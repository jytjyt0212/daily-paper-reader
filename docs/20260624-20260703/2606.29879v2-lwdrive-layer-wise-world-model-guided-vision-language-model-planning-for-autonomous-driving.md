---
title: "LWDrive: Layer-Wise World-Model-Guided Vision-Language Model Planning for Autonomous Driving"
title_zh: LWDrive：面向自动驾驶的逐层世界模型引导的视觉语言模型规划
authors: "Chen Yang, Yuhao Wei, Ze Xu, Ziheng Zou, Shuang Liang, Delin Ouyang, Lingfeng Qi, Jie Li, Guofa Li"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.29879v2"
tags: ["query:self-driving"]
score: 9.0
evidence: 自动驾驶中的世界模型用于轨迹细化
tldr: 针对VLM直接生成轨迹几何精度低、缺乏未来感知和多视图融合的问题，提出LWDrive框架。该框架将VLM输出作为意图感知的粗轨迹，通过Foresight Cascade Planner进行逐层细化。引入未来帧生成监督，使VLM学习前瞻性场景表示，并利用多层级特征、历史状态、Action-Query和多视角BEV特征实现由粗到细的轨迹修正。在NAVSIM上达92.0分，展示了世界模型引导的VLM规划有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29879v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 882, \"height\": 768, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29879v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1773, \"height\": 871, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29879v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 850, \"height\": 1185, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29879v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1682, \"height\": 549, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29879v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1847, \"height\": 877, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29879v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1849, \"height\": 553, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29879v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 731, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29879v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 885, \"height\": 307, \"label\": \"Table\"}]"
motivation: VLM直接生成轨迹仅编码粗意图，精度不足且缺乏未来感知和多视图融合。
method: 以VLM粗轨迹为中心扩展候选空间，通过Foresight Cascade Planner利用世界模型监督的层级特征逐步细化，融合历史、Action-Query和BEV特征。
result: 在NAVSIM上得分92.0，NAVSIM-v2上得分89.6。
conclusion: LWDrive通过逐层世界模型引导，有效提升VLM规划的几何精度和场景感知能力。
---

## 摘要
视觉语言模型（VLM）为端到端自动驾驶（E2E-AD）规划提供了强大的语义理解和常识推理能力。然而，VLM直接生成的轨迹通常仅编码粗略的驾驶意图，不足以实现几何精确、未来感知和多视角融合的规划。为了解决这些局限性，我们开发了逐层世界模型引导的驾驶框架（LWDrive）。LWDrive是一个VLM规划框架，通过逐层世界模型引导来优化粗略轨迹。LWDrive不将VLM输出视为最终轨迹，而是将其用作意图感知的粗略规划，在其周围扩展多样化的候选空间，并通过前瞻级联规划器（FCP）逐步优化候选轨迹。具体而言，我们引入未来帧生成监督，鼓励VLM学习前瞻性场景表示，从而将规划相关的预测动态注入其内部隐藏状态。基于这些世界模型监督的表示，FCP利用VLM跨多层的特征，并结合历史时间状态、动作查询表示和当前帧多视角鸟瞰图（BEV）特征，以从粗到细的方式优化候选轨迹。这种设计能够在逐步修正空间位置和运动趋势的同时，以多视角场景线索为基础进行轨迹细化，并保留大模型产生的高层驾驶意图。最后，一个评分头评估优化后的候选轨迹，并选择最佳轨迹作为最终规划输出。实验表明，LWDrive在NAVSIM基准上得分为92.0，在NAVSIM-v2上得分为89.6。代码和模型将公开提供。

## Abstract
Vision-Language Models (VLMs) provide powerful semantic understanding and commonsense reasoning for End-to-End Autonomous Driving (E2E-AD) planning. However, trajectories directly generated by VLMs often encode only coarse driving intentions and remain insufficient for geometrically accurate, future-aware, and multi-view-grounded planning. To address these limitations, we develop the Layer-Wise World-Model-Guided Driving framework (LWDrive). LWDrive is a VLM planning framework that refines coarse trajectories through layer-wise world-model guidance. Instead of treating the VLM output as the final trajectory, LWDrive uses it as an intent-aware coarse plan, expands a diverse candidate space around it, and progressively refines the candidates through a Foresight Cascade Planner (FCP). Specifically, we introduce future-frame generation supervision to encourage the VLM to learn forward-looking scene representations, thereby injecting planning-relevant predictive dynamics into its internal hidden states. Built upon these world-model-supervised representations, FCP exploits VLM features across multiple layers and integrates historical temporal states, Action-Query representations, and current-frame multi-view Bird's-Eye-View (BEV) features to refine candidate trajectories in a coarse-to-fine manner. This design enables progressive correction of spatial positions and motion trends while grounding trajectory refinement with multi-view scene cues and preserving the high-level driving intention produced by the large model. Finally, a score head evaluates the refined candidates and selects the best trajectory as the final planning output. Experiments show that LWDrive achieves a score of 92.0 on the NAVSIM benchmark and 89.6 on NAVSIM-v2. Code and models will be made publicly available.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机与背景）

- **问题**：现有 Vision-Language Models (VLMs) 具备强大的语义理解与常识推理能力，但在端到端自动驾驶规划中直接解码轨迹存在严重不足：轨迹仅编码粗略驾驶意图，缺乏精准的几何精度、未来场景感知以及多视图融合能力。
- **背景**：现有 VLM 规划方案分为两类：① 直接解码轨迹（粗粒度，缺乏时空约束）；② 单次融合 VLM 特征与驾驶骨干网络（语义注入一次，与后续修正弱耦合）。二者均未充分利用 VLM 的层级化预测表示。
- **核心假设**：VLM 应提供意图感知的粗计划，其内部预测性表示可用于逐步细化候选轨迹，从而实现从粗到精的规划。

## 2. 方法论

### 核心思想
提出 **LWDrive** 框架，将 VLM 输出视为粗轨迹，通过**逐层世界模型引导**的**前瞻级联规划器（FCP）** 逐步细化候选轨迹池，融合多层级 VLM 特征、历史时间状态、Action-Query 表示及多视角 BEV 特征，最终由评分头选择最佳轨迹。

### 关键技术细节

- **世界模型监督的粗规划**：
  - 使用 Qwen2.5-VL-3B 作为骨干，输入当前前视图、自车状态、导航指令和可学习动作查询。
  - 引入**未来帧生成监督**：通过冻结 VAE 编码未来图像，将 VLM 隐藏状态作为去噪条件，预测未来潜在表示。该监督仅在训练时使用，使 VLM 隐藏状态编码预测性动态。
  - 同时训练粗轨迹解码器，用 SmoothL1 损失监督粗轨迹与专家轨迹的一致性。
  - 第一阶段损失：\(\mathcal{L}_{stage1} = \lambda_{wm}\mathcal{L}_{wm} + \lambda_{traj}\mathcal{L}_{traj}\)

- **前瞻级联规划器（FCP）**：
  - **候选池初始化**：从 Action-Query 隐状态、自车状态编码和可学习提议嵌入中生成 Np 个候选轨迹。
  - **层级特征选择**：以间隔 M 层选取 Qwen 动作层（共 L 层），形成 R 个细化阶段，每个阶段利用对应层级的 VLM 视觉/任务隐藏状态、Action-Query 隐藏状态、当前帧 BEV 特征和自车状态。
  - **桥梁注意力（Bridge Attention）**：构造多源记忆（提议自注意力、Action-Query/自车状态、VLM 预测记忆），让每个提议与其它提议及 VLM 信息交互。
  - **BEV 轨迹细化**：提议查询进一步与 BEV 特征交互，预测残差更新 \(\Delta Y\)，逐步修正轨迹位置和运动趋势，保留高意图。
  - 每阶段更新：\(Y^r_i = Y^{r-1}_i + \Delta Y^r_i\)

- **评分与选择**：对细化后的每个候选，用 MLP 从最终提议特征预测标量分数。采用 NAVSIM 风格的 proposel-level 分数（结合碰撞、可行驶区域、进度、TTC、舒适度）作为监督。
- **训练策略**：两阶段。第一阶段冻结 FCP/评分头，仅训练粗规划分支（VLM+世界头+轨迹解码器）。第二阶段冻结 VLM 骨干和 Action-Query 嵌入，训练 FCP 和评分头。细化损失采用逐阶段 Minimum-over-N 方法，前面阶段权重衰减。

## 3. 实验设计

### 数据集与基准
- **NAVSIM**：提供封闭循环规划评估，报告 Predictive Driver Model Score (PDMS)，包含 No-collision (NC)、Drivable Area Compliance (DAC)、Time-to-Collision (TTC)、Comfort (C)、Ego Progress (EP)。
- **NAVSIM-v2**：扩展版，报告 Extended PDMS (EPDMS)，增加 Driving Direction Compliance (DDC)、Traffic Light Compliance (TLC)、Lane Keeping (LK)、Human Comfort (HC)、Extended Comfort (EC) 等。
- **nuScenes**：开放循环规划，使用 ST-P3 基准，报告平均 L2 位移误差和碰撞率。

### 对比方法
- **E2E 方法**：UniAD、LTF/TransFuser、DriveX-S、PRIX、DiffusionDrive、Hydra-MDP++、iPad（NAVSIM）；Ego Status MLP、DriveSuprim、ARTEMIS 等（NAVSIM-v2）。
- **世界模型方法**：DrivingGPT、World4Drive、Epona、WoTE、DriveWorld-VLA。
- **VLA 方法**：FSDrive、AutoVLA、DriveVLA-W0、ReCogDrive、SGDrive。
- **nuScenes 对比**：包括非自回归（ST-P3、VAD、UniAD 等）和自回归方法（DriveVLM、GPT-Driver、OccWorld 等）。

## 4. 资源与算力

- **GPU 型号与数量**：文中未明确说明 GPU 型号及具体数量。
- **训练时长**：
  - 第一阶段（粗规划+世界模型监督）：8,000 步，全局 batch size 48，约 100 小时。
  - 第二阶段（FCP+评分头）：12 个 epoch，全局 batch size 64，约 80 小时。
  - 总计约 180 小时训练时间。
- **模型规模**：VLM 骨干为 Qwen2.5-VL-3B（3B 参数），BEV 编码器使用 ResNet-34。共 36 个 Qwen 动作层，6 个轨迹细化模块。

## 5. 实验数量与充分性

- **主实验**：在三个不同基准（NAVSIM、NAVSIM-v2、nuScenes）上分别给出详细结果，每个基准包含多个子指标（6~10 个），与 10~15 种前沿方法进行对比，充分证明方法优越性。
- **消融实验**：表 4 在 NAVSIM 上对世界模型监督、层级特征、桥梁注意力、BEV 细化进行系统性消融（共 6 组变体），验证各组件贡献。
- **可视化**：图 4 给出定性示例。
- **充分性评估**：实验覆盖主流规划基准，消融设计系统，对比方法全面（包括 E2E、世界模型、VLA 三大类），结果统计显著（LWDrive 在 PDMS 和 EPDMS 上均最高）。实验设计客观公平，采用官方评估协议。不足在于缺乏实际车辆闭环测试或更复杂场景（如长尾）的评估。

## 6. 主要结论与发现

- LWDrive 在 NAVSIM 上达到 **PDMS 92.0**，在 NAVSIM-v2 上达到 **EPDMS 89.6**，均超过所有对比方法，包括最新的 iPad、DriveWorld-VLA、SGDrive 等。
- 在 nuScenes 开放循环基准上取得最低 L2 误差（0.37 m）和低碰撞率（0.16%），证明从粗到细的 VLM 规划具有强泛化能力。
- 世界模型监督使 VLM 隐藏状态具备预测性动态，层级特征与 BEV 细化联合提升几何精度和场景一致性。
- 消融表明：各组件（世界模型监督、层级特征、桥梁注意力、BEV 细化）均显著贡献最终性能，其中 BEV 细化贡献最大（PDMS 从 89.3 提升至 92.0）。

## 7. 优点

- **创新性设计**：将 VLM 规划从单次解码重构为**世界模型引导的渐进式细化**，利用 VLM 层级预测特征，与 BEV 几何约束结合，缓解了直接 VLM 轨迹的精度不足。
- **训练策略巧妙**：两阶段训练先学习预测性表示，再冻结 VLM 进行细化，防止下游模块干扰 VLM 表示，促进模块解耦。
- **实验充分且公平**：覆盖三个权威基准，与 20 余种方法对比，消融验证充分，结果具有统计显著性。
- **可复现性**：承诺开源代码和模型，有利于社区验证和后续改进。
- **性能全面领先**：在安全性（碰撞率、TTC）、规则合规性（可行驶区域、交通灯）和效率（进度）上均表现优异。

## 8. 不足与局限

- **计算开销大**：两阶段训练总时长约 180 小时，且依赖 Qwen2.5-VL-3B 大模型，推理时仍需运行 VLM 和 FCP，实际车载部署可能面临延迟和算力挑战。
- **依赖单一 VLM 骨干**：实验仅基于 Qwen2.5-VL 系列，未验证在其他 VLM（如 LLaVA、DriveVLM 等）上的可移植性，存在过拟合到特定语言模型的风险。
- **场景覆盖有限**：评估仅在 NAVSIM（模拟）和 nuScenes（开放循环）上，未包含真实世界封闭循环测试或更复杂场景（如恶劣天气、行人密集区、未标记道路等）。未来帧生成监督基于单帧预测，时序动态建模较浅。
- **评分头设计**：评分头使用预定义的 NAVSIM-style 分数作为监督，可能无法完全反映真实驾驶中的多目标权衡（如舒适度 vs. 效率），存在优化偏差。
- **开放循环局限**：nuScenes 评估为开放循环（仅预测一步），不能充分反映闭环累计误差，LWDrive 在闭环场景中的鲁棒性有待进一步验证。

（完）
