---
title: "Introduction to Stochastic Differential Equations for Generative Machine Learning: A Variational Perspective"
title_zh: 生成式机器学习的随机微分方程导论：变分视角
authors: "Ole Winther, Paul Jeha, Sander Dieleman, Andriy Mnih, Manfred Opper, Andrea Dittadi"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31576v1"
tags: ["query:gen-models"]
score: 9.0
evidence: 生成机器学习中SDE的介绍，涵盖扩散和流匹配
tldr: 生成机器学习中，常微分与随机微分方程促进了图像、视频等数据生成。本文从变分视角自包含介绍随机微分方程及其在生成建模中的应用，推导Fokker-Planck方程和ELBO，将扩散模型、分数匹配和流匹配统一为变分框架的不同参数化，并通过一维密度示例比较各方法。该视角为理解生成模型提供了统一理论基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31576v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1636, \"height\": 842, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31576v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1687, \"height\": 1566, \"label\": \"Figure\"}]"
motivation: 统一解释扩散模型、分数匹配和流匹配等生成方法，为生成机器学习提供严格的变分理论基础。
method: 从变分下界ELBO出发，推导Fokker-Planck方程，并将扩散模型、分数匹配、流匹配视为该框架下的不同参数化。
result: 在一维密度建模示例中，比较了不同参数化在拟合效果上的差异，展示了各种方法的等价性。
conclusion: 随机微分方程结合变分视角为生成模型提供了统一框架，有助于理解不同方法的本质联系。
---

## 摘要
常微分和随机微分方程的使用已经推动了生成式机器学习的显著进展，例如在图像、视频和生物分子生成中的应用。本文提供了对微分方程、将其用于生成建模的概率框架以及控制微分方程随机变量边缘分布时间演化的Fokker-Planck方程的自包含且非正式的介绍。推导了对数似然的变分下界（证据下界，ELBO），并将其作为讨论扩散模型、分数匹配和流匹配的一般起点。所有这些方法都可以被视为最一般的变分方法的特定参数化。本文使用一维密度建模问题作为简单示例来比较不同的参数化。

## Abstract
The use of ordinary and stochastic differential equations has led to substantial progress in generative machine learning with applications to, for example, image, video and biomolecule generation. This paper provides a self-contained and informal introduction to the differential equations, the probabilistic framework for using them in generative modeling and the Fokker--Planck equation that governs the temporal evolution of the marginal distribution of the stochastic variables of the differential equations. The variational lower bound on the log-likelihood (the evidence lower bound, ELBO) is derived and used as a general starting point for a discussion of diffusion models, score matching, and flow matching. All of these approaches may be viewed as specific parameterizations of the most general variational approach. A one-dimensional density modeling problem is used as a simple example to compare different parameterizations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：近年来，扩散模型、分数匹配、流匹配等基于微分方程的生成模型取得了巨大成功，但这些方法分别从不同角度提出，缺乏统一的概率和变分理论解释。这给初学者理解和对比它们带来了困难。此外，现有教程要么过于浅显（缺少概率基础），要么过于数学化（需要 Itô 积分、Girsanov 定理等背景）。
- **整体含义**：本文旨在提供一个**自包含的、非正式的导论**，从变分视角出发，系统介绍常微分方程（ODE）和随机微分方程（SDE）在生成建模中的应用，并证明扩散模型、分数匹配、流匹配均可视为同一变分框架下的不同参数化。该工作为后续研究提供了一个统一的理论基础，降低了入门门槛。

### 2. 论文提出的方法论
- **核心思想**：将生成模型视为一个**潜在过程**（由 ODE/SDE 定义），辅以简单先验和似然函数，并通过**变分下界（ELBO）** 进行最大似然训练。
- **技术细节**：
    - **Fokker–Planck 方程**：从 Kramers–Moyal 展开出发，推导出 ODE 和 SDE 对应的 FPE，并利用其建立**“不同动力学（ODE/SDE/反向 SDE）共享相同边际分布”**的关系。
    - **ELBO 推导**：
        - **ODE 情形**：通过变分公式（22）和使用 ODE 的对数密度变化公式（24），得到可求解的 ELBO（25），但需要求解 ODE（模拟训练）。
        - **SDE 情形**：由于边缘分布不可解析，进一步利用路径 KL 散度（E 节）或 KL 导数分解（D 节），将 KL 项上界为时间平均的均方误差项 D(f,g,y)，得到模拟自由的 ELBO（36）。
    - **参数化策略**：
        - 变分边际可通过**标准化流**或**混合流**定义，推导对应的 ODE 速率和 SDE 漂移（Section 6）。
        - 扩散模型（VDM）采用高斯边际，分数匹配和流匹配分别将生成漂移参数化为**评分函数 s(x,t)** 和**流函数 v(x,t)**，从而导出相应损失（53、56、59）。
    - **算法伪代码**：提供了 ODE 和 SDE 的 ELBO 计算及采样算法（Algorithm 1 & 2）。
- **统一性**：所有方法源自同一个 ELBO 框架，只是对生成漂移和变分分布的具体参数化不同。

### 3. 实验设计
- **任务与数据集**：一维密度建模。数据分布为 k=5 个 Laplace 分布的混合（位置等距，尺度为 1）。训练集 n=1000 点，验证集 nval=8000 点。参考对数似然由真实分布计算得到 -2.9862。
- **对比方法**：
    1.  **ODE**：含神经网络漂移、Dirac 似然、高斯先验，使用 ODE 模拟 ELBO 训练。
    2.  **Flow Matching**：标准流匹配目标（59），σ=1，不使用似然项，仅优化扩散损失。
    3.  **SDE（VDM）**：变分扩散模型，使用线性 log-SNR 方案，信号预测网络，Likelihood 与 t=1 变分宽度匹配。
    4.  **SDE（通用参数化）**：自由学习变分分布的 α(y,t) 和 β(y,t)，其余同 SDE。
- **评价指标**：验证集对数似然（对于 ODE 和 SDE 为 ELBO），以及生成密度可视化。
- **Baseline**：真实分布的对数似然 -2.9862。
- **结果**：
    - ODE：验证 -3.025，训练 -2.990。
    - Flow Matching：可视化接近 ODE，但未给出可比的对数似然值。
    - SDE（VDM）：验证 -3.256（平滑后），训练 -3.259。
    - SDE（通用）：验证 -3.052，训练 -2.975。

### 4. 资源与算力
- **未明确说明**：文中没有提及使用的 GPU 型号、数量或训练总时长。仅给出了超参数（epochs、batch size、学习率等），但未涉及硬件配置。

### 5. 实验数量与充分性
- **实验数量**：仅有**一组简单的一维密度建模实验**，比较了四种方法（ODE、流匹配、SDE-VDM、SDE-通用）。没有其他数据集（如图像、视频）或消融实验。
- **充分性与公平性**：
    - 实验旨在**概念验证**而非严格评估方法性能。所有方法在相似架构和超参数下训练，但参数配置（如学习率、训练轮次）差异较大（ODE 200 epochs，流匹配 10000 epochs，SDE 2000 epochs），直接比较不够公平。
    - 未进行统计显著性检验，未报告多次运行的平均值和标准差。
    - 流匹配无法输出可比较的对数似然值，削弱了对比的全面性。
    - **结论**：实验覆盖范围小、深度有限，作为导论尚可，但不足以支撑方法间的优劣判断。不充分。

### 6. 论文的主要结论与发现
- 三种流行方法（扩散、分数匹配、流匹配）可以在统一的变分框架下得到解释，均可看作 ELBO 的不同参数化。
- 在一维示例中，ODE 和 SDE 均能近似真实分布，通用参数化的 SDE 比固定形式的 VDM 收敛更快、ELBO 更高。
- 流匹配目标虽然简单（仅优化扩散损失），但也能学习到合理的密度，只是无法直接与似然比较。
- 统一的变分视角有助于揭示方法间的联系，并可能启发超出当前范式的新参数化。

### 7. 优点
- **自包含性**：从 ODE/SDE 基本概念开始，推导出 Fokker–Planck 方程和 ELBO，不依赖 Itô 微积分或 Girsanov 定理，适合初学者。
- **统一性**：首次在单一框架下系统阐述扩散模型、分数匹配和流匹配，并给出清晰的关系图（式（36）、（53）、（56）、（59））。
- **实用性**：给出了伪代码（Algorithm 1 & 2），便于实现；讨论了变分分布的设计（高斯流、混合流），为扩展提供指导。
- **理论创新**：推导了 KL 散度在 SDE 下的分解，并揭示了反向时间 SDE 对 ELBO 间隙的贡献。

### 8. 不足与局限
- **实验局限**：仅一维玩具示例，无法代表真实生成任务（如图像、视频）的挑战。所有模型均未在标准基准（如 CIFAR-10、ImageNet）上测试。
- **缺乏实证比较**：未评估生成质量（FID、IS）、采样效率、计算成本等关键指标。流匹配未给出可比较的似然值，无法定量判断其相对于其他方法的优劣。
- **理论基础依赖假设**：推导中假定扩散矩阵可逆、密度正则性等，部分实际应用中可能不成立。
- **未涵盖最新进展**：不涉及一致性模型、潜在扩散模型、最优传输等前沿方法，也未讨论架构设计（如 U-Net）、速度优化等实用技巧。
- **参数化选择的指导不足**：虽然给出了几种可能的变分分布，但未提供如何针对实际数据选择合适分布的建议。
- **偏差风险**：所有作者均来自学术机构或工业实验室，未声明利益冲突；但作为导论，可能存在选择性地展示对变分框架有利的视角。

（完）
