---
title: "ReWorld: Learning Better Representations for World Action Models"
title_zh: ReWorld：为世界动作模型学习更好的表示
authors: "Tianze Xia, Lijun Zhou, Kaixin Xiong, Jingfeng Yao, Yu Zhu, Zhenxin Zhu, Bing Wang, Guang Chen, Hangjun Ye, Wenyu Liu, Haiyang Sun, Xinggang Wang"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27504v1"
tags: ["query:self-driving"]
score: 9.0
evidence: 自动驾驶世界动作模型的表征学习
tldr: "自动驾驶世界动作模型（WAMs）需高效学习世界表示用于规划。现有方法仅监督输出端，中间表示只能间接优化。本文提出ReWorld，首个针对WAM的表示学习框架：对生成模块的中间表示施加未来预测监督，对规划模块的中间表示进行跨模态对齐与安全边界难负样本判别。在nuScenes和NAVSIM上，视频生成FVD从81.3降至61.9（提升23.9%），闭环PDMS从89.1提升至90.4，训练收敛加速约2倍。方法简单有效，可推广至其他世界模型。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27504v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1338, \"height\": 836, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27504v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1393, \"height\": 536, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27504v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1402, \"height\": 386, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27504v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 741, \"height\": 380, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27504v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1243, \"height\": 595, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27504v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1461, \"height\": 666, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27504v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 838, \"height\": 264, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27504v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 547, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27504v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 373, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27504v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 305, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27504v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 890, \"height\": 444, \"label\": \"Table\"}]"
motivation: 现有WAM训练仅监督输出端，中间表示缺乏直接优化，限制了世界表示质量与规划性能。
method: ReWorld从三方面直接优化中间表示：Video DiT引入未来预测监督，Action DiT进行跨模态对齐与安全边界难负样本判别。
result: "在nuScenes和NAVSIM上，视频生成FVD降低23.9%，闭环PDMS提升至90.4，从零训练收敛加速约2倍。"
conclusion: ReWorld通过直接优化中间表示为自动驾驶世界模型提供高效表示学习方案，显著提升生成质量与规划性能。
---

## 摘要
世界动作模型（WAMs）在动作条件下建模未来环境演化，为自动驾驶提供了一种可扩展的范式。然而，现有方法主要关注模型架构设计，而WAM如何高效学习更好的世界表示用于规划仍未得到充分探索。为解决这一问题，我们提出了ReWorld，这是首个专门为自动驾驶世界动作模型设计的表示学习框架。在WAM中，标准训练仅监督生成和规划模块的输出端，使得承载世界知识的中间表示只能作为拟合这些输出的副产品被间接塑造。ReWorld的核心思想是将中间表示作为优化的直接目标，沿三个互补维度对其进行塑造。在负责生成的视频DiT上，我们对其中间表示施加未来预测监督。在负责规划的动作DiT上，我们首先将其中间表示与视频世界表示进行跨模态对齐，然后通过硬负样本监督进一步塑造其在安全关键边界附近的判别性。此外，我们系统分析了现有表示学习方法在视频生成世界模型中的有效性，并讨论了为何其在此任务上性能有限。在nuScenes和NAVSIM上的实验表明，ReWorld将微调后的视频生成FVD提升了23.9%（从81.3降至61.9），将闭环PDMS从89.1提升至90.4，且无需任何后训练（如强化学习或后处理），并将从头训练的收敛速度加快了约2倍。

## Abstract
World Action Models (WAMs) model future environment evolution under action conditioning, offering a scalable paradigm for autonomous driving. However, existing approaches focus largely on model architecture design, and how a WAM can efficiently learn better world representations for planning remains underexplored. To address this gap, we propose ReWorld, the first representation learning framework specifically designed for autonomous-driving world action models. In WAMs, standard training supervises only the output ends of the generation and planning modules, leaving the intermediate representations that carry world knowledge to be shaped only indirectly, as byproducts of fitting these outputs. The core idea of ReWorld is to treat intermediate representations as direct targets of optimization, shaping them along three complementary dimensions. On the Video DiT responsible for generation, we impose future-predictive supervision on its intermediate representations. On the Action DiT responsible for planning, we first align its intermediate representations cross-modally with the video world representation, then further shape them to be discriminative around safety-critical boundaries via hard-negative supervision. In addition, we systematically analyze the effectiveness of existing representation learning methods in video generation world models, and discuss why their performance is limited on this task. Experiments on nuScenes and NAVSIM show that ReWorld improves fine-tuned video generation by 23.9% in FVD (81.3 to 61.9), raises closed-loop PDMS from 89.1 to 90.4 without any post-training such as RL or post-processing, and accelerates from-scratch convergence by approximately 2x.

---

## 论文详细总结（自动生成）

# 论文总结：ReWorld: Learning Better Representations for World Action Models

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有世界动作模型（WAMs）如 DriveLaW 在训练时仅监督生成（视频输出）和规划（轨迹输出）的末端，中间层的表示从未被直接优化。这些中间表示承载着世界知识，却只能作为输出拟合的副产品被间接塑造，导致生成质量与规划能力解耦——模型虽能生成逼真的未来帧，却不一定会产生更好的规划结果。
- **整体含义**：论文提出将中间表示作为直接优化目标，是提升 WAM 效率与效果的新范式。通过专门的表示学习框架，在无需外部编码器或教师模型的情况下，同时改善生成质量、收敛速度与闭环规划安全性。

## 2. 论文提出的方法论

- **核心思想**：在 DriveLaW 的链式架构（Video DiT 生成 → Action DiT 规划）基础上，沿三个互补维度直接优化中间表示：
  1. **未来预测世界表示（Video DiT 侧）**：
     - 在选定的中间层添加轻量级预测头，监督其预测流匹配速度目标 \( \hat{v}_t^{(l)} \) ，损失函数 \( \mathcal{L}_{\text{Mid}} \)。
     - 推理时利用浅层与深层预测的差异进行自引导 extrapolation：\( v_w = v_i + \gamma(v_f - v_i) \)。
  2. **世界接地动作表示（Action DiT 侧）**：
     - 对动作 DiT 的每个交叉注意力层，强制动作隐状态与其注意力读出的视频表示进行余弦相似度对齐，损失 \( \mathcal{L}_{\text{align}} \)；视频读出处使用 stop-gradient 避免干扰生成分支。
  3. **安全感知动作表示（联合微调阶段）**：
     - 为每个训练场景构造一个离线候选轨迹池，用 PDM 模拟器评分，选出几何接近专家轨迹但评分低的硬负样本。
     - 引入排斥距离损失 \( \mathcal{L}_{\text{RDE}} \)，在 delta 表示空间最大化预测轨迹与硬负样本的距离。
- **训练流程（三阶段）**：
  - Stage 1：训练 Video DiT，优化 \( \mathcal{L}_{\text{Gen}} + \lambda_{\text{Mid}} \mathcal{L}_{\text{Mid}} \)。
  - Stage 2：冻结 Video DiT，训练 Action DiT，优化 \( \mathcal{L}_{\text{FM}} + \lambda_{\text{align}} \mathcal{L}_{\text{align}} \)。
  - Stage 3：联合微调两个 DiT，优化 \( \mathcal{L}_{\text{FM}} + \lambda_{\text{RDE}} \mathcal{L}_{\text{RDE}} \)（不包含对齐损失）。

## 3. 实验设计

- **数据集**：
  - **视频生成**：训练语料为 nuPlan（~1200 小时真实驾驶数据）和 nuScenes（1000 个城市驾驶序列，850 开发/150 测试）。
  - **闭环规划**：NAVSIM（非反应式数据驱动基准），包含 Navtrain（~103k 场景）和 Navtest（~12k 场景），基于 nuPlan 的 OpenScene 构建。
- **评估指标**：
  - 视频生成：FVD（弗雷歇视频距离），不报告 FID 因其无法反映时序一致性。
  - 规划：五个子指标（NC, DAC, TTC, Comf, EP）及聚合 PDMS。
- **对比方法**：
  - 视频生成：DriveGAN, DriveDreamer, DrivingGPT, Vista, Epona, DriveLaW。
  - 规划：传统端到端方法（VADv2, UniAD, TransFuser, PARA-Drive, DiffusionDrive 等）和世界模型方法（Epona, DrivingGPT, LAW, ReSim, WoTE, DriveVLA-W0, PWM, WorldDrive, DriveLaW 等）。
  - 表示学习方法对比（从零训练 120k 步）：Vanilla Flow, SRA, SRA2, Self-Flow, REPA（使用 DINOv2/VideoMAEv2/DepthAnything3/V-JEPA2）, ReDi。

## 4. 资源与算力

- **明确信息**：论文**未明确报告**训练所使用的具体 GPU 型号、数量或总训练时长。
- 仅提供了训练步数、批大小等设置：
  - Stage 1：全局 batch 64，20k 步（从 LTX-Video 预训练权重继续训练）。
  - Stage 2：全局 batch 128，6k 步。
  - Stage 3：全局 batch 160，10k 步。
  - 从零训练对比实验：全局 batch 32，120k 步，输入尺寸 224×224×25。
- 计算效率分析中，归一化训练成本显示 ReWorld 中间表示监督仅带来 0.3% 的额外开销（Vanilla 为 1.0×，ReWorld 为 1.003×），远低于 REPA（~1.7×）等方法。

## 5. 实验数量与充分性

- **实验数量**：共 8 张量化表格和 1 张定性对比图。
  - 主结果：视频生成 FVD 对比（表1）、闭环规划 PDMS 对比（表3）。
  - 表示学习方法对比（表2）。
  - 消融实验：规划组件增量消融（表4）、监督层选择（表5）、自引导强度（表6）、排斥损失权重（表7）、计算效率对比（表8）。
  - 定性结果（图3）：展示高速场景下的生成质量。
- **充分性与公平性**：
  - 与 DriveLaW 相同预训练权重、相同数据，对比公平。
  - 消融覆盖了主要超参数，但缺少对对齐损失权重 \( \lambda_{\text{align}} \)、硬负样本池大小、不同距离度量等的进一步消融。
  - 与外部表示学习方法对比时保持统一训练条件（去掉 text encoder），但未讨论不同视频长度的影响。
  - 总体实验较为充分，结论可信；但未在更多数据集（如 Waymo、CARLA）上验证泛化性。

## 6. 论文的主要结论与发现

- **生成质量提升**：在 nuScenes 上微调后的 FVD 从 81.3 降至 61.9，相对提升 23.9%。
- **闭环规划提升**：PDMS 从 89.1 提升至 90.4（+1.3），无需任何后训练（如 RL 或后处理）；NC、DAC、TTC 均为世界模型方法中最优。
- **收敛加速**：从零训练约加速 2 倍。
- **表示学习方法的局限性**：外部对齐方法（如 REPA）在视频生成中效果有限甚至劣于基线，原因在于外部编码器预训练于静态图像或短片段，缺乏长时动态建模能力，且引入竞争梯度；而自监督内部表示（如 ReWorld）更稳定高效。
- **各组件互补**：世界接地（+0.4 PDMS）和安全感知（+0.7 PDMS）组合后提升更大（+1.3）。

## 7. 优点

- **理念新颖**：首次将中间表示直接优化引入 WAM，提出三维度表示塑造，填补了该方向的空白。
- **方法简洁高效**：无需外部编码器或教师模型，附加训练开销极小（~0.3%），易于复现和推广。
- **同时提升生成与规划**：不牺牲生成质量的情况下提升规划安全性，且收敛更快。
- **深入分析**：系统对比了现有表示学习方法在视频生成上的表现，并解释了其失败原因，为后续研究提供参考。
- **实验设计严谨**：与 DriveLaW 相同基础架构，消融实验覆盖主要变量，计算效率对比规范。

## 8. 不足与局限

- **算力信息缺失**：未报告 GPU 型号、数量、具体训练时长，影响可复现性评估。
- **数据集范围有限**：仅在 nuScenes 和 NAVSIM (nuPlan) 上验证，未在 Waymo、CARLA 或真实闭环环中测试泛化性。
- **硬负样本偏差**：依赖 PDM 模拟器评分构造硬负样本，可能引入模拟器固有偏差；且离线池大小固定（64 个候选），未探讨池大小影响。
- **消融不完整**：缺少对 \( \lambda_{\text{align}} \)、中间监督层数量、硬负样本选取策略（如不同 δ 阈值）的消融；未比较不同距离度量（如 L2 vs. delta 表示）。
- **推理成本未分析**：虽训练开销小，但推理时使用自引导（额外一次前向）和硬负样本是否需要，未讨论推理时间。
- **与最新方法对比有限**：引用截止于 2026 年部分方法，可能遗漏更近期的工作。
- **可迁移性待验证**：基于 DriveLaW 架构设计，能否直接应用于其他 WAM 架构（如 Epona、DrivingGPT）未证明。

（完）
