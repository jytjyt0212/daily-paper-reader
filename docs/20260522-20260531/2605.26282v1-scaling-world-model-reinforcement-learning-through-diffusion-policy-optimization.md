---
title: Scaling World-Model Reinforcement Learning Through Diffusion Policy Optimization
title_zh: 通过扩散策略优化扩展世界模型强化学习
authors: "Xiaoyuan Cheng, Wenxuan Yuan, Zhancun Mu, Yuanzhao Zhang, Yiming Yang, Hai Wang, Zhuo Sun, Che Liu"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.26282v1"
tags: ["query:world-models"]
score: 8.0
evidence: 基于扩散策略优化的世界模型强化学习
tldr: 基于世界模型的强化学习中，搜索与价值学习的结构错位是限制规模化应用的关键瓶颈。为解决此问题，提出模型基扩散策略优化（MBDPO），将策略优化重构为潜空间中搜索轨迹的扩散过程，并利用数据集隐式能量函数优化评分场，消除错位。实验表明，该方法在多任务离线预训练、在线学习和微调中均表现优越，且离线场景下性能随模型容量单调提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26282v1/fig-001.webp\", \"caption\": \"\", \"page\": 3, \"index\": 1, \"width\": 1100, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-002.webp\", \"caption\": \"\", \"page\": 3, \"index\": 2, \"width\": 1099, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-003.webp\", \"caption\": \"\", \"page\": 3, \"index\": 3, \"width\": 383, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-004.webp\", \"caption\": \"\", \"page\": 3, \"index\": 4, \"width\": 1100, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-005.webp\", \"caption\": \"\", \"page\": 3, \"index\": 5, \"width\": 1100, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-006.webp\", \"caption\": \"\", \"page\": 6, \"index\": 6, \"width\": 5192, \"height\": 1650}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-007.webp\", \"caption\": \"\", \"page\": 6, \"index\": 7, \"width\": 4050, \"height\": 1399}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-008.webp\", \"caption\": \"\", \"page\": 21, \"index\": 8, \"width\": 1659, \"height\": 948}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-009.webp\", \"caption\": \"\", \"page\": 21, \"index\": 9, \"width\": 1644, \"height\": 957}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-010.webp\", \"caption\": \"\", \"page\": 22, \"index\": 10, \"width\": 1600, \"height\": 540}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-011.webp\", \"caption\": \"\", \"page\": 22, \"index\": 11, \"width\": 1991, \"height\": 789}]"
motivation: 世界模型RL中搜索与价值学习存在结构错位，导致训练不一致，限制了规模化扩展。
method: 提出MBDPO，将策略优化重构为搜索轨迹的扩散过程，利用数据集隐式能量函数细化评分场。
result: 多任务离线预训练、在线学习等设置下均有效，离线时性能随模型容量单调增长。
conclusion: 通过扩散统一搜索与策略优化，解决了结构错位，实现了可扩展的世界模型RL。
---

## 摘要
基于模型的强化学习可以通过使用世界模型在大规模上得到有效支持。然而，在实践中，扩展这类方法仍然受到根本限制。一个公认的挑战是模型偏差和误差累积，这会降低长期预测的质量。除了这些问题，我们还发现一个更关键但未被充分探索的瓶颈：现有世界模型方法中搜索与价值学习之间的结构性错位。具体而言，策略改进通常依赖于由独立的非搜索策略诱导的价值函数，导致训练不一致，最终导致次优学习。为解决这一限制，我们提出世界模型中的基于模型的扩散策略优化（MBDPO），该框架通过扩散策略表示统一了搜索和策略优化，从而释放世界模型在可扩展策略学习中的潜力。我们不是在学习到的世界模型上构建显式规划器，而是将策略优化重新表述为潜在世界模型中搜索轨迹上的扩散过程。基于此，我们从收集的数据集中提取一个隐式能量函数来锚定策略，使MBDPO能够优化用于策略提升的分数场，同时缓解错位问题。我们在多种设置下评估MBDPO，包括多任务离线预训练、在线学习和离线到在线微调。在离线场景中，我们通过在大规模数据集上进行预训练进一步研究其扩展行为，观察到随着模型容量的增加，性能呈现一致且单调的提升。

## 速览
**TLDR**：世界模型在模型基强化学习中面临模型偏差和误差累积问题，但更关键的瓶颈是搜索与价值学习之间的结构性错位，导致策略改进不一致。本文提出MBDPO框架，利用扩散策略表示在潜在世界模型中将策略优化重构为对搜索轨迹的扩散过程，提取隐式能量函数锚定策略并修正得分场，从而缓解错位。在多任务离线预训练、在线学习和离线到在线微调中，MBDPO均取得显著改进，并且离线预训练时随模型容量增加性能单调提升。 \
**Motivation**：现有世界模型方法中搜索与价值学习存在结构性错位，导致策略改进不一致，限制了模型的扩展能力。 \
**Method**：提出MBDPO框架，将策略优化转化为对世界模型中搜索轨迹的扩散过程，利用隐式能量函数锚定策略并优化得分场。 \
**Result**：在多任务离线预训练、在线学习和离线到在线微调场景中均表现优异，离线预训练随模型容量增加性能持续提升。 \
**Conclusion**：MBDPO通过统一搜索与策略优化，有效解决了世界模型中的关键瓶颈，展示了可扩展的策略学习潜力。

---

## Abstract
Model-based reinforcement learning (RL) can be effectively supported at scale through the use of world models. However, in practice, scaling such approaches remains fundamentally limited. A commonly recognized challenge is model bias and error compounding, which degrade long-horizon predictions. Beyond these issues, we identify a more critical yet underexplored bottleneck: a structural misalignment between search and value learning in existing world model approaches. In particular, policy improvement often relies on value functions induced by a separate, non-search policy, resulting in training inconsistency and ultimately suboptimal learning. To address this limitation, we propose Model-Based Diffusion Policy Optimization (MBDPO) in world models, a framework that unifies search and policy optimization through diffusion policy representations, thereby unlocking the potential of world models for scalable policy learning. Instead of constructing an explicit planner over a learned world model, we reformulate policy optimization as a diffusion process over searched trajectories in latent world models. In this view, we extract an implicit energy function from the collected dataset that anchors the policy, enabling MBDPO to refine the score field for policy optimization while mitigating misalignment. We evaluate MBDPO across a wide range of settings, including multi-task offline pretraining, online learning, and offline-to-online fine-tuning. In the offline regime, we further investigate its scaling behavior by pretraining on large-scale datasets, observing consistent and monotonic performance gains with increasing model capacity.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

**研究动机**：基于模型的强化学习（MBRL）通过世界模型进行规划被认为具有样本效率优势，但实际扩展时面临根本限制。通常认为主要障碍是模型偏差和预测误差累积导致长期预测退化。然而，本文指出一个更关键但被忽视的瓶颈：**现有世界模型方法中搜索（search）与价值学习（value learning）之间存在结构性错位**。

- 现有方法（如TD-MPC系列）使用独立于搜索过程的非搜索策略网络（β）生成数据来训练价值函数（Q），而策略改进则依赖基于世界模型的启发式搜索（如MPPI）生成的轨迹，导致分布不匹配。
- 这引发两个问题：  
  - **价值迭代差距**：搜索策略π使价值函数被问询到分布外的状态-动作对，产生过乐观估计，误差被放大。  
  - **策略改进差距**：真实改进可能远小于估计改进，因为Q函数在π偏离β时不可靠。
- 这些错位导致训练不一致、性能次优，且限制了世界模型的可扩展性。

**整体含义**：本文旨在通过统一搜索与策略优化来释放世界模型在策略学习中的潜力，实现可扩展的模型基强化学习。

## 2. 方法论：MBDPO

**核心思想**：将策略优化重新表述为世界模型中潜空间搜索轨迹的扩散过程，利用从数据集中提取的隐式能量函数来锚定策略，并用扩散策略表示统一搜索与策略优化，消除结构错位。

**关键技术细节**：

1. **策略目标**：在KL约束下最大化期望回报  
   ```math
   \max_{\pi_\phi} \mathbb{E}_{a\sim\pi_\phi}[G(z_t, a_{t:t+H})] \quad \text{s.t.} \quad D_{KL}(\pi_\phi\|\beta) \leq \varepsilon
   ```
   其中β为非搜索行为策略，H为规划水平。

2. **隐式能量函数**：通过对比学习从数据集学习能量函数Eθ，使其近似于-log β，从而隐式编码KL约束，无需显式计算β密度。  
   能量函数被吸收进奖励：`\tilde{R}(z_h,a_h) = R(z_h,a_h) - \eta\gamma^{h-t}E(z_h,a_h)`。

3. **扩散策略**：采用DDPM框架，将动作序列从高斯先验逐步去噪至最优Gibbs策略。  
   - 反向扩散核由评分函数φ引导，评分函数通过世界模型中的想象轨迹估计：  
     ```math
     \phi(z_t,a_{t:t+H}^\tau,\tau) = -\frac{a_{t:t+H}^\tau}{1-\bar\alpha_\tau} + \frac{\sqrt{\bar\alpha_\tau}}{1-\bar\alpha_\tau}\mathbb{E}_{a^0}\left[ w(a^0) \cdot a^0 \right]
     ```
     其中权重w由能量正则化累计回报`\tilde{G}`的softmax定义。
   - 蒙特卡洛采样近似期望值，通过世界模型滚动出轨迹并计算权重。

4. **训练流程**（算法1）：  
   - 世界模型（编码器E、潜动力学F、奖励R、价值Q、能量E）通过联合损失（14）更新。  
   - 扩散策略的评分网络通过监督L2损失（16）训练，标签由世界模型产生。  
   - 支持在线、离线、离线到在线多种范式。

**理论保证**：定理1证明当评分匹配条件满足时，扩散策略等价于KL约束下的最优Gibbs策略，即搜索与策略优化统一。

## 3. 实验设计

**数据集/场景**：使用四个主流基准共121个任务  
- **DMControl** (39 tasks)：包括Acrobot、Cheetah、Walker、Humanoid等  
- **MetaWorld** (50 tasks)：多种机器人操纵任务  
- **ManiSkill2** (5 tasks)：物体操纵，含YCB物体集  
- **MyoSuite** (10 tasks)：肌肉骨骼运动控制（手部精细操作）  
- 额外包含**Visual RL**（10个图像输入任务）和**Locomotion**（7个高维运动任务）

**对比方法**：  
- SAC（模型无关离线策略）  
- DreamerV3（领先的基于世界模型的方法）  
- TD-MPC（TD-MPC系列首个工作）  
- TD-MPC2（最新代表作）

**实验设置**：  
- 在线训练：每个任务平均3个随机种子，报告奖励或成功率。  
- 多任务离线预训练：使用80个任务和30个任务数据集，模型参数量从1.7M到340M。  
- 离线到在线微调：在70个任务预训练后，在10个未见任务上仅用40k步交互微调。

## 4. 资源与算力

文中明确提及：
- **在线训练**：使用单张NVIDIA A800-80GB GPU（每步时间表1(a)给出，例如MetaWorld平均1.08ms/步）  
- **多任务离线预训练**：使用单张NVIDIA H200 GPU，训练时长如表1(b)所示（1.7M模型39.9小时，340M模型206.8小时）。  
- **消融实验**：单个A800-80GB，每次实验约数十小时。  
- **总实验量**：约覆盖121个任务 × 3种子 × 多种对比方法，加上消融实验，总GPU时长估计在数万GPU小时量级。

## 5. 实验数量与充分性

- **实验数量**：非常充分。涵盖：  
  - 4个基准121个任务的在线从头训练  
  - 多任务离线预训练（2个数据集×5个模型规模）  
  - 离线到在线微调（10个未见任务）  
  - 消融实验：正则化因子η（8个值）、扩散步数N（5种）、蒙特卡洛样本数T（5种）  
  - 额外分析：交叉TD误差、动作漂移、潜轨迹可视化（80个任务）  
- **充分性与公平性**：  
  - 对比方法均为SOTA，且严格遵循官方设置（TD-MPC架构相同，仅替换搜索部分）。  
  - 所有任务使用相同超参数，避免调参偏袒。  
  - 消融实验覆盖关键设计维度。  
  - 缺点：未在真实机器人上验证；未扩展到更大规模（如数十亿参数）的模型与数据集。

## 6. 论文的主要结论与发现

1. **性能优势**：MBDPO在所有在线基准上一致优于SAC、DreamerV3、TD-MPC和TD-MPC2，尤其在视觉控制和困难操纵任务中提升显著。  
2. **可扩展性**：多任务离线预训练中，性能随模型参数量（1.7M→340M）单调提升，且在小模型时优势更明显，说明扩散策略在弱世界模型下更稳定。  
3. **缓解错位**：MBDPO有效降低了交叉TD误差和策略漂移，验证了理论分析（问题1和2）的解决。  
4. **迁移能力**：离线到在线微调显著优于从零训练，证明预训练世界模型和策略的有效性。  
5. **表示质量**：潜轨迹可视化显示MBDPO生成更符合物理规律的闭环（周期性任务）和直线性（操纵任务）轨迹，而TD-MPC2更杂乱。  
6. **计算效率**：在线训练时间与TD-MPC2相当甚至更快（视觉RL），离线预训练时间增量不大（1.02×-1.32×）。

## 7. 优点

- **理论清晰**：严格证明搜索与策略优化的错位，并提出KL约束和隐式能量函数作为解决方案，具有理论保证。  
- **创新性强**：将扩散模型与基于世界模型的RL深度结合，为“将搜索转化为策略优化”提供了新范式。  
- **实验全面**：覆盖121个任务，多种设置（离线、在线、微调），消融充分，可视化深入。  
- **性能突出**：在几乎所有指标上达到SOTA，且可扩展性有数据支撑。  
- **开源**：提供代码和Hugging Face模型，可复现。

## 8. 不足与局限

- **规模限制**：最大模型340M，未测试更大（如几十亿参数）的缩放行为；计算资源有限，未探索多GPU并行训练效率。  
- **环境局限**：仅在模拟器中评估，未部署到真实机器人，实际机器人中的模型误差和系统识别问题未考虑。  
- **任务偏差**：“Hopper Hop”任务表现次优，分析指出其潜嵌入分布偏移，说明表示质量问题仍会影响优化。  
- **超参数敏感性**：正则化因子η在[0.3,1]内稳定，但极端值（0或5）性能下降明显，需调参。  
- **与表示学习的关系**：依赖TD-MPC架构的表示学习，未探索更强大的预训练视觉骨干；表示质量直接影响策略优化。  
- **假设简化**：隐式能量函数近似log β时假设负样本均匀分布，在复杂多模态动作分布下可能不够准确。

（完）
